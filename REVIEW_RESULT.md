1. tests/server/boar_brain.spec.luau:244-252 — the new control case "does not turn hard when it is moving freely" is vacuous, so claim 25's "The pair fails if the branch is deleted *and* fails if the branch fires when it should not" is false: the boar is despawned long before the anti-stuck branch could ever fire, and the assertion is then measured over nothing. Evidence: `:249` supplies `positionAt(tick) = Vector3.new(0, 0, -CONFIG.STUCK_DIST * tick)` = `(0, 0, -3 * tick)`. The brain enters `FLEE` on tick 1 (threat at `(0,0,30)`, flat distance 33 ≤ `DETECT_RADIUS` 40) and never calms (distance exceeds `CALM_RADIUS` only from tick 14, and `_sinceCalm` accrues `SENSE_INTERVAL` once per 12 ticks, so it reaches 0.8 s against `CALM_TIME = 4` before the run ends). At tick 64, `z = -192 <= FIELD.exitZ = -190` (`:19`) and `Brain.luau:162` `if self._state == "FLEE" and position.Z <= field.exitZ then return "escaped"` fires; from tick 65 `Brain.luau:269-271` (`if self._state == "GONE" then return intent end`) returns an intent whose `facing` is `nil`, so `biggestTurn`'s `if previous and facing then` (`:215`) never measures again. The stuck branch cannot fire before tick 240: `Brain.luau:242` needs `_stuckTime >= STUCK_TIME` (2 s) per increment and `Brain.luau:358` needs `self._stuckCount >= 2`, i.e. 4 s. So over the 62 ticks actually measured `stuck` is never even true, and `expect(moving <= CONFIG.TURN_RATE * DT + 1e-6)` (`:251`) is a restatement of the existing "never turns faster than TURN_RATE" test (`:266-284`), not a control on the stuck branch. Required: keep the control boar inside `FIELD.bounds` and short of `exitZ` for the whole `ticks` window (e.g. move it back and forth by more than `STUCK_DIST` per tick), and assert at the end that it is still `FLEE`/not despawned, so a silent despawn cannot make the control pass.

2. REVIEW_REQUEST.md claim 1 — false as written against the repo, on the same point rounds 1 and 2 already corrected twice. Evidence: claim 1 says the grep hits outside `init.luau` are "the **definitions** in `Body.luau` and `Brain.luau` plus exactly one call: `tests/server/boar_brain.spec.luau` calls `Brain.new(CONFIG, FIELD, START, Random.new(...))`". `grep -rn "Brain\.new" src tests` returns two calls in that spec, not one: `tests/server/boar_brain.spec.luau:26` `return Brain.new(CONFIG, FIELD, START, Random.new(seed or 1))` and `tests/server/boar_brain.spec.luau:343` `local brain = Brain.new(CONFIG, CONFIG.field, spawn, Random.new(9))` — the latter added by this round's own fix for round-2 finding 6. Required: restate claim 1 to match the repo (two spec call sites, one of which uses the production `CONFIG.field` and `CONFIG.spawnPoint`).

3. docs/research/2026-09-24-boar-ai.md:181-193 — the durable record of deviations from the design is now incomplete, and its heading is wrong, which is the same pattern as round-2 finding 4. Evidence: the addendum section is headed "### 3. Two corrections to the design's formulas (§5), made in the code" and lists only `onExitSide` and the home pull; `TASKS.md` ("Builder's note") likewise says "Two of its formulas are wrong … Both are corrected in the code … recorded in the research-note addendum". But this round's code deviates from the design in two further places that appear nowhere in the note: (a) `docs/design/boar-ai.md:234` "**Despawn.** `position.Z ≤ field.exitZ` → `escaped`" versus `src/server/Boar/Brain.luau:162` `if self._state == "FLEE" and position.Z <= field.exitZ then`; (b) `docs/design/boar-ai.md:322` "`spawnPoint` | `Vector3.new(-40, 0, -60)`" versus `src/server/Boar/init.luau:83` `spawnPoint = Vector3.new(-40, 0, 20),`. Both are also contradicted by `docs/design/boar-ai.md:471` ("crossing `exitZ` gives `despawn = true, reason = "escaped"` once"), which the spec now satisfies only after first driving the brain into `FLEE` (`tests/server/boar_brain.spec.luau:311-316`). `REVIEW_REQUEST.md` is rewritten fresh per task (CLAUDE.md loop step 4), so claim 29 is not the record. Required: add the two deviations, with their reasons, to the research-note addendum, and drop or fix the "Two corrections" framing there and in claims 12-13.

4. tests/server/boar_brain.spec.luau:15 — the reaction assertion is twice as loose as the design's live-spec list requires, undeclared; claim 4 presents 0.5 s as the figure under test without saying the design asked for 0.25 s. Evidence: `docs/design/boar-ai.md:331` "Reaction: state is `FLEE` within **0.25 s** of a threat inside `DETECT_RADIUS` | pure spec, exact" and `docs/design/boar-ai.md:457` "enters `FLEE` within **0.25 s** of a threat at 39 studs (the ≤ 0.5 s requirement)". The spec defines `local REACTION_LIMIT = 0.5 -- seconds; the Director's requirement, not a CONFIG value` and asserts `expect(elapsed <= REACTION_LIMIT).to.equal(true)` (`:76`), so the test would still pass with `SENSE_INTERVAL` raised from 0.2 to ~0.45 — the very regression the 0.25 s bound exists to catch. This is the identical category as round-2 finding 4(a) (`WANDER_SPEED * 2` against the design's `* 1.5`), which was restored. Required: assert against 0.25 s as the design specifies, or record the deviation in the research-note addendum and in the claim.

---
REVIEWER verdict on commit `e821aa4d30ca95ddaafbe0933348a8da340299d2` (round 3) · 2026-09-24 20:48 UTC · session cost $2.54, 32 turns · written by tools/agents.py
