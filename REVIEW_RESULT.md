1. docs/research/2026-09-24-map-generator.md:199 — a new broken `§` cross-reference, introduced by the commit that fixes round-1 finding 3 (broken `§` cross-references). Evidence: :199 "**Bad, and it is why §12 below is written the way it is:**". The note has no §12: the numbered sections run 1–11 (`### 11. \`math.noise\`` at :193 is the last), and every later heading is unnumbered ("Numeric targets" :205, "Pattern adopted, and why" :227, "The smallest first generator task" :282, "What needs Karen" :308). Every other `§` in the file resolves (grep of `§`: 24, 26, 53, 90, 175, 214, 219, 220, 232, 234, 238, 248, 260, 265, 268, 277, 278, 293, 295, 296, 302, 314). REVIEW_REQUEST.md:146-150 claims this class of defect is fixed: "Finding 3 — three sets of broken section references … All three were wrong in the same way". Required: point :199 at what it means ("The smallest first generator task"), and re-check every `§` in the file rather than only the three the Reviewer named.
2. REVIEW_REQUEST.md:29 and :46 — the request still describes the note as having ten sources, in the change list and in claim 2's own verification pointer, while claim 2 says eleven. Evidence: :29 "| `docs/research/2026-09-24-map-generator.md` | the note: 10 sources with licence and maintenance, …"; :45-46 "**Rule 1 is met in substance: 11 sources, each with a licence and a maintenance status …** Verify: the note's "Sources", headings 1–10." The note has eleven headings (`### 1.` :30 … `### 11.` :193), and TASKS.md:23 says "11 sources". So the file the Director's merge gate reads (CLAUDE.md:153-155) states a false count, and the pointer given for verifying the eleven-source claim excludes source 11 — the source added this round to fix finding 2. Required: make :29 and :46 say eleven and 1–11.
3. REVIEW_REQUEST.md:166-167 — the "two table rows" summary that round-1 finding 5 called false is still in the request; item 21's claim that it is fixed is only half true. Evidence: :166-167 "**N/A — no code, and Studio is unreachable.** Nothing in this change executes: one markdown document and two table rows."; :153-154 "Finding 5 — `ESCALATE.md` was in the change but in no claim, and the summary said "two table rows".** Both fixed: it is in the table, the summary is corrected". Only the copy at :13-14 was corrected ("one new document plus edits to three existing ones"). The change is four files (`.agent-evidence/changed-files.txt:1,4,5,6`: ESCALATE.md, TASKS.md, docs/research/2026-09-24-map-generator.md, docs/research/INDEX.md), including a 47-line `ESCALATE.md` entry and a ~45-line "Director dispatches" section in TASKS.md (TASKS.md:25-70) that is not a table row. Required: correct :167 too, or delete the sentence.
4. docs/research/2026-09-24-map-generator.md:67-70 — the decisive finding's second leg rests on two sources that are never named or linked, which is round-1 finding 2 repeated, and the request states it as an exhaustiveness claim off an elided list. Evidence: :67-70 "Studio MCP server exposes no tool for them either (its tool list is `execute_luau`, `insert_asset`, `search_asset`, `screen_capture`, `generate_*`, `run/stop play`, input, console — nothing that drives the Terrain Editor). Community threads asking to automate multi-heightmap imports find no API answer." Neither the MCP tool list nor those community threads is one of the eleven numbered sources — no URL, no licence, no maintenance status (CLAUDE.md:13 "3+ external sources, named and linked, with licence and maintenance status"), and neither appears under "Could not verify" (REVIEW_REQUEST.md:174-206). The repo's only record of the MCP tool set is a different, shorter one that omits `insert_asset`, `search_asset` and `generate_*`: docs/research/2026-09-24-toolchain.md:33 "Real Play mode: `start_stop_play`, `get_console_output`, `execute_luau` per DataModel, `screen_capture`", and tools/studio_mcp.py wraps only `get_console_output` (:263), `start_stop_play` (:266) and `execute_luau` (:269) — so nothing in the repo backs the list either. REVIEW_REQUEST.md:41-43 then asserts absence from a list it ends with an ellipsis: "(`execute_luau`, … `get_console_output`, …) contains nothing that drives the Terrain Editor". Required: cite the MCP tool list as a numbered source (or cite the in-repo record and say it is partial), link the community threads as source 6 does, and either drop the "contains nothing" wording or state what the list was enumerated from.
5. docs/research/2026-09-24-map-generator.md:219 — the mesh limits are presented as Roblox's hard limits in the numbers table while the note's own source entry says they come from community/vendor pages, and unlike the part-count figures they are not listed as unverified. Evidence: :219 "| **Mesh budget per prop** | **≤ 21,000 triangles**, textures **≤ 1024 × 1024** | Roblox's hard import limits (§9)"; :162 "Licence: first-party docs for the APIs; the mesh-limit figures are community/vendor pages"; the §9 URLs for the limits are :158-159 "<https://devforum.roblox.com/t/whats-roblox-current-mesh-triangle-limit/2414597> · <https://meshlox.com/learn/roblox-mesh-size-limits>", and :164-168 asserts from them that "An import over the limit **fails at import with an error**". REVIEW_REQUEST.md:95-96 repeats it as fact ("the mesh limits (21,000 triangles, 1024 × 1024 textures) are Roblox's hard import limits") while :47-48 concedes "DevForum posts cited as figures only for 6 and the mesh limits", and "Could not verify" flags only the part counts (:195-196 "**The part-count figures are DevForum rules of thumb**"). Required: label the mesh limits as community/vendor figures where they are used, as source 6's figures are (:115 "That is where the part budgets in "Numeric targets" below come from", :116-117 "these are rules of thumb"), or cite a first-party page for them.

---
REVIEWER verdict on commit `41e9069e9d266867e7aa71ecbcc7acd5f1512bf9` (round 2) · 2026-09-24 21:44 UTC · session cost $1.56, 21 turns · written by tools/agents.py
