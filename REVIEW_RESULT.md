1. tests/server/boar_body.spec.luau:239-269 — the new test "can route across the REAL arena" never checks that the real arena is there, so it can pass over the default Baseplate alone and prove nothing about routing around cover. Evidence: the test body is `local production = Boar.defaultWorld()` (`:244`) and a `requestPath` from `CONFIG.spawnPoint + (0, BODY_SIZE.Y/2, 0)` to `(spawnPoint.X, groundY, exitZ)` (`:246-252`) — there is no reference to `Workspace.TestArena` and no wait for it, while `tests/server/test_arena.spec.luau:15` states the reason one is needed: "Script start order is not guaranteed, so give ArenaBoot up to 5 s, as sync.spec does for SyncCheck." The arena exists only after `src/server/ArenaBoot.server.luau:8` `TestArena.build()` runs; `ESCALATE.md:145-146` records that `Workspace` already holds a default Baseplate "2048×16×2048 at y = -8, so its top face is at **y = 0**", which carries the whole route on its own. REVIEW_REQUEST.md claim 6 calls this "the assertion that would catch the navmesh being genuinely unusable for a 2 × 3 × 5.5 agent" and offers the Edit-mode probe table as support, but in Edit mode no server script has run, so `Workspace.TestArena` does not exist and rows 1-3 of that table were measured on the bare Baseplate (commit `87769c1` labels them "baseplate 0,0 -> 0,-190"). The route is exactly the case that needs the arena: from `(-40, 20)` to `(-40, -190)` it passes straight through `WallWest` (`src/server/TestArena.luau:38`, x = -60, z = -100, size 80 × 8 × 4, footprint x ∈ [-100,-20], z ∈ [-102,-98]), yet the probe returned 54 waypoints for 210 studs — a straight line, i.e. no detour, i.e. no wall in the navmesh. Required: wait for `Workspace.TestArena` (as `test_arena.spec.luau:16-24` does) and fail if it is absent, before probing; and correct claim 6's probe table to say those rows were taken with no arena present.

2. src/server/Boar/init.luau:113,172,188-190 — `lastPathProblem` is a single overwritten slot, so REVIEW_REQUEST.md claim 6's "**requires the reason to be `NoPath`**, so any other failure is new and fails" is false. Evidence: `local lastPathProblem = nil :: string?` (`:113`) is assigned `lastPathProblem = if ok then tostring(path.Status) else "error: " .. tostring(err)` (`:172`) on every failure, and `lastPathProblem()` (`:188-190`) returns only "the most recent reason". `tests/server/boar_body.spec.luau:225-236` reads it once after the whole run and asserts `expect(problem).to.equal(tostring(Enum.PathStatus.NoPath))`. With 36 of 37 requests failing, a `pcall` error or a different `PathStatus` on any request but the last is silently discarded and the test still passes. Required: record the distinct reasons (or a per-reason count) and assert over all of them, or restate claim 6 to what the code checks — the last failure's reason only.

3. docs/research/2026-09-24-boar-ai.md:181-210 — this round adds a fifth departure from the Architect's design and it is not in the durable record, which is the identical defect the round-3 review raised as finding 3. Evidence: `docs/design/boar-ai.md:494-495` lists live-spec assertion 8 as "`stats().pathRequests > 0` and `pathFailures == 0` — so a silent permanent fallback to straight-line flee is a failure, not an invisible degradation", and `docs/design/boar-ai.md:583` rests an open risk on it ("§10 assertion 8 makes the third visible instead of silent"). `tests/server/boar_body.spec.luau:205-237` no longer asserts `pathFailures == 0`. Section 3 of the addendum, headed "Where the code departs from the design, and why", still lists only `onExitSide`, the home pull, `escaped`-gated-on-FLEE and the moved `spawnPoint`. `REVIEW_RESULT.md:5` (round 3) established why this matters: "`REVIEW_REQUEST.md` is rewritten fresh per task (CLAUDE.md loop step 4), so claim 29 is not the record." Required: add the dropped `pathFailures == 0` assertion, with the measured reason and the replacement assertions, to addendum §3.

4. docs/research/2026-09-24-boar-ai.md:212-225 — addendum §4 ("What the design asked for and I could **not** measure") is now false in three statements, in the file the Builder itself designated the durable record. Evidence, against `[harness] PASS: 24/24 checks @ fbe1d60 (clean tree)` and REVIEW_REQUEST.md claims 1-6: `:218` "**Which mover configuration works is unmeasured.**" — the boar is now measured landing and resting at `groundY + BODY_SIZE.Y/2` (`tests/server/boar_body.spec.luau:143-148`); `:221-224` "**Whether `PathfindingService` returns a usable path** — on the arena or on the spec's elevated plate — is unmeasured. `Runtime:stats().pathFailures` and the body spec's assertion on it exist precisely so this shows up as a failure" — it is measured (`NoPath`), and that assertion has been removed; `:225` "**No spec in this task has ever been executed.** Lint, format and `rojo build` pass; nothing else." — every spec has now executed. Required: rewrite §4 to what is still unmeasured (`Path.Blocked`, a real player as a threat, `maxBoars`).

5. TASKS.md:25 — the file contradicts itself after this round's own paperwork commit `66fc0d9`, which edited rows 17 and 18 and left this row untouched. Evidence: `:25` still reads "**Tasks 17 and 18 still need a harness run and a screenshot** | **after Karen's Connect** | Neither has ever run in Studio: `rojo serve` crashed in Task 17 and only Karen can press Connect … When Rojo is back: `python tools/studio_mcp.py test` on each branch, then the rule-5 screenshots", while `:23` and `:24` both say "**RAN 2026-09-25**" with the harness line and three inspected screenshots, and `ESCALATE.md:161-173` closes the blocker. Required: close or delete that row in the same commit that records the run.

6. TASKS.md:13 — the Builder's own correction to a **BLOCKING** queue row exists only in a file that is rewritten per task. Evidence: REVIEW_REQUEST.md "## Screenshots (rule 5)" says "Using the raw RPC returns a JPEG. **Play-time capture works**, which is new information against `TASKS.md` row 7", but row 7 still reads "**BLOCKING before any visual client code (UI, HUD, cursor art):** play-time screenshots | todo | StudioMCP's `screen_capture` is edit-time only, so rule 5 cannot be met for play-time visuals by tools." CLAUDE.md loop step 4 makes `REVIEW_REQUEST.md` non-durable, and the Director owns the queue, so the correction reaches nobody. Required: record it in `ESCALATE.md` for the Director (the queue is not the Builder's to edit), naming the `studio_mcp.Studio._call` `text`-only join as the cause.

7. REVIEW_REQUEST.md:42 — the "What changed" table's count for `boar_brain.spec` is wrong and contradicts claim 1 in the same document. Evidence: the row says "`tests/server/boar_brain.spec.luau` | 20 assertions, no physics"; the file has 24 `it(` blocks. Claim 1's own total only balances at 24: sync 3 + test_arena 4 + boar_brain 24 + boar_body 8 = 39, the "39 server" in the harness line. Required: 24.

8. REVIEW_REQUEST.md:186 — "**`Runtime:destroy` and `maxBoars`** are never exercised by a spec" is false for `Runtime:destroy`. Evidence: `tests/server/boar_body.spec.luau:93-103` `afterAll(function() … if runtime then runtime:destroy() end … end)`, so it ran in the green harness run and is known not to throw. Required: say "never asserted on", and keep `maxBoars` as stated.

9. .agent-evidence/request-only-diff.txt — the mechanical gate is red, because CLAUDE.md contradicts itself about what may follow the code commit. Evidence: the file reads "Files changed between them: ['ESCALATE.md', 'REVIEW_REQUEST.md', 'TASKS.md'] / NOT OK: more than REVIEW_REQUEST.md changed after the tested commit." CLAUDE.md loop step 4 says of `REVIEW_REQUEST.md` "and commit **only** that file", while CLAUDE.md git-workflow step 4 permits "`REVIEW_REQUEST.md`, `REVIEW_RESULT.md`, `ARCH_RESULT.md`, `docs/architecture/audit-NNN.md`, `ESCALATE.md` and the task's status row in `TASKS.md`" between the code commit and the head — and findings 5 and 6 above require exactly such commits. I confirmed the extra content is paperwork (`66fc0d9` changes `ESCALATE.md` by +37/-2 closing two entries and `TASKS.md` by 2 lines, rows 17 and 18), so the harness line still covers all of `src/` and `tests/` at HEAD. Required: reconcile the two sentences in CLAUDE.md (the Builder owns that file) so the check the loop runs matches the rule the merge gate states.

---
REVIEWER verdict on commit `4432292ce4a08344245524e45215ca5e1017e816` (round 4) · 2026-09-25 06:25 UTC · session cost $3.13, 40 turns · written by tools/agents.py
