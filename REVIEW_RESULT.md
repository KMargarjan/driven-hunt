1. CLAUDE.md:85-86 and tools/agents.py:23-25 — the new round check is described as tamper-proof, and it is not. Both say the round is counted "from the committed `REVIEW_RESULT.md` trailer (which only the script writes) ... so the count cannot be reset by hand", and claim 19 in REVIEW_REQUEST.md:129-130 repeats "which only this script writes". Evidence that "only the script writes" is false, from this change's own files: REVIEW_REQUEST.md:179-181 "The `NONE` placeholders in `REVIEW_RESULT.md` and `ARCH_RESULT.md` **were written by the Builder**, although only the scripts may write those files. That is audit-002 must-fix #5, queued as **Task 12**"; ARCH_RESULT.md:1-4 still holds that Builder-written placeholder ("NONE ... This file is written only by `tools/architect.sh`"); ESCALATE.md:30 and TASKS.md:18 record the same. And the code has the matching escape hatch, with a comment naming it: tools/agents.py:187-189 `m = TRAILER_RE.search(text)` / `if not m: return None, None  # the placeholder, or a file this script never wrote`, feeding tools/agents.py:248 `expected = 1 if prev_rnd is None or ...`. So a Builder at round 3 restores the `NONE` placeholder it wrote once before, commits, and `Round: 1` is accepted; nothing in the script or in git looks back. What the fix actually buys is that the round cannot be *raised or skipped* from REVIEW_REQUEST.md — which is what the docstring at tools/agents.py:180-181 says correctly ("the Builder cannot raise the round by editing REVIEW_REQUEST.md"). This is the same defect round 1 finding 2 raised (a stop rule presented as enforced when it is policy), and CLAUDE.md:153-158 ("What is enforced and what is policy") still does not list it. Required: either state it honestly in CLAUDE.md:85-86, tools/agents.py:23-25 and claim 19 (the count cannot be reset from `REVIEW_REQUEST.md`; the verdict file itself is protected only by policy until Task 12), or make the check resistant — e.g. take the last trailer from `git log -p -- REVIEW_RESULT.md` as well as the working tree, and refuse when the committed file has lost a trailer that history shows.
2. tools/agents.py:173,187 — the round is parsed from the first trailer-shaped line anywhere in the file, including the agent-written body above the real trailer, so a quoted trailer silently sets the round. `TRAILER_RE = re.compile(r"^REVIEWER verdict on commit \`([0-9a-f]{40})\` \(round (\d+)\)", re.M)` is line-anchored with `re.M`, and `previous_review` does `m = TRAILER_RE.search(text)` then `body = text[:m.start()]`, while tools/agents.py:270 writes the file as `result + trailer(...)` — `result` is free text the Reviewer produced, and a Reviewer checking claim 19 has every reason to quote REVIEW_RESULT.md:9 verbatim at the start of a line as its evidence. The first match then wins over the genuine trailer at the end, `body` is truncated at the quote, and the next round's `expected` comes from the quoted number (round 1 again, if the quote is of an earlier verdict). The Builder can steer this, since REVIEW_REQUEST.md is Builder-written and is what the Reviewer quotes. Required: anchor the parse to the real trailer — take the last match (`list(TRAILER_RE.finditer(text))[-1]`), or parse only the text after the final `\n---\n` separator that `trailer()` emits.

---
REVIEWER verdict on commit `20334d5706c396ff2ac86cf62934f7a4bdbe9694` (round 2) · 2026-09-24 19:23 UTC · session cost $2.16, 36 turns · written by tools/agents.py
