1. REVIEW_REQUEST.md:162-163 — claim 19's "live proof" is false for this run, the same defect round 3 found and that this round was authorised to close. Evidence: it reads "`expected` is 4, and `MAX_ROUNDS` refuses it. That is why this change is escalated rather than reviewed a fourth time — see `ESCALATE.md`." This run *is* the fourth review: `cmd_review` compares the round against `cap = max_rounds()` (tools/agents.py:311-313), not against `MAX_ROUNDS`, and claim 26 (REVIEW_REQUEST.md:240-241) plus REVIEW_REQUEST.md:281 say the opposite — "`Round: 4` at the top of this file was accepted only because of it" and "This is the authorised final round." Required: rewrite the sentence to state what actually happened — `previous_review()` returns (3, FINDINGS), `expected` is 4, and the run was accepted because the Director's `DIRECTOR_MAX_ROUNDS=4` raised the cap; the escalation is what produced that authorisation, not the outcome of this run.
2. REVIEW_REQUEST.md:127 — claim 16 is stale and contradicted by the repo and by claim 30 in the same document. Evidence: claim 16 says "Row 16 is marked **blocked** on Karen's map decision", but TASKS.md:22 row 16 reads status "unblocked, not next" with "**Blocker resolved: Karen chose map option C**", and claim 30 (REVIEW_REQUEST.md:262) says "row 16 is 'unblocked, not next'". Commit 8d0d06b changed row 16 and claim 16 was not updated with it. Required: correct claim 16 to say row 16 is unblocked and not next.
3. tools/agents.py:320 — the round cap shown to the Reviewer is the constant, not the cap actually enforced, so this session was told "round 4 of max 3". Evidence: `task = (f"Review commit \`{head}\` (round {rnd} of max {MAX_ROUNDS}) against base \`{base}\`.\n"` while the check two lines above uses `cap = max_rounds()` (tools/agents.py:311-313), which returned 4 here. The `DIRECTOR_MAX_ROUNDS` notice at tools/agents.py:75-76 goes to stdout/`.agent-logs/` only, which the agent never sees. Commit 8d0d06b introduced `max_rounds()` and left this string on `MAX_ROUNDS`. Required: use the effective cap (`cap`, already in scope for the `go()` closure) in the task text, so the agent is told the cap in force.
4. ESCALATE.md:28 — cross-references made stale by the commit under review, while the identical references elsewhere were updated. Evidence: "claim 19 now matches `tools/agents.py:28-31` and `CLAUDE.md:87-88`." tools/agents.py:28-30 is now the `DIRECTOR_MAX_ROUNDS` paragraph ("The cap is `MAX_ROUNDS` (3), or `DIRECTOR_MAX_ROUNDS` ...") and "**What is still policy, not enforcement:**" starts at :31 and runs to :34; CLAUDE.md:87-88 is now "the same item fails 3 rounds. `tools/review.sh` refuses `Round: 4`, counting the round from the / committed `REVIEW_RESULT.md` trailer ...", and "It is **not** tamper-proof" is at CLAUDE.md:90-91. Claim 19 (REVIEW_REQUEST.md:151-152) was updated to ":31-34" and ":90-91" in the same commit; ESCALATE.md was not, although 8d0d06b edits ESCALATE.md. Required: update the two line references in ESCALATE.md:28 to tools/agents.py:31-34 and CLAUDE.md:90-91.
5. REVIEW_REQUEST.md:259 — claim 29's statement about `ARCH_RESULT.md` is contradicted by the precomputed evidence. Evidence: "`ARCH_RESULT.md` is untouched on this branch", but .agent-evidence/changed-files.txt:2 is "A	ARCH_RESULT.md" and .agent-evidence/log.txt:328-331 shows commit 2958ab5 creating it with the `NONE` placeholder — the same placeholder REVIEW_REQUEST.md:300-301 says the Builder wrote. Required: say what is true and checkable — no `tools/architect.sh audit` ran for Task 11 and `ARCH_RESULT.md` has not changed since Task 9's commit 2958ab5, where the Builder wrote its placeholder.

---
REVIEWER verdict on commit `d624930d1ab6af4e40d649ad1d1476ab984b6725` (round 4) · 2026-09-24 19:45 UTC · session cost $1.56, 29 turns · written by tools/agents.py
