1. REVIEW_REQUEST.md:99-101 — the only justification for skipping the harness cites an `ESCALATE.md` entry that does not exist. Evidence: the request says "`rojo serve` is down (`ESCALATE.md`, \"NEEDS KAREN · `rojo serve` crashed\")". `ESCALATE.md` contains three entries — "Task 11 round 4 (authorised) returned 5 findings" (`ESCALATE.md:14`), "Task 11 reached round 3" (`:71`), "Audit-002 must-fix items are outside Task 10's scope" (`:151`). A grep for "rojo serve" in `ESCALATE.md` returns nothing, and the only `NEEDS KAREN` strings in the file are the template at `:8` and the Task 11 dispatch at `:219`/`:227`. So there is neither a harness line nor the record that `CLAUDE.md:103-106` requires ("Write the entry headed **`NEEDS KAREN`**, list the **exact clicks** in order, and stop"). Required: either run the harness on `9cf9d7d` and paste the `[harness] ... @ 9cf9d7d... (clean tree)` line, or write the `NEEDS KAREN` entry in `ESCALATE.md` with the exact clicks and cite it; do not cite a record that is not in the repo.
2. REVIEW_REQUEST.md:71-74 — claim 9 is false. Evidence: claim 9 says "the design's §3 assigns exactly one owner each" for the camera writer and the viewmodel writer. `docs/design/shotgun.md:58-91` §3.1 assigns `WeaponServer` and §3.2 assigns `WeaponInput` / `WeaponReplica` / `ShotEffects` — no camera owner and no viewmodel owner anywhere. §3.3 says the opposite of the claim: "**This design ships the shotgun on the default Roblox camera, with no ADS and no viewmodel**" (`docs/design/shotgun.md:94-95`), `:39` records "camera owner, **unassigned** (`GAME_DESIGN.md:24`)", `:343` repeats it, and `:611` says the Builder "fills none of the still-`_unassigned_` rows at `GAME_DESIGN.md:24-27`". The request itself contradicts claim 9 at `REVIEW_REQUEST.md:121-123` ("including its §3.3 cut of ADS and the viewmodel"). Required: rewrite claim 9 to what the design does — the note adopts one camera writer and one viewmodel writer, the design defers both and makes that blocking decision 13.2 — or drop it.
3. docs/research/2026-09-24-shotgun.md:175 and docs/design/shotgun.md:490 — the slug cone is mislabelled by a factor of two against its own derivation, and the unit is then ambiguous in the design. Evidence: the note's derivation is "10-in group at 100 yd = 0.91 studs across at 327 studs", which gives 2·atan(0.5·0.91/327) = 0.16° **full** cone (half-angle 0.08°) — the identical construction the buckshot row uses one row down ("a full-cone angle of 2·atan(0.5·0.091/3.27) ≈ **1.6°**", `:177`). The value is printed as "**0.16° half-angle**" and the design copies it: "Slug cone | 0.16° half-angle → implemented as an exact ray" (`docs/design/shotgun.md:490`) while the buckshot row directly above gives "1.6° full angle". `docs/design/shotgun.md:543` then specs "every direction is within the configured **half-angle** of the aim" and `:327` takes `halfAngleDeg`, so `ShotgunConfig` would hold one field in two units and the buckshot cone would be built at 2× or 0.5×. Required: state both cones in the same unit, correct "0.16° half-angle" to the full angle (or halve the number), and say which unit `ShotgunConfig` stores.
4. docs/research/2026-09-24-shotgun.md:177 — the buckshot value cell contradicts its own derivation cell. Evidence: the row reads "| **Buckshot spread** | **1.0 in/yd → ~1.0° full cone** | ... → a full-cone angle of 2·atan(0.5·0.091/3.27) ≈ **1.6°** ...". Every other statement of this number is 1.6°: `docs/research/INDEX.md:7` ("~1.6 deg cone"), `docs/design/shotgun.md:489` and `:652`, and `REVIEW_REQUEST.md:57` ("the ~1.6° cone from 1 in/yd"). Required: correct the value cell to ~1.6°.
5. docs/research/2026-09-24-shotgun.md:152-153 — the source-8 paragraph states two different bands two clauses apart. Evidence: "Used only to establish the **range** 0.5–1.0 in/yd, which is enough, because the two sources agree ... Our band is 0.5–1.5 in/yd." `REVIEW_REQUEST.md:62-63` (claim 6) asserts only the second. The middle of the band is what picks 1.0 in/yd, so which band it is is load-bearing. Required: one band, stated once.
6. REVIEW_REQUEST.md:54-58 — claim 5 ("Every number is derived from a stated source at a stated scale, or explicitly labelled a feel number ... each row's third column is either an arithmetic derivation from 1 stud = 0.28 m or the words 'Feel number' / 'must be measured'") is false for at least two rows of the table it points at. Evidence: `docs/research/2026-09-24-shotgun.md:182` — "| **ADS transition** | **0.2 s**, spring damper 1, speed 16 | Spring values straight from source 4's write-up |": the derivation covers damper 1 and speed 16 (which do appear at `:101-102`), but 0.2 s appears in no source, has no arithmetic and is not labelled a feel number. `:178` — "| **Pellets** | **9** | 00 buckshot, 2¾-inch shell, the standard load |" cites no source among 1–9 and is neither arithmetic nor a feel number. Required: label 0.2 s a feel number (or derive it), attribute the 9-pellet figure, or narrow the claim.
7. docs/research/2026-09-24-shotgun.md:7-8, :84, :179 — the note rests three numbers on files that are not in this repo, and does not say so. Evidence: ":7-8" cites "as established for the boar (`docs/research/2026-09-24-boar-ai.md` addendum §1)"; ":84" cites "the same shape as SimplePath's (boar-ai note)" — the rejection `REVIEW_REQUEST.md:47-48` (claim 3) leans on; ":179" cites "Task 18 `CONFIG.BODY_SIZE`" as the sole authority for "5.5 × 3 × 2 studs", which `docs/design/shotgun.md:508-509` then uses for the lag argument ("The boar is 5.5 studs long (note `:179`)"). `.agent-evidence/ls-files.txt` has no `2026-09-24-boar-ai.md` and `TASKS.md` has no row 17 or 18. The Architect flagged this from its side (`docs/design/shotgun.md:639-646`, `ARCH_RESULT.md:3`); the note itself does not. Required: mark these three references in the note as not present on this branch, and either repeat the derivation for the boar dimensions or drop the numbers that depend on them.
8. docs/research/INDEX.md:7 — the index row states, as the shotgun decision, what the design in the same commit defers. Evidence: the Decision column reads "One camera writer and one viewmodel writer on the client (character clone under the Camera, EgoMoose MIT spring)", while `docs/design/shotgun.md:94-95` ships "no ADS and no viewmodel", `:468` marks `rbx-fractality-spring` "**deferred, not adopted in this task**", and `:635-637` makes the cut a blocking Director decision. Rule 3 gives the owner call to the design (`CLAUDE.md:22-23`), so after this commit the repo's one-line summary is wrong. Required: the INDEX row records the design's deviation (or says the camera/viewmodel decision is open, per §13.2).
9. ARCH_RESULT.md:1 — the blocking item points at the wrong section. Evidence: it says "either land Task 6 first (smallest unblock in §11.1)". §11.1 is "What is testable today, without Task 6 or Task 7" (`docs/design/shotgun.md:527`); the smallest unblock is §11.3 (`:572-584`), which is also where the design's own §13.1 points ("Either Task 6 lands first (§11.3)", `:631`) and what `REVIEW_REQUEST.md:76-81` (claim 10) verifies against. Required: correct the reference to §11.3.
10. docs/design/shotgun.md:39, :40, :51, :592 and docs/research/2026-09-24-shotgun.md:33 — the citations into `docs/PROJECT_CONTEXT.md` are wrong, in the two documents whose method is citation. Evidence: `:39` attributes "Two systems wrote the creature's position" to `docs/PROJECT_CONTEXT.md:36`; that text is at `docs/PROJECT_CONTEXT.md:33`, and line 36 reads "a home-grown viewmodel system. Each produced a run of bugs and each was fixed". `:40` attributes "Three scripts set the mouse cursor" to `:33-34`; it is at `:31`. `:51` attributes "one predicate answered two unrelated questions" to `:34-35`; it is at `:32-33`. `:592` attributes "a visibility audit ignored parent visibility and certified a blank screen twice" to `:30-31`; it spans `:29-30`. Separately, the note's `:33` says "three of the five causes of the previous project's death are in exactly this system" — `docs/PROJECT_CONTEXT.md:25-40` lists **six** causes, and the three quotes the note then gives come from two of them (the "Overlapping owners" bullet at `:31-34` and the "Invented foundations" bullet at `:35-37`); `REVIEW_REQUEST.md:72-73` repeats the "three of the five" count. Required: fix the four line references and the cause count.

---
REVIEWER verdict on commit `2377d023c9c8d7d9c417c231c94220d149857624` (round 1) · 2026-09-24 21:13 UTC · session cost $2.12, 30 turns · written by tools/agents.py
