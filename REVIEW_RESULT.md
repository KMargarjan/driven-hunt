1. src/server/Boar/Brain.luau:357 — round 1's finding 2 is not fixed in substance: the anti-stuck 90° turn still cannot change the boar's course, so claim 20 ("The turn now goes on the direction the Brain actually returns") is true only of one intermediate variable, not of the output. Evidence: `Brain.luau:357-359` rotates `desiredDirection` by `math.pi / 2`, but `Brain.luau:367` then does `self._desired = slew(self._desired, unit(desiredDirection, self._desired), config.TURN_RATE * dt)` — every output (`intent.facing`, `intent.targetVelocity`, `Brain.luau:372-373`) derives from `self._desired`, which can move at most `TURN_RATE * dt` = 4/60 ≈ 0.067 rad (3.8°) in that tick. `stuck` is true for exactly one tick per `STUCK_TIME`, because `_updateStuck` resets `self._stuckTime = 0` when it returns true (`Brain.luau:243-244,250`), and on the next tick `desiredDirection` is recomputed by `_fleeDirection` (`Brain.luau:353`), which does not read the turned value (`self._heading` is only `unit()`'s degenerate fallback, `Brain.luau:220,227-229` — the same reason the original was dead). So a boar pinned against cover gets a ≤3.8° nudge that reverts on the following frame and keeps pushing into the obstruction; `docs/design/boar-ai.md:225-227` asks for "a 90° lateral offset". Round 1's required fix offered `self._desired` as the alternative, which would persist and slew back over ~pi/2 / TURN_RATE ≈ 0.4 s. Required: apply the offset where it survives (rotate `self._desired`, or hold the turned direction as state for a bounded time), and assert the *magnitude* of the resulting course change, not merely that it is non-zero.

2. tests/server/boar_brain.spec.luau:205 — the new "turns when it is stuck against something" test is vacuous: it passes with the entire stuck branch deleted, so claim 20's "asserts the output direction changes — the assertion that was missing" is false, as is the same sentence in `docs/research/2026-09-24-boar-ai.md:220-223`. Evidence: the test sets `turned = true` whenever two consecutive `intent.facing` values differ by more than `1e-6` (`:216-226`, `:221` `if delta > 1e-6 then`). `intent.facing` is `self._desired` (`Brain.luau:373`), which is re-slewed toward the flee direction every single tick (`Brain.luau:367`) starting from the random constructor heading (`Brain.luau:63,65`), so it changes by `TURN_RATE * dt` ≈ 0.067 rad on tick 2 — long before `_stuckCount` reaches 2, which needs `2 * STUCK_TIME` = 4 s (`init.luau:63-64`, `Brain.luau:238-250`). The stuck turn is never isolated. Required: assert something only the stuck branch can produce — e.g. capture the facing before the stuck tick and require a cumulative change of order `math.pi / 2`, or drive the identical pinned scenario with `STUCK_TIME` unreachable and require that `turned` is then false.

3. GAME_DESIGN.md:25 — the owner row contradicts the code and the specs, and claim 1's closing sentence ("`GAME_DESIGN.md` has the matching owner row") is therefore false. Evidence: the row reads "`Boar.Brain` (decisions) and `Boar.Body` (Instances) are private to it and called by nothing else", but `src/server/Boar/init.luau:28` exports `Boar.Brain = Brain` and `tests/server/boar_brain.spec.luau:26` calls `Brain.new(CONFIG, FIELD, START, Random.new(seed or 1))` through it — which is exactly the wording round 1's finding 5 made the Builder correct in claim 1 ("the only *production* caller"), left uncorrected in the owner table. Required: reword the row as the code shows (Brain is a deliberate export for `boar_brain.spec`; `Body` is private), or restate claim 1.

4. tests/server/boar_body.spec.luau:153 — two undeclared deviations from the design's live-spec list, while REVIEW_REQUEST.md still presents its deviations as exactly two plus the round-1 fixes ("Correction to the design, 1 of 2 … 2 of 2", claims 12-13) — the same undeclared-deviation pattern as round 1's finding 1. Evidence: (a) `docs/design/boar-ai.md:488` requires "its speed ≤ `WANDER_SPEED * 1.5`"; the spec asserts `expect(fastest <= CONFIG.WANDER_SPEED * 2)` — a 33% looser bound, and round 1's finding 3 already quoted the design's figure. (b) `docs/design/boar-ai.md:489-490` requires "within 1.0 s `AssemblyLinearVelocity` magnitude ≥ 0.5 × `SPRINT_SPEED`, **and the flat distance to the threat has grown by ≥ 20 studs after 2 s**"; the sprint test (`:156-169`) asserts the velocity, the state and uprightness but never the distance, and `local start = handle.position()` (`:157`) is used only to place the threat. Nothing in this task asserts on a physically simulated body that it actually gets away — the only "moves away" assertion is the pure-simulation one at `boar_brain.spec.luau:158-172`. Required: restore the design's `1.5` (or record the deviation in the addendum and the request) and add the ≥20-stud growth assertion.

5. tests/server/boar_body.spec.luau:137 — the landing guard claim 21 rests on can pass without the boar ever having landed, so the spec does not do what the claim says ("The spec now waits for the body to land (`|velocity.Y| < 1`)"). Evidence: `waitUntil` (`:25-34`) evaluates `predicate()` *before* its first `task.wait()` (`:27-31`), and the predicate is `math.abs(handle.part.AssemblyLinearVelocity.Y) < 1` (`:138`). A part created that same tick has zero velocity, and nothing between `runtime:spawn()` (`:108`) and this call yields — the two intervening `it` blocks (`:122-129`) contain only property reads — so `landed` is true immediately and `expect(landed).to.equal(true)` (`:140`) asserts nothing. The substantive half of the fix (measuring flat XZ speed, `:150`) is correct; the guard is not. Required: require at least one `task.wait()` before the first evaluation, or wait on a positive condition (the part's Y position within a tolerance of `groundY + BODY_SIZE.Y / 2`).

6. src/server/Boar/init.luau:80 — the production boar can despawn itself as `escaped` while idling, with no threat anywhere, and nothing asserts against it. Evidence: `_outcome` is evaluated in every state, not only FLEE (`Brain.luau:273` is reached before any state check, and `Brain.luau:159-160` returns "escaped" whenever `position.Z <= field.exitZ`). `spawnPoint = Vector3.new(-40, 0, -60)` (`init.luau:80`) is 130 studs from `exitZ = -190` (`init.luau:77`), while `HOME_RADIUS = 120` (`init.luau:49`) and the wander spec explicitly tolerates excursions to `HOME_RADIUS + 20` = 140 studs from home (`tests/server/boar_brain.spec.luau:281`). So an idle excursion the project's own spec accepts crosses the exit line, and the grey-box loop loses its one boar before any driver arrives — untestable by either spec, since `boar_brain.spec` starts at `START` in a 400-stud field and `boar_body.spec` uses its own plate (`exitZ = -40`, home 70 studs away). Required: make the two numbers consistent (move `spawnPoint`, shrink `HOME_RADIUS`, or gate "escaped" on `FLEE`), and add a brain assertion that an idle boar spawned at `CONFIG.spawnPoint` in `CONFIG.field` never despawns over 120 simulated seconds.

---
REVIEWER verdict on commit `732c74a8d797b0c4c3c28a620d49810c6950c0b1` (round 2) · 2026-09-24 20:38 UTC · session cost $2.49, 34 turns · written by tools/agents.py
