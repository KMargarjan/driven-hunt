1. CLAUDE.md:127-132 — the new merge gate is unsatisfiable as written, so the central change of Task 11 cannot ever be applied. Step 4 requires all three of "`REVIEW_RESULT.md` line 1 is `PASS` and its trailer names the **PR head commit**", "CI ... is green on that head commit", and "a clean-tree harness PASS names that same commit". But the same file's loop, CLAUDE.md:70-79, moves the head past each of those commits afterwards: step 3 "Commit (this is the **code commit**), then run the harness", step 4 "commit **only** that file" (REVIEW_REQUEST.md), step 5 "On `PASS`, commit `REVIEW_RESULT.md` and continue", step 6 the audit writes `ARCH_RESULT.md` plus `docs/architecture/audit-NNN.md`. The trailer is fixed to the commit reviewed, not to any later head: `tools/agents.py:201` `head, dirty = repo_state()` and `tools/agents.py:234` `write(os.path.join(REPO, "REVIEW_RESULT.md"), result + trailer("REVIEWER", head, ...))`. This PR demonstrates the failure: REVIEW_REQUEST.md:115 names the harness PASS at `6378a078...`, the commit under review is `0362ee09...` (.agent-evidence/head.txt:1), and committing the resulting REVIEW_RESULT.md moves the head again. Required: state the conditions against a commit that can actually exist at merge time (e.g. the harness/CI/Reviewer conditions hold for the last *code* commit and the trailing artefact commits are named as exempt), or add a loop step that re-runs the harness and re-reviews the final head; as it stands rule 10, step 4 and the Definition of done all demand something the loop makes impossible.
2. tools/agents.py:209,218-220 — the round-3 stop rule is not enforced, although CLAUDE.md:83 presents it as enforced ("the same item fails 3 rounds (`tools/review.sh` refuses `Round: 4`)") and CLAUDE.md:140-145 lists only the merge rules as unenforced policy. The round number is read straight from the Builder's own file — `rnd = re.search(r"^Round:\s*(\d+)", req, re.M)` — and compared to `MAX_ROUNDS` with no cross-check against the previous verdict, even though `trailer` already records `(round {rnd})` in the committed `REVIEW_RESULT.md` (tools/agents.py:234). A Builder that writes `Round: 1` every time never trips the rule, and the script cannot tell. Claim 5 ("refused past round 3") is therefore true only of an honest input. Required: derive or validate the round from the committed `REVIEW_RESULT.md` trailer (refuse when the request's round is not the previous round + 1), or say plainly in CLAUDE.md that the round count is self-reported policy, not enforcement.
3. TASKS.md:17 and commit 6378a07 — more than one task in the round (rule 4, CLAUDE.md:24 "**ONE TASK PER ROUND.** Small and testable, then stop"). Task 11 is defined by the Director as "Review Task 9 itself through the loop, and fix the findings" (ESCALATE.md:55). Commit 6378a07 also rewrites the merge policy (CLAUDE.md:30-32, 40, 50-51, 127-134, 140-145) and adds a fourth stop rule (CLAUDE.md:86-89) — two policy changes that are not "review Task 9" and not findings of any review, since this is round 1. Nothing in the repo records a Director instruction that put them in Task 11: the only record is TASKS.md:17 "Also: the Director (agent) now retargets and merges PRs...", a row the Builder wrote, while CLAUDE.md:57 gives the Builder only "its current task's status" in TASKS.md. PROJECT_CONTEXT.md:39 is the reason this matters: "Big multi-item rounds hid failures. Small single-task rounds worked." Required: split the merge-policy and `NEEDS KAREN` edits into their own task through the loop, or record the Director's instruction that added them to Task 11.
4. tools/agents.py:249 — the message is false for the prompt itself. On a dirty tree `cmd_architect` prints "[agents] note: working tree is dirty; the Architect sees committed HEAD only", but `run_agent` loads the prompt from the working tree, not from HEAD: tools/agents.py:128 `with open(os.path.join(REPO, prompt_file), encoding="utf-8") as f:` with `REPO` set at tools/agents.py:37. An uncommitted edit to `docs/ARCHITECT_PROMPT.md` therefore drives the session while the script says it does not. (`cmd_review` is safe only because tools/agents.py:201-203 refuses a dirty tree.) Required: read the prompt out of the worktree (`wt`), or refuse a dirty tree for `architect` too, or correct the message.
5. CLAUDE.md:44 — claim 13 is not backed by the evidence it cites. It states "Rule 10, the DIRECTOR row, the KAREN row, git-workflow step 4 and the 'what is enforced and what is policy' paragraph all say the same thing", but the KAREN row says nothing about merging: "| **KAREN** | the game | plays it, decides anything about feel or design, `PLAYTEST.md` feedback, and does the clicks no tool can do (Rojo **Connect**, the Studio MCP toggle, Studio itself) | |" — its "Never" column is empty, and `grep -n "merge" CLAUDE.md` returns no hit on line 44. The Roles table is where a reader looks first, and it is the one place the new policy is absent. Required: put "merges PRs" in the KAREN row's "Never" column, or drop the KAREN row from the claim.
6. CLAUDE.md:190-191 — the change makes the Layout table stale. The row "| `docs/` | none | `research/` (notes plus INDEX), `architecture/` (Architect audits) |" omits `docs/design/` (added by this change, docs/design/README.md), `docs/PROJECT_CONTEXT.md`, `docs/REVIEWER_PROMPT.md` and `docs/ARCHITECT_PROMPT.md`; the row "| `tools/` | none | Test harness |" still describes `tools/` as the test harness after this change put the four-agent gate there (`tools/agents.py`, `tools/review.sh`, `tools/review.ps1`, `tools/architect.sh`, `tools/architect.ps1` — .agent-evidence/changed-files.txt:12-16). Required: update both Notes cells to name the directories and files this change adds.

---
REVIEWER verdict on commit `0362ee0937c2158dbfd531051394e7ce6894360f` (round 1) · 2026-09-24 19:12 UTC · session cost $1.63, 31 turns · written by tools/agents.py
