1. docs/research/2026-09-24-map-generator.md:224 — the adopted pattern's determinism story does not work, and nothing in the note says how the seed reaches the terrain. Evidence: :224-226 "**`math.noise`'s exact range and determinism guarantees are not documented on the maths-library page** (I checked), so the generator must not assume: it seeds its own `Random` and the first task records what `math.noise` actually returns." `math.noise` takes no seed — the note's own quoted signature is `math.noise(x, y, z): number` (REVIEW_REQUEST.md:126) — so seeding a `Random` object changes nothing about `math.noise`'s output, its range or its determinism. Pattern point 2 at :227 then asserts "One `seed` number in a config table reproduces the map exactly", and pattern point 1 makes `math.noise` the source of the heightfield, but no text connects the seed to the heightfield at all. Required: state the actual mechanism (a seed-derived coordinate offset into `math.noise`, `Random` for placement only), or drop the claim that seeding `Random` answers `math.noise`'s undocumented determinism.
2. docs/research/2026-09-24-map-generator.md:225 — the maths-library page is used as a source but is never named or linked, so rule 1 is not met for the source the whole heightfield rests on. Evidence: :225 cites "the maths-library page (I checked)" with no URL; the "Sources" list :28-191 has ten entries and none of them is the Luau/Roblox maths library. REVIEW_REQUEST.md:125-128 escalates this to a "Could not verify" item — "The maths-library reference gives the signature `math.noise(x, y, z): number` and **nothing else**" — a claim about a page the note does not identify. CLAUDE.md:13 requires sources "named and linked, with licence and maintenance status". Required: add it as a numbered source with link, licence and maintenance status, or remove the claims that rest on it.
3. docs/research/2026-09-24-map-generator.md:267 — three sets of broken `§` cross-references in a note whose only value is being usable by whoever builds the generator. Evidence: (a) :267 "what does `math.noise` actually return, and is it stable across runs? (§10, pattern 1)" — §10 is ":178 ### 10. Place files and \"Save to File\" — the backup step", which says nothing about `math.noise`; (b) :115 "That is the budget §9 is built from" and :117 "to be replaced by measurement (§9)" — §9 is ":156 ### 9. Meshy → Roblox, and the Open Cloud Assets API"; the part budget is the "Numeric targets" table at :193; (c) :26 "the backup step (§6)" and :53 "it is also the reason §6 exists" — §6 is ":109 ### 6. Community part-count and mobile guidance", while the same backup step is cited correctly as "(§10)" at :241 and :286. Required: fix the references to point at what they mean.
4. REVIEW_REQUEST.md:98 — claim 12's "one-to-one" mapping is false. Evidence: :98-99 "The bullets under it map one-to-one onto the things sources 2, 4 and 10 could not settle." There are five bullets (map-generator.md:266-275): bullet 1 → §2 and bullet 3 → §4 hold; bullet 2 cites §10 for `math.noise`, which §10 does not cover (finding 3); bullet 4 (Edit-mode `screen_capture` as rule-5 evidence) traces to no source in the note — its subject is TASKS.md:13, not sources 2, 4 or 10; bullet 5 (what 50 trees cost) traces to source 6, not 10. Required: restate the claim to match the bullets, or cite the sources that actually raise each question.
5. REVIEW_REQUEST.md:14 — the "What changed" list is incomplete and its summary sentence is false. Evidence: :14 "The change is one new document plus two table rows. There is no `src/`, no `tests/`, no `tools/`." and the table at :26-30 names only `docs/research/2026-09-24-map-generator.md`, `docs/research/INDEX.md` and `TASKS.md`. `.agent-evidence/changed-files.txt:1` is "M	ESCALATE.md", a 47-line addition (`.agent-evidence/log.txt:26` "ESCALATE.md | 47 +++..."), and commit `fba85c8` — the commit named as `Code commit:` on :7 — changes nothing else. The entry is mentioned only in prose at :112-116 and no numbered claim covers its content. Required: list `ESCALATE.md` in the table and claim what it asserts, so it is reviewed rather than smuggled past the change list the Director's merge gate (CLAUDE.md:153-155) compares against.
6. TASKS.md:23 — the Director's dispatch is recorded nowhere, and the new task row is written in the Builder's voice, so the claims that rest on what the Director asked cannot be checked. Evidence: row 20 is added by this change (`.agent-evidence/diff.patch:262`) and reads "**ROADMAP Milestone 2, docs only:** map generator research note … **No code, no Architect design** (the Director dispatched research only)" — a paraphrase, not "transcribed verbatim and marked as the Director's" as CLAUDE.md:41 and CLAUDE.md:57 require of a Builder-written dispatch. REVIEW_REQUEST.md:11-12 ("the Director dispatched research only this time"), :37 ("as the Director asked") and :95 ("as the Director asked") have no verifiable basis in the repo. The precedent is TASKS.md:17, where Task 11's row points at `ESCALATE.md` "Task 11's dispatch, verbatim". Required: transcribe the Task 20 dispatch verbatim, marked as the Director's, and cite it from the row.

---
REVIEWER verdict on commit `67b090657d11aa96dfc2ac2436ffda1d75f4a5b5` (round 1) · 2026-09-24 21:37 UTC · session cost $1.66, 23 turns · written by tools/agents.py
