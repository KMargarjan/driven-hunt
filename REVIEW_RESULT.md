1. docs/research/2026-09-24-shotgun.md:275-278 — the addendum's four "correct" `PROJECT_CONTEXT.md` line numbers are all wrong, and three of the four design citations it condemns are in fact right. Evidence: the addendum says the four quotes "are at `:34`, `:32`, `:32-34` and `:29-31`". The file reads `docs/PROJECT_CONTEXT.md:36` "weapon. Two systems wrote the creature's position. Most of the worst bugs" (the design cites `:36` at `docs/design/shotgun.md:39` — correct); `:34` "- Overlapping owners. Three scripts set the mouse cursor. One predicate" (the design cites `:33-34` at `:40` — the range contains the quote); `:34-35` "One predicate / answered two unrelated questions" (the design cites `:34-35` at `:51` — correct); `:32-33` "A visibility audit ignored parent / visibility and certified a blank screen twice" (the design cites `:30-31` at `:592` — the only genuinely wrong one; `:30` is "green grass.", `:31` is "- Its own test harness was wrong…"). `REVIEW_REQUEST.md:128-131` (claim 22) repeats the false count: "four wrong `PROJECT_CONTEXT.md` citations … The four wrong line numbers are in the design". Round 1's finding 10 was itself wrong on all four replacements and was copied instead of checked, which `CLAUDE.md:101` makes an `ESCALATE.md` case ("you believe a Reviewer or Architect finding is factually wrong (write the evidence)"). Required: re-read `docs/PROJECT_CONTEXT.md` and state in the addendum which design citations are actually wrong (only `:592`) with the real line numbers, correct claim 22, and record the disagreement with round 1's finding 10 in `ESCALATE.md`.
2. docs/research/2026-09-24-shotgun.md:271-272 — the addendum misdescribes the config-unit hazard it exists to record, and `REVIEW_REQUEST.md:117-118` (claim 17) repeats it. Evidence: both say "§6 takes a `halfAngleDeg` field — so one config field would hold two units and the buckshot cone would come out at 2× or 0.5×". §6's only `halfAngleDeg` is `SafetyArc.isForbidden(origin: Vector3, direction: Vector3, line: DriveLine, halfAngleDeg: number)` (`docs/design/shotgun.md:327`), the forbidden safety arc — not the shot cone, which `ShotPattern.directions(ammo, aim, rng, config)` (`:325`) takes inside `config`. The real hazard is `docs/design/shotgun.md:543`, the pattern spec: "every direction is within the configured **half-angle** of the aim", against `:489-490` which gives buckshot as "1.6° full angle" and the slug as "0.16° half-angle". Required: cite §11.1 (`:543`) as the half-angle consumer in the addendum and in claim 17, and drop the false §6 attribution.
3. docs/research/2026-09-24-shotgun.md:82-83 — the note still rests numbers on off-branch work it cannot see, which is exactly round 1's finding 7, and neither the addendum nor claim 20 lists them. Evidence: "At 38 studs/s the boar crosses the arena in ~10 s; a slug crosses the whole 400-stud arena in about 0.4 s of real flight" — neither 38 studs/s nor the 400-stud arena appears in any of sources 1–9, in the Numeric targets table, or anywhere else in the repo (`.agent-evidence/ls-files.txt` has no boar note; `TASKS.md` has no row 17 or 18, and the arena is Task 17 per `ESCALATE.md:50`). `docs/design/shotgun.md:507-509` then builds the design's only lag figure on it: "a boar running at the note's 38 studs/s is ~3.8 studs from where the shooter saw it". The addendum's "What is not on this branch" (`:259-264`) names only the boar note, the boar design and `TASKS.md` rows 17–18; `REVIEW_REQUEST.md:122-126` (claim 20) says the fix covered the stud scale, the boar dimensions and the SimplePath comparison, and stops there. Required: attribute both numbers to a source, re-derive them here, or mark them as off-branch context like the SimplePath comparison — and correct claim 20's list.
4. docs/design/shotgun.md:490 — the change ships two documents that contradict each other on a load-bearing number, and the only record is the addendum whose facts finding 2 shows are wrong. Evidence: `:490` "| Slug cone | 0.16° half-angle → implemented as an exact ray | note |" against `docs/research/2026-09-24-shotgun.md:180` "**0.16° full cone** (0.08° half-angle) … 2·atan(0.5·0.91/327) = **0.16°** full cone". `REVIEW_REQUEST.md:116-118` (claim 17) says "Both rows now state the **full** angle with the half-angle in brackets" — true of the note only; round 1's finding 3 required "state both cones in the same unit … and say which unit `ShotgunConfig` stores", and the design half is unchanged (`.agent-evidence/log.txt:49-52`: `0f5627f` touched only `ESCALATE.md`, the note and `INDEX.md`). The Builder's reason for not regenerating is written (`REVIEW_REQUEST.md:138-141`) and rule 3 does forbid it editing `docs/design/`, but there is no `ESCALATE.md` entry and nothing in `TASKS.md` row 19 (`TASKS.md:23` lists only the three `ARCH_RESULT.md` items), so the next Builder reads §9 and §11.1 and gets a cone at 2× or 0.5×. Required: either re-run `tools/architect.ps1 design shotgun`, or write the `ESCALATE.md` entry that puts the choice to the Director (`CLAUDE.md:101-102`) and record the three design defects in `TASKS.md` row 19, so the record does not live only inside another document's addendum.

---
REVIEWER verdict on commit `009c20e15b4308aed5312bfb30a8bba9489e2b71` (round 2) · 2026-09-24 21:23 UTC · session cost $1.93, 22 turns · written by tools/agents.py
