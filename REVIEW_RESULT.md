1. src/server/Boar/init.luau:7 — the module header claims a behaviour the code does not implement (rule 9: comments carry the pattern actually used). Evidence: `init.luau:7-8` says "CreatePath -> ComputeAsync -> GetWaypoints; Path.Blocked carries blockedWaypointIdx, so we recompute only when the obstacle is ahead of us (the one idea copied from SimplePath)", but `defaultWorld().requestPath` (`init.luau:139-157`) creates the Path, calls `ComputeAsync`, reads `GetWaypoints()` and drops the object; `grep -n "Blocked" src` returns only that comment line. The design requires it (`docs/design/boar-ai.md:269-271`: "`Path.Blocked` is connected and, if `blockedWaypointIdx` is ahead of the current index, forces a repath — the one part of SimplePath's loop worth copying") and so does the research note (`docs/research/2026-09-24-boar-ai.md:107-108`, "recomputed on a timer and on `Path.Blocked` ahead of us"). The same design line also says "one `Path` object per agent created once", while `init.luau:141` creates a new Path on every request. REVIEW_REQUEST.md claims 12 and 13 are presented as the only two deviations from the design ("Correction to the design, 1 of 2 … 2 of 2"); this third one is undeclared. Required: either connect `Path.Blocked` and repath as the design says, or remove the false sentence from `init.luau:7-8`, record the deviation in the research-note addendum, and say so in the review request.

2. src/server/Boar/Brain.luau:331 — the anti-stuck manoeuvre is dead code. Evidence: `Brain.luau:331-333` does `if self._stuckCount >= 2 then self._heading = unit(rotateY(self._heading, math.pi / 2), self._heading) end`, but eleven lines later `Brain.luau:342` overwrites it with `self._heading = desiredDirection`, and `_fleeDirection` (`Brain.luau:201-220`) reads `self._heading` only as the degenerate fallback of `unit(...)` — when a waypoint exists it returns `unit(flat(ahead) - flatPos, self._heading)`, and when it does not it returns `unit(away + toTarget, away)` with `away = unit(flatPos - self._threatPoint, self._heading)`. In the case the code exists for — a boar pinned against cover with a threat behind it — `self._heading` is never the value used, so the 90-degree turn changes nothing. `docs/design/boar-ai.md:225-227` specifies it ("on the second consecutive time add a 90° lateral offset to the heading for one tick"), `CONFIG.STUCK_DIST`/`STUCK_TIME` (`init.luau:63-64`) exist for it, and neither spec covers stuck at all. Required: apply the offset to the value `_fleeDirection` actually returns (or to `self._desired`), and add a `boar_brain.spec` assertion that a boar whose position does not change for `STUCK_TIME` turns.

3. tests/server/boar_body.spec.luau:140 — this assertion will fail on the first real run, for a reason that has nothing to do with wandering. Evidence: `:137` samples `fastest = math.max(fastest, handle.part.AssemblyLinearVelocity.Magnitude)` — the full 3D assembly velocity — and `:140` asserts `fastest <= CONFIG.WANDER_SPEED * 2`, i.e. 8 studs/s. The boar is spawned clear of the ground (`src/server/Boar/init.luau:210`: `at = Vector3.new(at.X, self._field.groundY + config.BODY_SIZE.Y / 2 + config.SPAWN_CLEARANCE, at.Z)`, `SPAWN_CLEARANCE = 0.5` at `init.luau:72`) and `Body.create` sets `Anchored = false` (`Body.luau:43`) while the mover only drives X/Z (`Body.luau:94`, "Y is left to gravity"). A 0.5-stud free fall at Roblox's default gravity (196.2 studs/s²) peaks at sqrt(2*196.2*0.5) ≈ 14 studs/s, reached over ~5 physics frames. No frame elapses between the spawn at `:105` and the sampling loop at `:131-139`: the two intervening `it` blocks (`:119`, `:123`) do not yield, and the loop calls `task.wait()` once per Heartbeat, so it samples the drop (≈3.3, 6.5, 9.8, 13.1 studs/s). The design's own figure was tighter still (`docs/design/boar-ai.md:488`: "speed ≤ `WANDER_SPEED * 1.5`"). Required: assert the flat XZ speed (which is what "idles slowly" means), or wait for the body to settle before sampling; a spec that fails for a reason it is not testing is the harness fault rule 6 warns about.

4. REVIEW_REQUEST.md:16 — the authority for reviewing and accepting this task with no harness run and no screenshot exists nowhere in the repo. Evidence: the request states twice that "The Director dispatched this task in 'no-Studio mode' and asked explicitly that the review go ahead anyway" (`:15-17`) and "The Director's dispatch for this task says to run the review anyway and treat a `PASS` as 'PASS pending harness'" (`:149-150`). `TASKS.md:24` (row 18) records the task but contains no dispatch text and no mention of no-Studio mode; `ESCALATE.md:13-16` is a Task 17 entry ("branch `task-17-test-area`, code commit `48169db`") and its "State of the work" (`:64-68`) names only Task 17, so it is not evidence for this commit; there is no Task 18 `NEEDS KAREN` entry. CLAUDE.md:41 requires a Director dispatch that arrived outside the repo to be "transcribed verbatim and marked as the Director's" in `TASKS.md` — the precedent is `ESCALATE.md:297-319`, added after Task 11 review round 1 found exactly this gap. Rule 5 (`CLAUDE.md:25`) is unmet for a moving grey box, and the request concedes it (`:158-161`). Required: transcribe the dispatch verbatim into `TASKS.md` row 18 (or an `ESCALATE.md` entry) marked as the Director's, so the harness and screenshot waiver has a record; `.agent-evidence/request-only-diff.txt` is otherwise consistent (only `REVIEW_REQUEST.md` differs between `8a6edd0` and `ad35852`), and lint/format/build evidence backs claim 17.

5. REVIEW_REQUEST.md:59 — claim 1 is false as written. Evidence: it says "`Boar.newRuntime` … is the only thing that constructs a `Brain`" and "outside `init.luau` the only hits are `Boar.Brain` (the deliberate export, used by `boar_brain.spec`) and comments", but the claim's own grep returns `tests/server/boar_brain.spec.luau:26`: `return Brain.new(CONFIG, FIELD, START, Random.new(seed or 1))` — a Brain constructed outside `init.luau`. (Separately, `newRuntime` constructs no Brain at all; `Brain.new` is called in `Runtime:spawn`, `init.luau:215`.) Claim 5's "`Players` appears only in `init.luau`'s `defaultWorld`" is wrong the same way: `init.luau:19` is a module-level `game:GetService("Players")`. Required: restate claim 1 as what the repo shows — the module is the only *production* constructor, and the spec constructs Brains through the deliberate `Boar.Brain` export — and fix claim 5's location.

6. REVIEW_REQUEST.md:128 — claim 15 is false. Evidence: it says "no numeric literal outside it in `Brain.luau` or `Body.luau` except `0`, `1`, `2`, `1e-6` and the `math.pi / 2` stuck-turn", but `src/server/Boar/Brain.luau:189` reads `local sign = if rng:NextNumber() < 0.5 then -1 else 1` — `0.5` is not in that list. Relatedly, `init.luau:30` comments the table as "Every number the system has", yet `init.luau:93-98` holds `AGENT_PARAMETERS = { AgentRadius = 2, AgentHeight = 3, … }` outside it, and those are tunables (they decide whether the pathfinder will route a 2x3x5.5 body past the cover blocks), not structure. Required: either move the agent parameters into `CONFIG` and add `0.5` to the claim's exception list, or drop the absolute wording from both the claim and the `init.luau:30` comment.

---
REVIEWER verdict on commit `ad35852c50c51955f1d480eb1fc117f0cb83ee72` (round 1) · 2026-09-24 20:26 UTC · session cost $3.01, 41 turns · written by tools/agents.py
