PASS

## Notes (non-blocking)
- `src/serverstorage/MapGen/Config.luau` `CORRIDOR_MAX_SLOPE_DEG` — new this task, no reader anywhere (grep over `*.luau`/`*.py` finds only the definition), and its comment says "the spec measures it", which no spec does. That is the same shape as 43a(n), which this task claims to close. Drop it, or add the slope measurement the comment promises.
- `tests/server/map_contract.spec.luau` `it("stays inside the relief band, and flatter inside the drive corridor")` — it samples `for x = -250, 250, 25` and the same for z, which at 2048 studs is entirely inside the corridor (x ±340, z ±800). `worst` and `worstCorridor` are therefore the same number, so `expect(worst <= c.RELIEF)` and `expect(worst < 50)` cannot fail unless the corridor bound already has, and ~94% of the new map's area is sampled by nothing. The task grew the map; widen the loop to ±1000.
- `reviews/task-44/REQUEST.md` claim 10 and `tools/mapgen.py` `SHOTS` — `map-gate` is called "the fix this task exists for, and a picture is the only way to see it is really there", but neither the request nor TASKS.md says what it showed; the camera is fixed at `(0, 24, 180)` → `(0, 4, -40)` while the gates are seeded into x ∈ [−292, −48] and [48, 292], so whether one is in frame is seed-dependent. One sentence on what `map-gate.png` showed would close it. (The other shots are described in TASKS.md rows 44a(b)–(d), which is why this is a note.)
- `src/serverstorage/MapGen/Config.luau` `WORLD` comment — "10,240 studs of hedge in 24-stud segments = 427 parts". It is 430 before gating and 400 after, which is what measurement H and the request both say.
- `tools/mapgen.py` docstring — names `build`'s line as "[mapgen] OK: 269/269 steps" (the plan is 268) and the usage line calls `shots` "the six named captures" while `SHOTS` holds seven.
- `reviews/task-44/REQUEST.md` — "268 step lines and two run-log lines sit between the digests": `command_verify` prints one `log_run` line per build, so between the two `build N: digest=` lines there are 268 step lines and one run-log line.
- `src/serverstorage/MapGen/init.luau` `MapGen.verifyContract` — the declared return type is `{ ok, findings, counts }` but the returned literal also carries `streaming` and `markerDigest` (a `--!strict` type error for luau-lsp, which is not in CI, so nothing fails today). Its out-of-bounds finding also reads `"... is outside Map.FIELD.bounds"` while the code compares against `Config.FIELD.bounds` — the exact distinction claim 8 is careful about everywhere else.
- `src/shared/Map/init.luau` `Map.SIZE_STUDS` comment — "the M2.1 slice is `MapGen.CONFIG.SLICE_STUDS`"; this task removed `Config.SLICE_STUDS`.
- `tests/server/map_contract.spec.luau` `it("flattens every spawn pad to exactly the ground the contract asserts")` — the comment says "the shooter posts stand 46 studs apart"; `WORLD.postSpacing` is 80.
- `src/serverstorage/MapGen/Layout.luau` `Marker.facing` — every marker sets it to 0 and nothing reads it; `Markers.place` builds every CFrame with `CFrame.lookAt(position, position + Vector3.zAxis)`. Read it or drop it.
- `src/serverstorage/MapGen/Config.luau` `SCATTER_CLEARANCE` comment — "studs a tree keeps from a hedge line, a track or the bog edge", but `Props.rejectTree` applies the clearance to hedges only; tracks and the bog are rejected at their exact edge.
- `Code commit: 805e0e0` names a commit that changed only `reviews/task-44/REQUEST.md`; CLAUDE.md git workflow step 4 gives `6bdd43b`. The tested tree is code-identical, so the bound is only stricter and nothing is unverified — but it is the third task with this label slip (36a(3)(f), 38a(h)).
- `docs/research/2026-09-24-map-generator.md` measurement H — "400 trees x 2 parts + 12 tie trees x 2 parts + 14 markers = 1,238" omits the 400 hedge segments from the addition; 838 + 400 = 1,238, so the total is right and the sum as written is not.

---
REVIEWER verdict on commit `f05d974d31a753ab579568c8a300e82f683b9e10` (round 1) · 2026-09-26 03:58 UTC · session cost $3.30, 38 turns · written by tools/agents.py
