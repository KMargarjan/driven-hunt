PASS

## Notes (non-blocking)
- `tools/studio_mcp.py` docstring, step 7a.2 — still reads "In `test2` the replay goes to the FIRST client only, named by its studio_id", which the code and the `Two players` section now contradict (`replay_input(studio, scenarios, token, check, studio_id=shooter)`). Change it to the shooter's client.
- `tools/studio_mcp.py` docstring, `Two players` item 5 — "the server and **client 1** are PASS with 0 failed/errors/skipped" names a client the code no longer uses, and the list omits the `One client is on the Shooters team` check. Claim 10's "every check … are now there" overstates by one. Rename to "the shooter's client" and add the missing check.
- `reviews/task-34/REQUEST.md` claim 8 — "Every exit path then stops the test instances and says to press Cleanup if any survive" is not what the code does. The report loop's `json.loads(raw)` and `studio.console(...)` sit inside the `try` whose only `finally` is `write_token("")`; an exception there propagates to `main`'s handler, which prints `[harness2] FAIL: ...` and returns without `end_session`. Round 1 raised this; the classify-failure path was fixed and this one was not. Wrapping the body in `try/finally: end_session(...)` would make the claim true.
- `tools/studio_mcp.py` / `run_test2` — the window advertised and the window waited disagree. `START_CLICKS.format(seconds=120)` promises 120 s (and is printed after up to 15 s of token-sync wait has already burned), while `run_test2(studio, wait_seconds=180)` accepts a Start up to 180 s later. `TestKit.TOKEN_MAX_AGE = 120`, so a Start at t≈150 s gives three refused runners, three empty reports, ~360 s of sequential 120 s waits and three `runner reported within 120 s` FAILs, with no line saying the click was late. Cap the wait at the token's remaining life, or print "the token expired — run it again" when the studios appear after it.
- `tools/studio_mcp.py` / `run_test2` — the two team queries are sequential and each may block 45 s, and the stage inside `replay_input` may then block 60 s waiting for a boar. `tests/client/input_driving.spec.luau` starts its own `ARRIVE_TIMEOUT = 25` countdown as soon as TestEZ reaches it, a few seconds after Start. In `run_test` the replay begins ~2 s after Play; here it can begin a minute or more after. If that margin is exceeded the shooter's client fails for a reason that has nothing to do with two players. Query both clients' teams concurrently, or bound the team wait well under the replay's slack.
- `tools/studio_mcp.py` / `run_test2` — the spec-name comparison hardcodes the container: `wanted = {"ServerStorage.Tests." + os.path.basename(f)[: -len(".luau")] for f in server_specs}`. `run_test` derives the instance path from the Rojo sourcemap (`file_to_path`), so it survives a spec in a subfolder of `tests/server/`; this one would report it as both "not run" and "not in repo".
- `tools/studio_mcp.py` / `run_test2` — no equivalent of `run_test`'s final `check("Gate closed afterwards (token cleared in Studio)", ...)`. The token is cleared in the `finally`, but nothing confirms Studio saw it, which is the check that stops Karen's next playtest running the suite.
- `tools/studio_mcp.py` / `run_test2` — `wait_for` returns `value = None` when its last call raised `RuntimeError`, so `f"{len(found)} new studio(s) beside the editor"` can raise `TypeError` and replace the `NEEDS KAREN: nobody pressed Start` line with a harness-fault line. `len(found or [])`.
- `tests/server/match_teams.spec.luau` — `it("splits two players into one driver and one shooter, and arms only the shooter")` is the only name the one-player harness ever prints, and in that session the body asserts the opposite thing (a lone `Shooter`). Name it for the invariant it actually holds at every size.
- `tests/server/match_live.spec.luau` / `it("put every player on a team, and armed exactly the shooters")` — the rewritten body is a bare `for … in ipairs(Players:GetPlayers())`, so with zero players it passes having asserted nothing. The suite's first `it` asserts `#Players:GetPlayers() >= 1` separately, but TestEZ carries on past that failure. One `expect(#Players:GetPlayers() >= 1).to.equal(true)` inside the loop's `it` would close it.
- `tests/client/match_client.spec.luau` — `expect(string.find(text, "SHOOTER") ~= nil).to.equal(true)` with the comment "The player is a Shooter in a one-player drive" is a second reason the driver's client can only ever be an observation, alongside the missing gun. Say so where the driver's report is printed, so nobody later tries to make the driver's suite green.
- `TASKS.md` row 34 — the status cell reads "**reviewed: PASS, 2 rounds**" in the commit under review, before this round's verdict existed; round 1's `reviews/task-34/RESULT.md` is three blocking findings, not a PASS. The Builder owns the status cell, but it should record the round it is asking for, not the answer it expects.

---
REVIEWER verdict on commit `936400357d139d59d952f9708a95ff6eda55bbb2` (round 2) · 2026-09-25 15:16 UTC · session cost $2.34, 35 turns · written by tools/agents.py
