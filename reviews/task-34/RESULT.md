1. `tools/studio_mcp.py` / `run_test2` — the wait for the test windows is one studio short, so the mode can start classifying before the third process registers and then abort a correctly started run. Evidence: `found, ok = wait_for(lambda: studio.studio_list(), lambda v: len(v) >= 3, wait_seconds, 2)`, while the preceding check demands `len(before) == 1` ("One Studio instance before the test starts") and `ESCALATE.md`'s closed Task 30 entry records what Karen measured: "`python tools/studio_mcp.py studios` listed **FOUR** studios: the DEV edit Studio ... plus three unnamed ones - the local server and the two clients". With one Studio before, `>= 3` is satisfied by two of the three test processes; `classify_studios` then re-lists immediately (`for entry in studio.studio_list()`), sees one server and one client, fails "Found exactly two client DataModels" and `return verdict()` — a FAIL that costs Karen the click and the whole session. Required: wait for three *new* ids (entries whose `id` is not in `{s["id"] for s in before}`), not for a total of 3.

2. `tools/studio_mcp.py` / `run_test2` — the replay target and the `[client1] status PASS` check are picked by list order, but only one of the two clients *can* report PASS, and nothing ties it to `clients[0]`. Evidence: `replay_input(studio, scenarios, token, check, studio_id=clients[0])` and `check(f"[{name}] status PASS", ...)` for `client1`; with two players exactly one is a Driver (`Match/Phase.luau` `enterAssigning` -> `Roster.assign`, and the join-during-Assigning branch `Roster.smallerTeam`), `MatchBoot.server.luau` does `Weapon.setArmingPolicy(Match.mayCarryWeapon)`, and `Match.mayCarryWeapon` returns `team == "Drivers" and Match.CONFIG.DRIVERS_MAY_SHOOT` with `DRIVERS_MAY_SHOOT = false` — so the driver's client has no Shotgun at all. `tests/client/weapon_client.spec.luau` asserts `expect(parked).to.equal(true)` where `parkTool` scans the Backpack/character for a `Shotgun` for 15 s, and `tests/client/shoot_boar.spec.luau` asserts a hit marker arrives from a fired shot. `clients` comes from `studio.studio_list()` order, which has no relation to who is the Shooter, so the mode's headline check is a coin flip and claim 7's reason ("client 2 has nothing to react to") is not the operative one. Required: ask each client which team its `LocalPlayer` is on (one `query("Client", ..., studio_id=...)` per client), replay into and check the **Shooter's** client, and report the Driver's client as the observation.

3. `tests/server/match_live.spec.luau` under `test2` — the server suite `run_test2` requires to be `PASS` contains an assertion that is only true with one player, so the "[server] status PASS" check is not reliably satisfiable in the mode this task exists for. Evidence: `it("put the player on a team, and it is the SHOOTERS, so they keep a gun", ...)` does `local player = Players:GetPlayers()[1] ... expect(player.Team.Name).to.equal("Shooters")` and `expect(Match.mayCarryWeapon(player)).to.equal(true)`; with two players one of them is a Driver, and when both join before `markersComplete` (the case `Match:step`'s re-read comment says happened "on one harness run in three") `Roster.assign` sorts the userIds and gives the **lower** one `Drivers` — i.e. `GetPlayers()[1]` is the driver and this `it` fails, failing `[server] status PASS` and `[server] > 0 passed, 0 failed`. Required: make that assertion player-count-aware (guard it with `#Players:GetPlayers() == 1`, which is now `match_teams.spec`'s one-player branch anyway), so the server suite is true for whatever number of players is there — the property the new spec was written to have.

## Notes (non-blocking)
- `reviews/task-34/REQUEST.md` claim 2 — "the five checks before the click pass, the sixth fails": `run_test2` has four checks before the wait (Edit mode, DEV place, one Studio before, token synced), and the failing one is the fifth. Count only.
- `tools/studio_mcp.py` / `run_test2` — `check("Every server spec file in the repo ran", len(ran) == len(server_specs))` compares counts, where `run_test` matches by name (`ran exactly the repo's {side} spec files`, using `not_run` / `not_in_repo`). A renamed spec plus a stale one passes. Reuse the name comparison.
- `tools/studio_mcp.py` / `run_test2` — `consoles[name] = studio.console(studio_id=studio_id)` is collected for all three instances but only `consoles.get("server", "")` is printed; both client Outputs are fetched and thrown away.
- `tools/studio_mcp.py` / `run_test2` — the early `if server is None or len(clients) != 2: return verdict()`, and any exception in the report loop, return without `set_play(False)` and without the "press Cleanup in the Test tab" line that the normal path prints. Karen is left with three windows and no instruction.
- `tools/studio_mcp.py` / `classify_studios` — `server = entry["id"] if server is None else server` silently drops a second Server-offering instance instead of listing it in `unknown`, so "Found one server DataModel" can pass when two were seen.
- `tools/studio_mcp.py` / `run_test2` — `if scenarios is not None: replay_input(...)` says nothing when there is no scenario file; `run_test` prints "no tests/client/input_scenarios.txt: nothing to replay".
- `tools/studio_mcp.py` — `[harness2]`'s final line has the same shape as `[harness]`'s but the mode runs none of `test`'s checks 4–6 (disk-vs-Studio comparison, no-script-outside-Rojo, spec placement). The docstring lists its checks honestly; one sentence saying a `[harness2]` line is not a substitute for a `[harness]` line as PR evidence would stop the two being pasted interchangeably.
- `tests/server/match_teams.spec.luau` — `it("has both teams in the Teams service, with Roblox assigning nobody")` and the one-player branch duplicate `match_live.spec`'s `it("made two teams that Roblox will not assign anybody to itself")` and its Shooters/`mayCarryWeapon` assertion. Fixing finding 3 by moving that assertion here would remove the duplication rather than add to it.
- `TASKS.md` — no row or status for this task: row 33 ("Harness runs the specs with 2 players") is still `todo` and no Task 34 dispatch is transcribed, so the queue does not show the task as in progress.

---
REVIEWER verdict on commit `b4935dbbe7504440140c0978e075036eabf1926d` (round 1) · 2026-09-25 15:06 UTC · session cost $2.99, 39 turns · written by tools/agents.py
