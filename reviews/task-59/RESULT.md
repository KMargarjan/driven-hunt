1. `tools/meshy.py` / `selftest`, case *"...and a re-download replaced its record rather than doubling it"* — the assertion cannot detect the feature it names; it passes with the dedup deleted. Evidence: the case runs on the `broken` run, whose first pass uses `fake_download_fails`, which raises `Failed("HTTPError: 403 the signed URL expired")` for **every** URL — so `preview.png` and `preview.glb` both fail, `entry["artefacts"]` is saved empty, and the record on disk has no artefact to double. The resume then appends two fresh entries, and `ok("...and a re-download replaced its record rather than doubling it", len(names) == len(set(names)) == 2, str(names))` holds identically with or without `entry["artefacts"] = [a for a in entry.get("artefacts", []) if a.get("name") not in names]` in `finish_preview`. The one run that *does* have a prior artefact on disk across a resume is `partial` (`fake_glb_fails` lets `preview.png` through, so the saved record carries one `preview.png` entry, and the resume re-downloads it) — and `partial` has no artefact assertion at all, only `"resume collects the GLB afterwards"` and `"...and both files are on disk"`. Request claim 8 names this case as the verification. Required: assert the artefact list on the `partial` run after `collect(partial["runId"], fake_download)` (names == 2, no duplicate `preview.png`), or make `broken`'s first pass download the thumbnail successfully so a prior entry exists to be replaced — and mutation-check it by deleting the filter line.

## Notes (non-blocking)
- `tools/meshy.py` / `cmd_preview` — a 2xx POST whose body carries no parseable task id still writes `record["state"] = "failed"` (`"the create response carried no task id"`). Meshy accepted the request, so a task was probably created and paid for, but there is no id to resume with, so this is the one remaining hole in "money already spent stays collectable". Unchanged by this task and not fixable by `resume`; the printed line could at least send the operator to the Meshy dashboard. Queue it.
- `tools/meshy.py` / `selftest`, claim 6's mutation counts — not verifiable from the repo, and reading the cases I count more than the request states (reverting `stop_resumable` to `failed` looks like ~14 failing `ok(...)` calls, not 11; narrowing `RESUMABLE_STATES` looks like ~10, not 8). The counts are prose either way.
- `tools/meshy.py` / `selftest`, `collect` — catches `Refused` so a state regression reads as a named failing case, but a `Failed` raised inside `cmd_resume` (for example `read_status` on a malformed fake response) would still end the selftest before the later cases run. Same class as the thing claim 6 fixed.
- `docs/asset-briefs/README.md` and `docs/ASSET_PROMPT.md` — both now say the reference images live "beside them" in `docs/asset-briefs/`, and none is committed, so `validate_brief`'s `reference_names` check makes every image-to-3d brief unusable today. The Builder disclosed this and queued it as 59a(f); it is Karen's or the Director's call.
- `docs/design/meshy-tool.md` — still §4.1/§16 `<assets-dir>/briefs/` and "a reference that is not a PNG", no `preview-unresolved` row, no `STOPPED`, no `POLL_GIVE_UP_AFTER`. `reviews/task-59/DESIGN_DELTA.md` is the right place for it; it needs an Architect run to land.
- `tools/meshy.py` / `selftest` — blocks are still literally numbered `# 1.` … `# 11.` and the privacy block still says *"or case 5 is guarding nothing in CI"*, while `redact`'s docstring claims "Blocks are named, not numbered (Task 55b)". The Builder says so in "What I could not verify"; queued as 59a(e).
- `request` — the trailing `raise Failed(f"429 after {RETRIES + 1} tries: ...")` is unreachable: at `attempt == RETRIES` the 429 branch's `attempt < RETRIES` is false and the function returns `(429, parsed, raw)`. Harmless, and it is what makes claim 1's "5xx/429" reach the give-up counter rather than aborting.

---
REVIEWER verdict on commit `d2c6fa9f405bd229c5ac9c5bdfc70db81d946203` (round 2) · 2026-09-26 13:44 UTC · session cost $2.71, 35 turns · written by tools/agents.py
