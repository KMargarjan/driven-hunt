1. `tools/meshy.py` / `poll_and_finish` (the new give-up branch) vs `cmd_resume` — the give-up sets `state = "failed"`, and `cmd_resume` refuses any state but `preview-running`, so a task that is **still running and already paid for** can never be collected by the tool again. Nothing in the file ever sets a run back to `preview-running` (searched every `record["state"] =` assignment: `brief-ok`, `preview-running`, `failed`, `preview-ready`, `approved`, `expired`). Evidence: the branch does `record["state"] = "failed"` … `print(f"[meshy] FAILED: {record['runId']} {phase} cannot be polled -- {why}. The task may still be running at Meshy; \`runs\` has the id.")`, while `cmd_resume` does `raise Refused(f"run is {record['state']}; only preview-running can be resumed in this task")`. It fires on the **transient** path too — three consecutive non-200s (`consecutive >= POLL_GIVE_UP_AFTER`), i.e. ~15 s of 5xx/429, after `request` has already retried each poll four times — which the old code survived by falling through to `PENDING` plus *"re-run: python tools/meshy.py resume <run-id>"*. It also fires on 401: rotate the revoked key and the paid run is still unresumable. That leaves only `meshy.py preview` again, which the design forbids for exactly this reason (`docs/design/meshy-tool.md` §8: *"a second POST would pay twice for the same model"*; `docs/ASSET_PROMPT.md`: *"Never run a second `preview` for a run that is still going. Use `resume`."*). It also mislabels the record: §8/§7 reserve the `failed` terminal for *"a Meshy task came back FAILED, or a ceiling stopped the run"*, and here the task entry is still `PENDING`. Required: keep the run collectable — either leave the state resumable (or add a distinct state `cmd_resume` accepts) instead of the `failed` terminal, and say so in the printed line; plus a selftest case that `resume` still works after a give-up, or the same class reopens.

2. `tools/meshy.py` / `finish_preview` — the new failure line tells the operator to do something the code refuses. Evidence: it sets `record["state"] = "failed"` and prints *"The task itself SUCCEEDED and its credits are spent: `runs` has the id, and the model URLs **can be re-minted by re-polling** until the 3-day expiry."* Re-polling is `resume`, which refuses a `failed` run (`only preview-running can be resumed in this task`); `cmd_status` re-polls but never downloads. So a SUCCEEDED, paid preview whose thumbnail download failed is unrecoverable by any command, and the instruction printed to the ASSET agent is false. Required: make the run re-pollable (same fix as finding 1), or change the line to state plainly that the artefacts cannot be fetched by this tool.

## Notes (non-blocking)
- `docs/design/meshy-tool.md` — still says briefs live in `<assets-dir>/briefs/` (§4.1 line ~211, §16 table row *"reads `<assets-dir>/briefs/*.brief.json` and reference PNGs"*) and *"a reference that is not a PNG"* is refused (§4.1 line ~232). The code now reads `docs/asset-briefs/` and accepts four suffixes. The Architect owns that file; queue the §4.1/§8/§16 update, including a row for the new `POLL_GIVE_UP_AFTER` (§16's constants table does not list it) and for "a poll that cannot reach Meshy" in §8's failure table.
- Claim 6 / `selftest` — "Blocks are **named**, not numbered … cannot come back" overstates it. The selftest's blocks are still literally numbered (`# 1.` through `# 11.`), and one comment still cites a number: *"or case 5 is guarding nothing in CI"*. Only the three cross-references were reworded; the numbering they can rot against is untouched.
- `docs/asset-briefs/` — holds only `README.md` and the three briefs; no reference image is committed. So claim 7's stated benefit (*"the gun and the trees could not use references at all"*) is not yet reachable: `validate_brief` requires each reference to be a name found in that folder. Committing Karen's JPEG/WebP photographs into a public repo is her or the Director's call; queue it as a decision rather than assuming it.
- `finish_preview` — the guard is `preview.png` only. A `preview.glb` whose download fails still leaves `state = "preview-ready"` with nothing but `print(f"[meshy] note: {name} did not download ...")`, and the GLB is the artefact Task B needs before the 3-day expiry.
- `selftest`, the on-disk block's `finally` — `os.environ.pop("MESHY_API_KEY", None)` discards whatever was there instead of restoring it, unlike the `previous` dance done for `MESHY_RUN_DIR` two lines below. Harmless today (the process exits), but asymmetric and it silently drops a real key from the process env.
- `TASKS.md` — the two new rows are numbered `55b` and `55b-a` while the task, the branch gate and `reviews/task-59/` are 59; the request's title line also still reads "Task 55b". Paperwork only.

---
REVIEWER verdict on commit `52dadb8f2f5666046c83eb429f847345cd28efd2` (round 1) · 2026-09-26 13:17 UTC · session cost $2.20, 27 turns · written by tools/agents.py
