1. `tests/server/boar_hit.spec.luau` / `describe("the boar's teardown")` → `it("leaves no signal behind when the runtime is destroyed")` — the test cannot fail. It counts `BindableEvent` descendants of its own `testRoot` folder, but neither runtime signal is ever parented: `src/server/Boar/init.luau` `Boar.newRuntime` does `self._signal = Instance.new("BindableEvent")` and `self._hitSignal = Instance.new("BindableEvent")` with no `Parent` assignment anywhere, so `bindables` is 0 before `destroy()`, after `destroy()`, and with `self._hitSignal:Destroy()` deleted from `Runtime:destroy`. The sibling assertion `expect(#testRoot:GetDescendants() > 0).to.equal(true)` is also always true (`Body.ensureFolder` leaves the `Boars` folder behind). This is the only test of design §6.2/§13.1 item 7 ("`Runtime:destroy()` leaves no `BindableEvent` behind"), which is the reason `_hitSignal:Destroy()` was added in this diff — so the feature it exists for is untested. Required: assert on the signal itself rather than on a folder scan — e.g. hold a reference to a connection/`runtime.Hit` and assert that firing or connecting after `destroy()` behaves as a destroyed `BindableEvent` does, or have the runtime parent its signals somewhere countable and count those. Either way the test must go red if `self._hitSignal:Destroy()` is removed.

## Notes (non-blocking)
- `TASKS.md` — there is no row 24 and the file is unchanged from base `bb674dc` (`.agent-evidence/changed-files.txt`). `REQUEST.md` "Task" says the dispatch is "verbatim in `TASKS.md`" and claim 10 says the three measurements are "recorded in the commits and in `TASKS.md`"; the commits carry them, `TASKS.md` does not. The status row is paperwork and may still be committed after the code commit (CLAUDE.md git workflow step 4).
- `src/server/Weapon/init.luau` / `runReload` — `if reason and step.kind ~= "Load" then ... return end` swallows *every* `Load` rejection, not just the intended `"no-reserve"`. A `Load` rejected `"busy"` or `"barrel-full"` silently continues the sequence and the player gets a closed gun with fewer shells than they asked for, with no reason published. Name the accepted reason explicitly.
- `src/server/Weapon/init.luau` / `publish` — `lastPublish[player]` is written on every publish and never read; it is a per-player table that exists only to be cleaned up in `forget`. Drop it.
- `src/server/Weapon/init.luau` / `Weapon.grant`, `watchEquipped`, and `src/client/Weapon/Input.luau` / `watchTool` — both accumulate connections without pruning: `connections[player]` gains one `GetPropertyChangedSignal("Parent")` entry per grant (i.e. per respawn) and is only cleared in `forget`; `watchTool` adds a fresh `Equipped`/`Unequipped` pair every time the Tool moves between the Backpack and the character. `bind()`/`sync()` are idempotent so behaviour is unaffected, but both lists grow for the whole session.
- `tests/server/weapon_shot.spec.luau` / `describe("shot cost")` — the `Hits.group` half of the 1,000-cycle budget is a no-op: every synthetic impact has `instance = nil`, so `Hits.targetOf` returns on its first check and nothing is grouped. The budget therefore measures `Pattern.directions` only. Give the impacts a `Damageable` part if grouping is meant to be inside the 500 ms.
- `tests/client/weapon_client.spec.luau` / `it("never touched the camera while aiming")` — the drift assertion sits inside `if down.offset and up.offset then`, so if `CameraSubject` is ever not resolvable to a `BasePart` the measurement (and the printed number claim 8 rests on) silently disappears while the test still passes. Assert that both offsets were captured.
- `tests/client/weapon_client.spec.luau` / crosshair ancestor walk — `crosshair.Parent` is the `ScreenGui` and `PlayerGui` is not a `GuiObject`, so the `node:IsA("GuiObject")` branch never executes; what the loop actually proves is reachability to the DataModel. That is worth having (and `gui.Enabled`/`gui.Parent` are checked separately), but the comment "Parent visibility, which is what a 'visible = true' audit misses" overstates what this loop does.
- `docs/design/shotgun.md` §13.3 vs `tests/client/input_scenarios.txt` — the committed scenario is three clicks and one `R`; the design's is five clicks and two `R`s, and its assertions 2 and 4 still say "the three rejected clicks" and "though `R` was pressed twice". The deviation is measured, explained in the spec header and declared in claim 10, and the two dropped properties are proved exactly in `weapon_state.spec`, so this is the right call — but the Architect-owned design is now stale on this point and should be queued for the next regeneration (the Builder may not edit it, rule 3).
- Screenshots (rule 5) — design §13.4 asked for the readout in three states (full, after one shot, mid-reload) and the crosshair "over grass and over the grey plate". The request describes the full readout (`[*]* SLUG 24`) and one crosshair view; the other readout states and the two-background check are not described. The four that are described are specific enough to be evidence.
- `src/client/Weapon/Effects.luau` / `Effects.play` — tracers are drawn only for positions in `payload.impacts`, so a clean miss produces a muzzle flash and nothing else; there is no tracer along a missed pellet's path. Separate from the request's "could not frame a tracer".
- `src/server/Weapon/init.luau` / `publish`, `onFireRequest` — `Shotgun.remotes()` performs five `WaitForChild` calls and is invoked on every publish and every broadcast. Resolve it once in `start()`.
- `GAME_DESIGN.md`, arena row — still reads "Since Task 22 the arena is the only thing in Workspace", which `Workspace.Boars` and now the client's `Workspace.WeaponEffects` contradict at run time. Outside this task's change, but this task made it one system less true.

---
REVIEWER verdict on commit `cfa9adbea3de5b1cb2a47f61975fbcbaac2ddf52` (round 1) · 2026-09-25 09:21 UTC · session cost $5.20, 68 turns · written by tools/agents.py
