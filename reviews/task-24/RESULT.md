PASS

## Notes (non-blocking)
- `docs/design/shotgun.md` §13.3 vs `reviews/task-24/DESIGN_DELTA.md` — the delta doc's item 6 covers only the click/`R` count and X's 2.6 s, but §13.3 is stale in three more places the Architect will need: the scenario row `| X | SelectAmmo | ammo flips to "Buck", loaded unchanged |`, assertion 1's `reserve == 24` and assertion 4's "`reserve` ends at **22**" (scalars), and assertion 6's `loaded == {"Slug","Slug"}` (`SelectAmmo` did not rewrite loaded shells)` — the committed spec now asserts the opposite (`expect(state.loaded[1]).to.equal("Buck")`). Add them to DESIGN_DELTA.md so the next `design shotgun` run does not have to rediscover them.
- `src/server/Weapon/init.luau` / `onActionRequest` `SelectAmmo` branch — the empty-pocket refusal is the one part of Karen's rule with no test at any level: `stats.emptySwaps += 1` and `publish(player, state, true, "NO " .. string.upper(ammo))` are unreachable from a spec because `onActionRequest` is a local and the harness cannot drain 24 shells. The request says so honestly. `onActionRequest`'s `SelectAmmo` guard is small enough to extract as a pure predicate (state, ammo) -> refusal reason? and unit-test; queue it in `TASKS.md` row 24a.
- `src/server/Weapon/StateMachine.luau` / `StateMachine.apply` — `if kind ~= "SelectAmmo" and now < state.busyUntil` still exempts `SelectAmmo` from the busy check, a leftover from when X was a label swap. Now that X starts a 2.0 s reload, the busy check exists twice: once as that exemption's absence in the reducer and once as `now < state.busyUntil` in the owner's `SelectAmmo` branch. Safe today because the owner is the only caller, but it is the "two correct pieces of code disagreeing" shape `docs/PROJECT_CONTEXT.md` warns about. Either drop the exemption or comment why it stays.
- `src/client/Hud/init.luau` / `textFor` and `src/server/Weapon/init.luau` / `publish` — `notice` is sticky: it is carried on one snapshot and the Hud shows it until the next `StateChanged`, so after an X into an empty pocket the readout keeps `NO BUCK` indefinitely if the player does nothing else. Nothing clears it on a timer. Decide with Karen whether it should fade.
- `src/server/Weapon/init.luau` / `runReload` — `TASKS.md` row 24a(a) (abort after `Break` leaves `open == true` and the gun unusable) is still open and now has a second entry path, X. I re-checked reachability on the new code: after a successful `Break`, `"busy"` cannot occur (each `task.wait(step.wait)` outlasts the `busyUntil` that step set, including the `no-reserve` walk-past which leaves `busyUntil` at the earlier `Load`'s deadline), `"barrel-full"` and `"is-closed"` cannot occur, so there is still no reachable path. Applying `Close` on an abort would close it for good.
- `reviews/task-24/REQUEST.md` header — "82 server assertions across 8 spec files and 27 client across 3" counts TestEZ `successCount`, which is test cases (`it` blocks), not assertions; the same wording is in `TASKS.md` row 24. Cosmetic.
- Merge gate, not a code defect — `TASKS.md` row 24 says "NOT MERGEABLE until Karen plays it", and the request states plainly she has not played this X. The 24-of-each `START_RESERVE` is the Builder's number, flagged as a dial for the Director.

---
REVIEWER verdict on commit `b8a9f397539d809b6880fc3546cbe1fac1c1913f` (round 1) · 2026-09-25 09:55 UTC · session cost $2.10, 33 turns · written by tools/agents.py
