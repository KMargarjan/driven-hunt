PASS

## Notes (non-blocking)
- `Props.furniture` — decides `CanCollide` at length and says nothing about `CanQuery`. `part()` leaves it at the default `true`, so every piece stops a raycast while being walk-through. This project decides `CanQuery` explicitly everywhere else: `Config.BRUSH` says "CanCollide and CanQuery are both false, so a bush never eats a slug", and Task 63 made crowns `CanQuery = true` on purpose. Most furniture is out of any line of fire, but `logPile.at[2]` (`x = 420, side = 1` → `z = −680`, 6.6 studs tall, x 412–428) stands 20 studs downrange and 20 studs right of shooter post 7 (`(7 − 4.5) × 160 = 400`), so a slug can be absorbed by logs a player walks through. Decide `CanQuery` per kind, or widen 66a(a), which currently covers `CanCollide` only.
- `Layout.highSeats` / `Layout.tieTreePoints` — high seat 4 and tie tree 12 are both at `x = 560` (`(8 − 4.5) × 160` and `−560 + 1.0 × 1120`), 5 studs apart in z (−740 vs −745). `highSeat.clearRadius` only removes *scattered* trees; the tie trees are placed. Nothing actually intersects (legs at x 557.15–557.85 / 562.15–562.85 clear the 3-stud trunk by 0.65 studs, and every crown starts at y ≥ 26 above the roof at ≈19.4), but it will read as a tower built around a trunk. Shift `highSeat.atPosts` or add an offset.
- `Layout.furniture`, the fence rails — each rail sets `anchorX = middle` while its posts set `anchorX = x`, so rails and posts sample different ground heights on RELIEF 40 field terrain and will not line up. Claim 4's "one ground height per object" is applied to the high seats and the log piles but not here.
- `REQUEST.md` claim 7 — "fence 10 posts + **18 rails**" is 16: the rail loop runs `span` 800→980 (10 spans), middles 810…990, and middles 870 and 890 fall in the gate gap, leaving 8 spans × 2 `railHeights`. The breakdown as written sums to 139; the stated total 137 is what the code produces.
- `REQUEST.md` claim 7 — "6,387 → 6,393 parts": the *before* figure is wrong. `TASKS.md` row 63 records 6,264, and 6,264 − 8 (four trees × trunk+crown) + 137 = 6,393, so only the first number is off.
- `REQUEST.md` claim 7 — "(before the last two shots-only commits)" is not true of `c16f1f3`, which changed `Config.FURNITURE.logPile` (`offsetZ` 30→20, log size) and `reeds` plus `Layout.luau`. `MapGen.digest` hashes every descendant's `Position` and `Size` (`digestEntries`), so the quoted digest `e064d598…` is not the digest of the reviewed code. The determinism *property* is unaffected — and `contract OK`, the 25/25 reachability and the 137/250 budget all survive the change, since the moved pieces are non-collidable — but the value and the label are stale.
- `Config.FURNITURE.logPile` — the header says "stacked 3 + 2" while `Layout.furniture` builds `rows = { 3, 2, 1 }`, six logs per pile; the comment two lines below ("three rows") and claim 7's "log 12" are the correct description.
- `Config.FURNITURE.highSeat.rail` vs `Layout.furniture` — Config says "across the back, facing the drive", Layout says "across the DRIVE side, so the seat reads as facing the wood it watches". The code puts it at `seat.z + platform.Z / 2`, the +z drive side; the two sentences contradict each other about which way the seat faces.
- `tools/mapgen.py`, `map-stand-close` — the comment says "the next one at 160 studs"; from the camera at `x = −305` the next stand (`x = −80`) is 225 studs away, 160 being `postSpacing`. Claim 1's "~230" is the right figure.
- `Layout.furniture`, the reeds — the ring's radius runs `bog.radius − 12 ± 8` = 100–116 studs about `bog = (−820, −300)`, so it crosses `x = −760`, where `WORLD.hedgeX` runs a hedge line the full depth of the map. A clump or two will stand inside a hedge segment.
- `TASKS.md` row 66 — pastes `[harness] PASS: 30/30 @ 2fcafba` and no `[harness2]` line, while the request's `Code commit:` and both harness lines are `41f4735`.
- `docs/design/map-generator.md`, the `Layout.luau` module row — lists Layout's functions and does not mention `Layout.furniture` / `Layout.highSeats`. The Architect's file; queue it rather than fix it here.

---
REVIEWER verdict on commit `004c48891b08d48ae1200fa6ccf5e9399e92349f` (round 1) · 2026-09-26 18:38 UTC · session cost $2.72, 27 turns · written by tools/agents.py
