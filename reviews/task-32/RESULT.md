PASS

## Notes (non-blocking)
- `tools/studio_mcp.py`, module docstring — claim 12 says the stage wait and the report window are "in the docstring, which is the single source of truth", and they are not. The docstring was not touched by this task: its Staging section still reads "it finds the FIRST BasePart inside Workspace.<targetFolder> -- the target;" with no mention of waiting, and it states no report window at all. Only the inline Luau comment ("WAIT for a target rather than failing on the first look") and the Python comment ("# 120 s: …") changed. CLAUDE.md "Run / test" says to update the docstring with any change to the test system. Queue the two sentences.
- `src/server/Match/init.luau` `dispatch` / `applySpawnBoar` — the owner's half of the round-1 fix is exercised by no spec. `match_phase.spec` "does not lose a release to a JOIN that lands on the release moment" passes `aliveBoars = cfg.MAX_ALIVE_BOARS` by hand, so it would pass unchanged against round 1's owner (round 1's `maybeRelease` already read `event.aliveBoars or 0`); "takes a release back when the runtime refused it after all" calls `Phase.releaseFailed` directly, never through `applySpawnBoar`. What is untested is exactly the two lines that were wrong: `if filled.aliveBoars == nil then filled.aliveBoars = boarsAlive() end`, and `state = Phase.releaseFailed(state, now())` in the pcall-failure branch. `Match` being a started singleton makes this hard today; queue a seam (e.g. assert `Match.stats().releasesDeferred` against `releasesDone` in `match_live`, or expose the dispatch path).
- `src/server/Match/init.luau` `dispatch`, `Assigning` block — `snapshotCache = nil` runs *before* `applyEffects`, but `board = Score.new()` and the re-`Score.join` of every player run *after* it. The `broadcast` effect on that step therefore builds and caches a snapshot from the previous drive's board, and the cache is not cleared afterwards, so it survives until the next dispatch (≤0.25 s at `MATCH_STEP_HZ = 4`). Clearing `snapshotCache` at the end of that block, and forcing one broadcast, would make the drive-start snapshot correct on the first push.
- `tests/client/shoot_boar.spec.luau`, "was staged by the harness" — the comment still reads "The harness waits 90 s for the client report AFTER the replay"; this commit made it 120 s.
- `tests/client/shoot_boar.spec.luau` — the same commit that gave the stage a 60 s wait (because no boar exists until ~`INTERMISSION_SECONDS + FIRST_RELEASE_SECONDS` ≈ 40 s into the session) *cut* this spec's own waits, 70 → 45 and 20 → 16. It passed this run, but the budget from suite start to `latchedAt` is now roughly 65–70 s against a ~40 s release, and a slow Play start eats that margin. Worth a line in `TASKS.md` rather than a change now.
- `src/server/Weapon/init.luau` `mayArm` — the pcall fallback claim 5 makes ("a policy that errors falls back to the default rather than leaving a player permanently unarmed") has no test; no `tests/server/weapon_*.spec.luau` changed, and none drives `setArmingPolicy` with a throwing policy. The happy path is covered end-to-end by `weapon_client.spec` needing a Tool in the Backpack.
- `tests/server/match_live.spec.luau` — round 1's note stands: `Match.forget` and `Match.stop` are asserted nowhere. `expect(Match.trackedPlayers()).to.equal(#Players:GetPlayers())` passes with `Match.forget` deleted, so design §11.6's "per-player tables held after a player leaves: 0" has no test.
- `reviews/task-32/REQUEST.md` claim 1 — says `GAME_DESIGN.md` has "three amended" rows; there are four (TestArena, Boar AI, Weapon, UI). The Weapon row was amended in round 2 in response to round 1's note, and the count was not updated.

---
REVIEWER verdict on commit `ff5ca4ba22feb0f4df5d86150ad3bf54de218dcf` (round 2) · 2026-09-25 14:51 UTC · session cost $4.27, 55 turns · written by tools/agents.py
