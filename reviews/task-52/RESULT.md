PASS

## Notes (non-blocking)
- `tools/studio_mcp.py` / `FLAG_NAME_RE` — the comment reads `# Flags.NAME_PATTERN, kept in step by a spec`, and no spec does that. Nothing in `tests/server/flags.spec.luau` asserts the Python `r"^[A-Z][A-Z0-9_]*$"` or the hard-coded `len(name) > 40` against `Flags.NAME_PATTERN` / `Flags.MAX_NAME_LENGTH`. Harmless today (the Luau side still checks `Flags.DEFAULTS[name]`), but either add the assertion or drop the claim from the comment.
- `src/shared/Flags/init.luau` / `resolveOnClient` — the comment says "The SIGNAL wakes it and the DEADLINE bounds it: a bare poll would cost up to one interval of latency on every boot", and the research note repeats it as difference 3. The loop is `task.wait(0.05)` and re-reads `GetAttribute(Flags.DIGEST_ATTRIBUTE)` itself each pass, so the connection saves nothing over the poll it sits beside. Behaviour is fine; the rationale overstates it.
- `src/shared/Flags/init.luau` / `ensure` — `began = os.clock()` is taken before `resolveOnClient()`, which may yield up to `Flags.WAIT_SECONDS`. Any client that waits at all will print `[flags] resolution took N ms, over the 1 ms budget`, which is about the wait, not the resolution. Time the resolve only, or exempt the wait.
- `tests/server/flags.spec.luau` / `describe("the worked example")`, first case — `expect(Match.CONFIG.TIE_UNTIL_DRIVE_END).to.equal(Flags.isOn("TIE_UNTIL_DRIVE_END"))` passes identically with the pre-task literal `TIE_UNTIL_DRIVE_END = true`, so it does not demonstrate the migration. The migration is real (I read `Match.CONFIG`) and the second case exercises both states through `Penalty.expired`, so nothing is missing — the first case just proves less than it reads as.
- `tests/server/flags.spec.luau` / case "has no writer, and publishing twice changes nothing" — it calls `Flags.publish(Flags.all(), Flags.source())`, while `src/server/FlagsBoot.server.luau`'s header says "Nothing else in the repo may call Flags.publish". The effect is nil (same digest, same attributes) and the case is worth keeping; name the spec as the one exception in the FlagsBoot comment or in `GAME_DESIGN.md`'s row so rule 3's written owner and the tree agree.
- `tools/studio_mcp.py` / `selftest` block 9b, "every argument builder asks _scoped" — the count is taken over this file's own text and includes the two literals on the assertion line itself. It is correct now and the mutation check is genuine, but any later comment or string containing `self._scoped(` or `"arguments": args` moves the count and fails CI for no real reason.
- `tools/studio_mcp.py` / `run_test2` — `studio.default_studio_id = before[0]["id"]` assumes `before[0]` is the editor. When the "One Studio instance before the test starts" check has already failed (leftover test Studios from an earlier run that Karen has not Cleaned up — the very situation this task documents), that id may be a test process and the run carries on scoped to it. `run_flags` does this properly with `studio_for_role(studio, "edit")`; `run_test2` could too.
- `tools/studio_mcp.py` / `run_test` — it never sets `default_studio_id`, so by the task's own argument (the three test Studios outlive a `test2` session) a one-player `test` run after a `test2` is refused at `studio.mode()`. Not a regression and not this task's statement, but it is the same class the task closed for `test2`; worth a `TASKS.md` row.
- `reviews/task-52/REQUEST.md` — 13 claims over ~206 lines against CLAUDE.md's "one page … at most 10 claims". Context only, not a deliverable.

---
REVIEWER verdict on commit `100666269323411b0e4ecb4778f1c34e58f731a3` (round 1) · 2026-09-26 12:17 UTC · session cost $3.78, 58 turns · written by tools/agents.py
