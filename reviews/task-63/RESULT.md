PASS

## Notes (non-blocking)
- Round 1's blocking finding is genuinely fixed, verified in the repo rather than from the request: `Scatter.weighted` inserts `{ x = x, z = z }` with no `key`; `Scatter.speciesAt(x, z, seed, config, groundY)` has no key parameter and draws `Scatter.pointKey(x, z, seed, Scatter.STREAM.species)`; `Props.trees`' only call site is `Scatter.speciesAt(point.x, point.z, seed, config, y)`. The grep for `point.key` finds it only in `Scatter.brush`'s sort over `jitteredGrid` candidates, which is the same question that drew it. The independence argument holds: `dx`, `dz` and `key` are separate `rng:NextNumber()` draws, so the point's position — and therefore its positional species key — is independent of the acceptance key. The new spec is a real test: with the mutation the thin tier's `other` is 0 and `expect(other > expected - 0.08)` fails, and `(1 - 0.65) * (1 - (0.16 + 0.0625 + 0.04 + 0.0225)) = 0.2503` is the correct derivation.
- **Harness timeline, for the Director to confirm before merging.** `4755ad8` is authored 19:42:25 and `20f2281` — the commit that first pastes *both* lines at `4755ad8` — 19:47:07: 4 min 42 s to hold a `test` that `tools/studio_mcp.py`'s docstring measures at "about 90 s" plus a `test2` that CLAUDE.md calls "a human click and about eight minutes". Nothing untested can have slipped through — `git diff a2ebfca..4755ad8` is `TASKS.md` and `REQUEST.md`, and `.agent-evidence/paperwork-after-code-commit.txt` says only `REQUEST.md` followed — so the substance of the gate holds either way, and the Builder discloses that `[harness2]` is the Director's run. But the `[harness2] @ 4755ad8` line is the one piece of this I cannot check and the arithmetic is tight.
- `TASKS.md` row 63 — still `[harness2] PASS: 32/32 @ 1484bac` and "377 server specs", stale against the request's `4755ad8` and 378. Row 63 also still opens on round 1's text; the round-2 paragraph is appended.
- `map_contract.spec`, "draws the species independently of the density" — the tier cut is `MapGen.Layout.treeDensity(point.x, point.z, c) < strength`, where `strength = c.SPECIES_CLUMP_STRENGTH`. A species constant is answering a density question: it works only by the coincidence that `0.14 < 0.42 < 0.65 < 1.0`. Move the tier's own number (`c.TREE_DENSITY.dense`, or `< c.TREE_DENSITY.drive * 1.5`) into the comparison — this spec is the one place that should not repeat the fault it was written for.
- `map_contract.spec`, "plants Karen's four species... with alder in the wet" — the four offsets are `(±10, 0)` and `(0, ±14)` from the bog centre, i.e. all inside one `SPECIES_CLUMP_STUDS = 260` clump, and `groundY` is the literal `-20`. So claim 7's "four points instead of two" is still effectively one sample of the override, and it asserts nothing about the ground at the bog actually being below `SPECIES_WET_DEPTH`. If that clump's leader were alder the assertion would pass with the override deleted. Sample points in different clumps, or pair it with `speciesAt(same x, z, ..., 0) ~= "alder"`.
- `REQUEST.md` claim 6 — calls `map_contract.spec:1131` "its tier-agreement line". 1131 is the thin tier's `expect(other > expected - 0.08)`; the tier-agreement line is `expect(math.abs(thin - dense) < 0.08)` at 1146. The failure it describes is consistent (thin = 0 fails 1131 first, before the agreement line runs), so the run is credible — the label is wrong.
- `Scatter.hash01` — `h * 1274126177` with `h` up to 2.147e9 reaches ~2.7e18, past 2^53, so the second multiply is rounded before the `% 2147483647` and the function does not compute the modular hash its comment describes. It stays deterministic (IEEE doubles, same ops) and the measured deciles show it is flat, so nothing to change now, but the comment overstates what the arithmetic is.
- `map_contract.spec`, per-tier species bands — the `× 0.4 … × 1.8` loosening of design §16.1's ±25 % is now in a second place (per tier as well as overall), with the reason in a comment. Row 63a(i) already says loosening an Architect's number belongs in `ESCALATE.md`; round 2 widened its footprint instead of escalating it.
- `docs/design/map-generator.md` §7.1 and the module table at line 143 — §7.1 still writes `Scatter.speciesAt(x, z, seed, config)` and describes "the candidate's own drawn key", and line 143 lists only `speciesAt` / `weighted` / `brush` as Scatter's new functions. The code now has a fifth `groundY` parameter plus public `pointKey`, `STREAM` and `clumpLeaderAt`. The design is the Architect's file, so this is a queue item, not an edit.
- `Scatter` header vs `Height.at` in the spec — the header's "the closest two trunks anywhere in this map are over 11 studs apart" is cited as why no two trees share a `pointKey`; the walkability spec measures that over 5 seeds only, so the sentence is an extrapolation. Separately the spec feeds `speciesAt` a `Height.at` y while `Props.trees` feeds it `Height.atFlattened`; those agree for every accepted tree (pads and benches are density 0), so this is a readability note, not a divergence.

---
REVIEWER verdict on commit `20f228152be7d8303504b413a6efaf3da71b2594` (round 2) · 2026-09-26 17:53 UTC · session cost $2.67, 32 turns · written by tools/agents.py
