1. `Scatter.weighted` / `Scatter.speciesAt` / `Props.trees` — ONE DRAWN NUMBER ANSWERS TWO UNRELATED QUESTIONS, and it silently defeats the species mix over roughly half the wood. `Scatter.weighted` keeps a candidate only when `key < weight(x, z)` and then stores that same `key` on the point; `Props.trees` passes it straight on as `Scatter.speciesAt(point.x, point.z, (point :: any).key or 0, ...)`, and `speciesAt` does `if key < config.SPECIES_CLUMP_STRENGTH then return leader end`. `SPECIES_CLUMP_STRENGTH = 0.65`, `TREE_DENSITY.drive = 0.42` and `TREE_DENSITY.backdrop = 0.14`, so every candidate accepted outside the dense band has `key < 0.42 < 0.65` and **always** returns the clump leader — the "rest drawn from the same shares" branch is unreachable there. Evidence: design §7.1 says "a low-frequency `math.noise` field ... chooses a band, **and the candidate's own drawn key picks inside it**"; `Config.SPECIES_CLUMP_STRENGTH`'s comment says "The rest is drawn from the same shares, so a spruce stand has birch in it"; request claim 6 says "65 % of a clump is its leader and the rest is drawn from the same shares". By the request's own numbers (17.71 trees/10k over the 1400 × 520 dense band ≈ 1,289 of 2,737) about 53 % of the wood is in the drive and backdrop tiers, where every clump is a pure monoculture. The marginal shares survive (the leader is picked by `pickByShare`), which is why claim 5's measurement still looks right and no spec catches this. Required: give the species its own number instead of reusing the acceptance key. `Scatter.weighted` already knows the weight, so `speciesKey = key / w` is uniform on [0,1) for an accepted candidate and **changes no tree position or count** — N2 and N3 stand as measured and only the species assignment moves. A fresh `rng:NextNumber()` also works but moves the whole wood and costs a rebuild.

## Notes (non-blocking)
- `reviews/task-63/REQUEST.md`, N3 vs claim 1 — the build reports **2,746** trees and claim 1 quotes the spec's note as **2,737**. The point count is a pure function of seed and config (`Scatter.trees` never touches `Height`), and the spec runs at `SEED = 7`, so at one seed the two must be equal. The request never names the build's seed. Name it, or reconcile the nine trees.
- Claim 9, `Config.SPECIES` header comment, design §7.1 — "no colour channel maximum below 120" is false of three of the four trunk colours: spruce `Color3.fromRGB(78, 62, 46)`, alder `(84, 72, 60)`, oak `(96, 78, 58)`, and of `Config.TREE_PROXY.trunkColor` `(78, 62, 46)`. Spruce's trunk is darker than the RGB(90, 80, 70) Task 22 measured into near-black. The code matches the design's §7.1 table exactly, so the code is not the fault — the sentence is, in all three places, and the request's own `map-autumn` line ("the non-birch trunks go **near-black**") is the measurement that contradicts it. Fix the sentence, or raise the trunks.
- `Props.placeTree` / `Config.SPECIES` alder — `top = math.min(y + row.height - crown.Y/2, y + trunk.Y + crown.Y/2 - CROWN_MIN_OVERLAP)`. For alder `trunk.Y + crown.Y = 26 + 30 = 56 < height 57`, so the second branch always wins and the alder proxy is **54** studs tall, not 57. Claim 9's "57–71 studs tall" should read 54–71, or alder's trunk/crown should sum to ≥ 59. Design §7.1's table carries the same arithmetic gap.
- `map_contract.spec`, "is denser where the shooter looks than in the backdrop" — `backdropArea = c.SIZE_STUDS * c.SIZE_STUDS - denseArea` counts the flank fields, the benches, the rest of the corridor and the 2048² minus 2000² outside `treeArea` as backdrop, while `backdrop` counts only non-corridor trees. So "0.88 in the backdrop" and claim 3's "twenty times thicker" are inflated. The assertion itself is fine; the denominator is not.
- Design §16.1 test rows vs what was written — walkability is measured over **5** seeds, not the design's 20; the design's third walkability property ("density per 10,000 studs² never above the dense tier's rate", 10000/22² ≈ 20.7) is not asserted; the species tolerance is `× 0.4 … × 1.8` (−60 %/+80 %) against the design's **±25 %**, and every measured share (39.6/28.2/20.5/11.7 against 40/25/20/15) sits inside ±25 %; and no spec asserts §16.1's "`treeBlocks` tile the map exactly once". Loosening an Architect's number belongs in `ESCALATE.md` (loop step 2), not in a comment.
- `Scatter.luau` header — still says "It takes `reject` as an ARGUMENT rather than requiring Layout, so this file needs no `script`, no require and no Studio: a server spec drives it directly". The module now opens with `local Layout = require(script.Parent.Layout)` and `Scatter.trees` hard-wires `Layout.treeDensity` as its weight. Either pass the weight in (which `Scatter.weighted` already allows) or correct the header. Also `require(script.Parent.Layout)` without `:WaitForChild`, unlike every sibling.
- `Scatter.weighted` grid anchoring — the grid is anchored at `block.minX/minZ` and `math.floor(1000/22) = 45` leaves 10 studs unused at each block's far edge, so between the last west cell centre (−21) and the first east cell centre (+11) the spacing is **32** studs, not 22. Two thinner lines cross the whole map at x = 0 and z = 0, one of them down the middle of the drive. Relatedly, `Scatter.trees`'s comment "a block is a rectangle of the same global grid, so which trees exist does not depend on how the work was cut into steps" is false twice: the grid is anchored to the block, and `blockSeed` folds the block rectangle into the rng seed, so re-cutting the blocks changes every tree.
- `Scatter.trees` — passes both `reject` (`Props.rejectTree` = `treeDensity <= 0`) and the weight (`Layout.treeDensity`). `treeDensity` is therefore evaluated twice per candidate, and `reject` can never drop a point the weight would keep (`key >= 0` always). Drop the argument; claim 2's "one source" is already true without it.
- `Props.brush` — declares `(number, string?)` and returns `nil` for `why` on every path; `MapGen.runStep`'s `if why then` branch is dead.
- `MapGen.verifyContract` — still checks only `Map.BUDGET.parts`. Design §16.1 check 8 asks for trees ≤ `BUDGET.trees`, brush ≤ `BUDGET.brush`, hedge parts ≤ `BUDGET.hedgeParts` and **each ≥ half its budget**; `reviews/task-58/RESULT.md` flagged this and said brush was M2.8b's. The per-step ceilings in `runStep` are the upper half only.
- Screenshots vs design §18 — the M2.8b row names `map-drive` / `map-crossing` / `map-autumn` / `map-stand`; the request describes `map-crossing`, `map-post`, `map-wide`, `map-autumn`. Say which shot replaced which, or match the table (row 58a(d) already carries the `tools/mapgen.py` `SHOTS` deviation).
- Claim 1 wording — the spec asserts `expect(inCorridor > 500)` and *notes* "2,095 of 2,737"; it does not assert those two numbers.

---
REVIEWER verdict on commit `02feb6885e523b15be7678f6ff4f106f0f4c3640` (round 1) · 2026-09-26 16:56 UTC · session cost $2.98, 34 turns · written by tools/agents.py
