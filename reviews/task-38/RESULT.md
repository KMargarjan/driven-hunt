PASS

## Notes (non-blocking)
- `tests/client/weapon_client.spec.luau` / `it("listens to ONE Tool, however many the session has destroyed")` — `expect(Weapon.Input.watchedTools() <= 1)` also passes at 0, and its power still rests entirely on the session having created at least two Tools before the "the client replica" describe runs, which the comment asserts ("This session has already respawned at least once by the time the drive assigns teams") but nothing pins. `Body.place` yields in `LoadCharacter`, so `applyRefreshArming` runs after the new character exists and a placement may produce only one Tool. `expect(watchedTools()).to.equal(1)` for a shooter, plus a client-side counter of Tools ever passed to `watchTool` so the assertion can say "seen N, holding 1", would make the leak fail loudly instead of relying on timing.
- `reviews/task-38/REQUEST.md` — `Code commit: 310f7b0` names a paperwork-only commit ("Task 38: review round 1 verdict", `reviews/task-38/RESULT.md` alone, per `.agent-evidence/log.txt`). CLAUDE.md git workflow step 4 defines the code commit as the last commit that changed anything but the loop's own paperwork, which is `97d8333`. No evidence hole — `310f7b0`'s tree contains all of `97d8333`'s code and the harness ran clean on it, and naming a later commit only makes the "only paperwork after it" bound stricter — but the label is wrong.
- `src/server/Weapon/init.luau` / `runReload` abort branch — after `task.wait(remaining)` the recovery `Close` is applied to the `state` captured *before* the wait, and `setState(player, closed)` runs without re-checking `state.epoch == epoch`. A `revoke` or a fresh `grant` landing inside that window would be overwritten by the stale pre-wait state, and the trailing publish (whose epoch check would then match) would send it to the client. Unreachable today — after a successful `Break`, `Load` can only be refused `"no-reserve"` (walked past) and `Close` cannot be refused while `open` and not busy — which is what the request says too, but the epoch re-check belongs next to the only yield on this path.
- `src/client/Weapon/Input.luau` / `Input.start` — the Tool half of 24a(c) is fixed, the container half is not: `watchContainer` appends a `ChildAdded` connection to the module-wide `connections` for every new Backpack and every new character, and `connections` is cleared only by `Input.stop()`, which nothing calls. Two dead connections per respawn, the same shape of leak the task closed one level down. Worth queueing under 38a.
- `tests/client/input_driving.spec.luau` / `it("kept its own action bound for the whole run…")` — the header comment says the spec "owns exactly one action, under the reserved test prefix, and **bound no other**", but nothing asserts "no other": the body reads only `GetAllBoundActionInfo()[ACTION_NAME]`. Either scan the table for other `DrivenHunt.Test.` entries or drop that clause. (The `it` itself is now falsifiable, which was the 6a(a) ask.)
- `tests/client/weapon_client.spec.luau` / `it("has the four remotes on disk, with the right classes")` — still named and written for four remotes while this change corrected `GAME_DESIGN.md` to five; `HitMarker` is unasserted there.
- `GAME_DESIGN.md`, arena row — "the arena is the only thing this repo puts in Workspace at all" is contradicted by the next sentence, "Three more run-time folders join it during a session". The Edit-mode half ("Workspace holds only `Camera` and `Terrain`") is right and is the part round 1 asked for.
- `src/server/Match/init.luau` / `applyFreeze`, `dispatch` — claims 4 and 5 remain untested at every level, as the request states (the started-singleton wall). Rows 32a and 35b now say so rather than counting them closed, which is the right record; a seam that lets a spec watch `Match.snapshot().rows` across a drive boundary is the task that would close all of 32a(b)(c)(d)(e) and 35b(b)(c) at once.
- Rule 5: the one visible change (no readout without an equipped gun) has no "after" screenshot. The stated reason checks out — `Studio.capture` sends no `studio_id`, and `TASKS.md` row 35 records StudioMCP refusing `screen_capture` with "missing the required `studio_id`" when more than one Studio is connected — and the change is asserted by `Hud.readoutText() == ""` in the tied branch of `zz_tie_to_a_tree.spec.luau`. Stated, not skipped.

---
REVIEWER verdict on commit `3fb6324d72b5a2e4f72fe1077f285ebb38962c03` (round 2) · 2026-09-26 00:30 UTC · session cost $5.42, 56 turns · written by tools/agents.py
