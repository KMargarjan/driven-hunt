1. `src/server/Match/init.luau` / `dispatch` (claim 4, TASKS.md 32a(c)) — the drive-start broadcast still carries the previous drive's score rows. The fix clears a cache *after* the push has already gone out. Evidence: in `dispatch` the order is `state, effects = Phase.step(...)` → `snapshotCache = nil` → `applyEffects(effects)` → `if state.phase == "Assigning" then ... board = Score.new() ... snapshotCache = nil end`. `Phase.enterAssigning` appends `table.insert(effects, { kind = "broadcast" })`, `applyEffects` runs it as `broadcast(false)`, and `broadcast` does `remotes.MatchState:FireAllClients(Match.snapshot())` while `Match.snapshot()` builds `rows` from `Score.leaderboard(board)` — `board` is still the finished drive's board at that moment, because it is reset five lines later. The throttle does not save it either: `broadcast` returns early only if `at - lastBroadcast < STATE_MIN_INTERVAL` (0.25 s), and the previous push at the Scoring→Assigning boundary is the 5 s heartbeat or `enterScoring`'s broadcast, so the send almost always happens. The new `snapshotCache = nil` only prevents the stale snapshot being *re-served* from the cache, which the next tick's `snapshotCache = nil` (4 Hz, already present at the top of `dispatch`) did 0.25 s later anyway. So the observable defect — clients showing the previous drive's rows at drive start, until the next event-driven broadcast or the 5 s heartbeat — is unchanged, while `reviews/task-38/REQUEST.md` claim 4 and `TASKS.md` rows 32a and 38 record it as fixed. Required: rebuild the board before `applyEffects` runs (or re-broadcast after the board is rebuilt) so the drive-start push carries the new drive's rows, and correct the claim and the two TASKS.md rows. There is no test for this either — `grep` over `tests/` for the drive-start rows finds nothing — so a spec on `Match.snapshot()` rows across a drive boundary would be worth having, if a seam allows it.

## Notes (non-blocking)
- `tests/client/weapon_client.spec.luau`, `it("is listening to no Tool at all, and holds nothing for one that died")` — this one cannot detect the 24a(c) leak: it runs only in the driver's describe, a driver is never granted a Tool, so `watchedTools()` is 0 with the fix and 0 with `forgetTool` deleted. It is a fine driver-half assertion, but claim 3 lists it as one of "the two specs" that verify the fix; only `it("listens to ONE Tool, however many the session has destroyed")` can. That one's power also rests on the session having placed the shooter at least twice before the spec body runs, which the comment asserts but nothing pins — worth a sentence saying what makes it ≥ 2.
- `src/server/Weapon/init.luau` / `runReload` abort branch — the guard cannot help for the one refusal that could plausibly arise. `StateMachine.apply` starts with `if kind ~= "SelectAmmo" and now < state.busyUntil then return state, "busy"`, so a `"busy"` abort makes the recovery `Close` return `refused` too and the gun stays open. Also, the abort path `return`s before the trailing "one last publish once the final window has elapsed", so after a successful recovery-Close the client's snapshot still carries `busyFor = RELOAD_CLOSE` and the Hud keeps its `R` — the exact thing that closing publish exists to prevent.
- `src/server/Boar/Wound.luau` — `Wound.apply`'s inner `charge` still computes `local known = config.DAMAGE[zone] ~= nil; local key = if known then zone else FALLBACK_ZONE` instead of calling `Wound.chargedZone`. Behaviour agrees, but "One function, one answer" in the new header is not literally true; route `charge` through `chargedZone`.
- `src/server/Boar/init.luau` / `Runtime:takeHit`'s new `tally` — it has no `count <= 0` guard where `Wound.apply`'s `charge` returns early on `count <= 0`, so a zero-count entry in `payload.zones` opens a `zoneHits` bucket the damage side never opens. Same divergence, smaller.
- `GAME_DESIGN.md`, arena row — the new sentence says the arena is "the only thing BUILT FROM DISK in Workspace, and the only thing there at all in Edit mode". It is not there in Edit mode at all: `ServerScriptService.ArenaBoot` is a server script, so `Workspace.TestArena` exists only during a session, and in Edit `Workspace` holds `Camera` and `Terrain` (TASKS.md row 7, "correctly empty: since Task 22 Workspace holds only Camera and Terrain"). The three run-time folders and their owner rows are right.
- `tools/studio_mcp.py` module docstring — the new `load_scenarios` refusal is not in it. The docstring's validation sentence still reads "an unknown device or action, a missing key or button, a non-numeric moveTo or a `wait` outside StudioMCP's 0..10000 ms fails the run there and then", with no mention of a mouse button before any `moveTo`. CLAUDE.md "Run / test" makes that docstring the single source of truth for the test system.
- `docs/architecture/audit-003.md` — quotes the now-removed warning text (`Camera.start` warns "no stock PlayerModule to disable…"). Architect-owned; queue it for the next audit/regeneration rather than editing it here.
- `src/client/Weapon/Input.luau` / `forgetTool` — it disconnects the `Unequipped` hook along with the others when the Tool's ancestry goes `nil`. If `AncestryChanged` is delivered before `Unequipped`, `bound` stays `true` and the `DrivenHunt.Weapon.*` actions stay bound with no Tool in hand. Harmless today (the server derives `equipped` from `Tool.Parent` and refuses `"not-equipped"`), and it matches the pre-fix behaviour, but it is now a deliberate teardown rather than an accident — worth an `unbind()` in `forgetTool`, or a line saying why not.
- `tests/server/boar_body.spec.luau` — the getaway loop `break`s as soon as `flatGap() - gapBefore >= GAIN_STUDS` (20 studs), which at `SPRINT_SPEED` is well under 3 s, so "upright throughout the flee" is really "upright until it has gained 20 studs". Still strictly better than the old half-sprint window; the comment quoting the design's "throughout a 3 s flee" overstates it slightly.
- `tests/client/input_driving.spec.luau` — `expect(#log > 0).to.equal(true)` is introduced under the comment "it never took the keys away from the game: every handler here returns Pass". A non-empty log does not show `Pass` was returned; either assert that elsewhere or drop the sentence.
- `src/server/Match/init.luau` / `applyFreeze` (claim 5) — no test at any level for either half: neither `Match.Scored` carrying the −50 nor `Punished` firing on the no-anchor path. The 32a notes already say `Match` being a started singleton is what blocks this; naming it as still-untested under 35b(b)/(c) rather than closed would be more accurate.
- `TASKS.md` row 38 — "Nine T/D items fixed" is followed by eleven items.

---
REVIEWER verdict on commit `cbea46f6d583a70701c6aafda30135c76254947a` (round 1) · 2026-09-26 00:13 UTC · session cost $4.61, 58 turns · written by tools/agents.py
