1. `tools/studio_mcp.py` / `run_test`, the new drive-clock seam check — it crashes the harness on the one path it most needs to survive. Evidence: the check reads `reports["server"].get("seamClosed")` while `reports` is initialised empty (`reports, output = {}, ""`) and is only filled when a report actually arrives; the loop immediately above it guards exactly that case — `report = reports.get(side)` … `if not check(f"[{side}] runner reported within {REPORT_WINDOW} s", report is not None): continue`. When the server report does not arrive inside `REPORT_WINDOW` (the failure the window was widened twice for, Tasks 34 and 41), line 1345 raises `KeyError: 'server'` before `verdict()` runs, so the run ends in a traceback with no `[harness] FAIL: n/m checks @ <sha>` line and no "HEAD unchanged during the run" check — after an eight-minute Studio run. `run_test2`'s equivalent uses the safe form (`server_report = reports.get("server")`), and this is the same class as the already-queued harness fault in TASKS.md row 41a(a). Required: read it defensively, e.g. `(reports.get("server") or {}).get("seamClosed") is True`, so a missing report fails the check instead of aborting the run.

## Notes (non-blocking)
- `tests/TestKit.luau` / `finish()` — `report.seamClosed = activeToken == nil` sits on the line immediately after `activeToken = nil`, in the same scope, so in any committed state it is the constant `true`. It bites only against deletion of the line above it (which is what the dirty-tree demonstration shows); it would not notice anything that re-sets the token after `finish`, and it is the same shape as the note in `reviews/task-41/RESULT.md` about two counters incremented on consecutive lines. Computing it in `tests/TestRunner.server.luau` after `TestKit.run` returns (`report.seamClosed = TestKit.activeToken() == nil`) observes the module from outside `finish`, costs the same, and keeps the require-cache property the measurement in claim 8 established.
- `tools/studio_mcp.py` docstring — CLAUDE.md says it is "the single source of truth" for "gate, runners, report format, every check" and must be updated with any change to the test system. The `Report (JSON): side, token, placeId, specs …, message` line does not list `seamClosed`, and check list item "8. Stop. The token is cleared and the gate is seen closed in Studio." does not mention the new seam check, so the documented count is still 27.
- `TASKS.md` — there is no row 48 and no row 48a; the last rows are 47, 47a, 47b, 47c. The request's claim 11 rests its decision not to fix the `watchedTools()` flake on "Row 48a carries it", and the request's other queued items (the `test2` seam check) name 48a too. Add the rows; the task's status row and its note row are paperwork the merge gate allows after the code commit.
- `src/client/Weapon/Input.luau` / `watchTool`, `forgetTool` — claim 11's diagnosis checks out against the code: `watchTool` is called synchronously from `Backpack.ChildAdded`, while the dead Tool is dropped only from `tool.AncestryChanged` with `parent == nil`, so a transient count of 2 across a respawn is reachable and is not a leak. Queue the retry-based fix as described; nothing here needed changing this round.
- `docs/design/map-generator.md` (Architect-owned) — line 273 still declares `MapGen.digest(): { …, seed: number, … }` against the new `seed: number?`, and section 9's settings step still describes `Settings.apply(Map.STREAMING)` as applying, not refusing. For the next audit, alongside rows 45a(g) and 47a(f).
- `src/serverstorage/MapGen/init.luau` / `MapGen.verifyContract` — the `root == nil` early return now omits a fourth field (`seed`) against its declared return type, on top of `streaming`, `markerDigest` and `terrainCells`. Rows 45a(c) and 47a(d) get one field worse. Also `seed` in the full return has no reader: `mapgen.py`'s `command_contract` prints `counts`, `streaming`, `markerDigest` and `findings`, never `seed`.
- `reviews/task-48/REQUEST.md` — claim 10 says "three `MapGen` files"; `changed-files.txt` shows two (`MapGen/Settings.luau`, `MapGen/init.luau`). The file also carries 11 claims against the "at most 10" cap, numbered 1–9, 11, 10.

---
REVIEWER verdict on commit `0609c42155bc6323e6d0ad814d74e23b2c333326` (round 1) · 2026-09-26 05:53 UTC · session cost $3.11, 44 turns · written by tools/agents.py
