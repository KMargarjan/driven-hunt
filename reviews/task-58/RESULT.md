PASS

## Notes (non-blocking)
- `Layout.hedgeSegments` / `tests/server/map_contract.spec.luau` — round 1's finding 1 is fixed in code, but the only assertion holding it closed is `MapGen.verifyContract`'s blocker walk, which the harness never runs (`MapGen` runs in Edit, driven by `tools/mapgen.py verify`). `hedgeSegments` is pure: delete `if Layout.onBench(x, z, config, config.HEDGE.segmentStuds / 2) then blocked = true end` and every harness spec still passes — "stays inside the design's hedge part budget" only asserts `segments <= 500` and the count rises to ~272. Queue the pure half: no segment of any line within `halfWidth + verge` of a bench centreline.
- `Props.rejectTree` / spec `it("puts no tree in the corridor, on a track, in the bog or in a hedge")` — the new `or Layout.onBench(x, z, config)` clause was not added to that spec's four checks, so the tree half of finding 1's class is also unasserted in the harness.
- Design §16.1 `Layout` pure-spec row, in M2.8a's scope per §18 — two items nothing asserts: "the road's span covers every post" and "`inField` and the benches do not overlap". Both hold today (posts reach ±560 inside `ROAD.from/to` = ±900; `fields.innerX = 760` against `ASSEMBLY.to = 700`), so nothing is wrong now; queue them so a `postSpacing` or `fields` change cannot break them silently.
- `MapGen.verifyContract` blocker walk — it tests `part.Position` only: `local dz = math.abs(part.Position.Z - Map.ROAD.z)`. A z-axis hedge segment is laid as `(3, 7, 24)`, so a centre 33–44 studs off the centreline still overlaps the verge and would not be reported. The placer's half-segment clearance covers that today, which is why the live build is `contract OK`, but the check is weaker than claim 1's headline "Nothing collidable stands on the road". Compare extents, not centres.
- `MapGen.verifyContract`, the `driverStart` loop — `(start.CFrame * CFrame.new(corner)).Position.Y` for an unrotated 1-stud-thick part is the bottom face at all four corners, so the corners can never disagree with the centre. It is what design §16.1 check 4b asks for and it does catch a rotated or mis-sized strip, but it does not measure the ground under the 1,120-stud strip, which is what the request's "its centre said nothing about the strip eight drivers actually stand on" implies.
- `reviews/task-58/REQUEST.md` claim 7 — "The comment that claimed the contract already did this is gone" is not true: `Layout.markers`' post comment still ends "the bottom face sits exactly on the road bench, **which is the one number the contract checks**". Round 1's requirement is satisfied anyway, because the contract now does check it — so the code needs nothing and only the claim is wrong. Two small follow-ons: it is now two numbers (bottom face and `|z − Map.ROAD.z|`), and claim 9 cites `Ground.paletteMaterials(config)` where the code has a file-local `paletteMaterials`, not a `Ground.` member.
- `Digest.luau` — the doc line "The whole digest: 64 hex characters, the marker half first." now sits directly above `Digest.paletteText`, with `Digest.of` below it. That is exactly the misplacement round 1 noted in `Ground.luau` and this round fixed there.
- `Ground.luau` — `applyPalette` and `readPalette` reach terrain through the lowercase `workspace` global; every other function in the file uses the `local Workspace = game:GetService("Workspace")` at the top.
- `Map.BUDGET.hedgeParts = 400` and `Map.BUDGET.brush = 600` are read by nothing, while `it("stays inside the design's hedge part budget")` asserts a hard-coded `expect(segments <= 500)` and `Config`'s comment claims "inside `Map.BUDGET.hedgeParts = 400`". Three copies of one ceiling, none agreeing — the same class as finding 2, at lower stakes. Design §16.1 check 8 wants the budget check with a `>= half` lower bound (brush is M2.8b).
- `Layout.benches(config)` is added and called by nothing: `Height`, `Ground` and the specs all read `config.WORLD.benches` directly. Either route the readers through it or drop it.
- `Layout.onRoad` returns true on any bench's gravel, the assembly track included; `Ground.surfaceMaterial` relies on that. The name says road.
- `it("leaves the terrain palette at the contract's colours for the world it names")` cannot distinguish a correct `Map.PALETTE_DEFAULT` from an arbitrary one: after any `MapGen.clear`, `resetPalette` has written exactly that table into the engine, so the comparison is engine-against-what-we-wrote. It does catch the case it exists for (an autumn palette surviving a teardown). Worth one line in the spec saying which of the two it proves.

---
REVIEWER verdict on commit `92d9d511458e2c3b0f6011cca08a9f722ff50b84` (round 2) · 2026-09-26 13:42 UTC · session cost $4.41, 40 turns · written by tools/agents.py
