1. `Config.WORLD.hedgeX` / `Layout.hedgeSegments` / `Props.hedge` — an ungated, collidable hedge wall stands across the forest road at x = ±760. Evidence: `hedgeX = { -760, 760 }` with `hedgeHalfSpan = 1024`; `Layout.hedgeLines` sets `crossesCorridor = x >= corridor.minX and x <= corridor.maxX` → false (corridor is ±620), so `MapGen.runStep`'s hedgerow branch gets `from, to = Layout.corridorSpan(line, Config)` = nil and `gates = {}`. `Layout.hedgeSegments` then emits a segment every `HEDGE.segmentStuds = 24` from `line.from = -1024`, which includes z = −712 and z = −688; `Props.hedge` lays each as `Vector3.new(size.Z, size.Y, size.X)` = (3, 7, 24) along z, through `part()`, which sets no `CanCollide` (Part default true). The road bench is `{ at = -700, halfWidth = 8, from = -900, to = 900 }`, gravel z ∈ [−708, −692] — fully covered by those two segments at both x = −760 and x = +760. Design §6.2 says the suppression exists "so the road runs off the map flat"; a 7-stud wall across it is the opposite, and it sits inside `map-edge`'s frame ((600, 8, −700) → (1024, 6, −700)), which the request describes as "the road runs out flat and the rim closes the horizon with ground and trees" with no mention of it. Required: reject or gate hedge segments where a line crosses a bench, and assert the road is clear along its whole `from`→`to`.

2. `src/shared/Map/init.luau` `Map.ROAD` / `Map.ASSEMBLY` — added to the contract and read by nothing; the bench geometry now lives in two places with no check that they agree. Evidence: `Map.ROAD = { z = -700, halfWidth = 8, verge = 24, from = -900, to = 900 }` and `Map.ASSEMBLY = { z = 700, halfWidth = 5, verge = 20, from = -700, to = 700 }`; a grep of every `*.luau` finds no reader of either. `Config.WORLD.benches` re-declares the same five numbers per bench, and its own comment says "Map.ASSEMBLY flattens the strip it stands on" — it does not; `Config.WORLD.benches[2]` does. Two lines above, `Config.SPAWN_PAD = Map.SPAWN_PAD` carries the repo's own rule: "READ FROM THE CONTRACT, not copied: … have to be the same number or the map fails its own check". Design §2.2's ownership table lists `Map.ROAD`, `Map.ASSEMBLY` under what the generator **reads**. This is the duplication claim 7 says it is closing for `MapGen.VERSION`, reopened for the road. Required: derive `Config.WORLD.benches` from `Map.ROAD`/`Map.ASSEMBLY`, or assert equality in a spec.

3. The palette — the whole feature is untested; no spec exercises any of it. Evidence: the three digest specs call `MapGen.Digest.of({ entry("A", 0), entry("B", 10) }, samples, 100)` with **three** arguments, so `Digest.paletteText(nil)` returns `""` and the new fourth-argument branch is never run. No spec calls `Ground.readPalette`, `Ground.applyPalette`, `Ground.resetPalette` or `Terrain:GetMaterialColor`, and none asserts `MapGen.VERSION == Map.GENERATOR`. Design §16.1 names all three as this task's: check 5 ("`Terrain:GetMaterialColor` for the four palette materials equals `Map.PALETTE_DEFAULT` in the arena branch"), check 10 ("`MapGen.VERSION == Map.GENERATOR`"), and the pure-spec row "`Digest` | … **changes when a palette colour changes** (§8.4)". §18's M2.8a scope row ends "the specs above". Request claim 5, "A hand-edited colour changes the digest", is backed by nothing the harness runs. Required: the three assertions.

4. `Layout.markers` / `MapGen.verifyContract` — nothing asserts a shooter post stands on the road at ground, and a code comment says it does. Evidence: `Layout.markers`'s post comment ends "the bottom face sits exactly on the road bench, **which is the one number the contract checks**". `MapGen.verifyContract`'s tolerance loop is `for _, spawn in ipairs(taggedInside(root, Map.TAGS.boarSpawn))` and nothing else — no `ShooterPost`, no `DriverStart`, no z-on-road test. Design §6.5 and §16.1 check 4b/4c: "every `ShooterPost`'s **bottom face** (`Position.Y - Size.Y/2`) and the `DriverStart` part's four corners likewise; **4c NEW** every post's `|z − Map.ROAD.z| <= Map.ROAD.halfWidth`". The 9.5-stud drop (row 43a(l), audit-004 F9) is claimed closed "by construction" with no assertion holding it closed. Required: add 4b and 4c, and fix or remove the comment's claim.

5. `map_contract.spec.luau`, `it("stays inside the relief band, and flatter inside the drive corridor")` — the assertion is no longer true of the map and survives only because its lattice stops short of the edge. Evidence: it samples `for x = -1000, 1000, 50` / `for z = -1000, 1000, 50` under the comment "THE WHOLE MAP, not just the middle", and asserts `expect(worst <= c.RELIEF)` with `RELIEF = 40`. `Height.at` now returns `math.max(shaped, Height.edgeFloor(x, z, config))`, and `Height.edgeFloor` returns `config.GROUND_Y + config.EDGE_MAX_Y * (1 - smoothstep(toEdge / config.EDGE_BAND))` = `GROUND_Y + 44` at `toEdge = 0`, i.e. at |x| or |z| = `SIZE_STUDS / 2` = 1024, where terrain is written. At the lattice's outermost ring `toEdge = 24`, giving 44 × (1 − smoothstep(0.24)) = 37.6 — 2.4 studs under the bound by sampling luck. Design §16.1's `Height` row asks for "`edgeFloor` never exceeds `EDGE_MAX_Y`", which is absent. Required: bound the whole-map assertion against `EDGE_MAX_Y` (or sample to ±1024), and add the `edgeFloor` check.

## Notes (non-blocking)

- `tools/mapgen.py` `SHOTS` — the comment says "The design's section 16.3 list", and it is not: `map-bog` (design row 7) is gone, `map-stand` is not in the design's eight, and the cameras for `map-crossing` ((240, 6, −700) → (−240, 6, −700) vs the design's (0, 4, −540) → (0, 3, −780)), `map-drive` and `map-autumn` all differ. Either match the table or record the deviation.
- `MapGen.reachability` — design §16.4 says "`Reach` gains a `to` field"; the code concatenates `from.name .. "->" .. to.name` into `from` and leaves the type at three fields.
- `Props.rejectTree` — rejects `inCorridor`, `onTrack`, bog and hedge clearance, but not a bench. The road runs to x = ±900 while the corridor stops at ±620, so trees can be scattered on the gravel and its verge for |x| ∈ (620, 900]; at `treeCount = 400` over the 2000×2000 `treeArea` that is roughly one trunk in the road today and ~7 at M2.8b's ~2,925. The design assigns the `rejectTree` rewrite to M2.8b — queue it there explicitly, with the bench case named.
- `Config.WORLD.tracks` — unchanged, and the widened corridor made one stale: `{ axis = "z", at = 430 }` now runs the length of the drive **inside** the corridor (±620) while its comment still reads "along the map, outside the corridor". Design §7.6 replaces both v2 tracks with one flank track at x = 880, half-width 5.
- `Ground.PALETTE_MATERIAL` — a second copy of the role → material mapping that `Config.MATERIAL` already holds, with a different key for the same material (`road` vs `track`). The inversion the screenshot caught is exactly this class of duplicate; derive one from the other.
- `Ground.luau` — the new "the palette" section comment was inserted between the "HOW MUCH TERRAIN EXISTS" doc block and `Ground.cells`, so that function's documentation now reads as the palette section's preamble.
- `tests/client/map_client.spec.luau` does not exist in the repo. The request's "they **replicate to a client**, so `map_client.spec`'s colour check stands rather than being dropped" names a file that is not there; design §16.2 also describes it as existing. Worth one line in `TASKS.md` so N1's "yes" actually lands somewhere.
- Hedge network 5 lines → 3 is listed under **M2.8b** in design §18's build-order table, not M2.8a (rule 4).
- `it("raises the map's rim but leaves the road's band flat where it runs off")` — the `atRim` assertion is seed-dependent, not structural: at x = 1000 the bench taper has ended (`outside = 100 > taper = 60`) and `benchBand` is back to 1, so the sample is `math.max(noise × RELIEF, GROUND_Y)` and reads 0 only because the noise is negative there at `SEED`. It does catch the regression it exists for; it will not survive a seed change.
- `Height.edgeFloor` — the suppression takes `across` over **all** benches, so the rim is also flattened at (±1000, +700), where the assembly bench never reaches (it ends at x = ±700). A notch in the rim nobody chose.
- Design §6.5 asks for "200 sampled points on each bench centreline"; the new spec walks at 10-stud steps (181 and 141).

---
REVIEWER verdict on commit `33fdea9b739452b9183efe551887c355d4b86fa2` (round 1) · 2026-09-26 13:02 UTC · session cost $4.56, 41 turns · written by tools/agents.py
