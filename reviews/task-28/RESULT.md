1. `reviews/task-28/REQUEST.md` / `src/client/Hud/init.luau` `build`, `src/server/Boar/Body.luau` `buildZones` — two visual changes shipped with no screenshot description and no statement that they could not be captured (rule 5, rule 8). Evidence: the Hud gains a brand-new drawn element — `mark.Name = "HitMark"` with four `diagonal.Rotation = corner.rotation` arms, `HIT_MARK_ARM_PX = 9`, `HIT_MARK_COLOR`, `HIT_MARK_KILL_COLOR = Color3.fromRGB(235, 70, 55)` — and the boar's silhouette and colours change: `zonePart.Color = if zones.ZONE_TINT then spec.tint else config.BODY_COLOR` with tints `RGB(206,120,120)` / `RGB(198,150,150)` / `RGB(150,128,104)` against `BODY_COLOR = RGB(198,158,110)`, plus a head standing 1.15 studs proud of the front face. The request describes exactly one screenshot — claim 6's carcass, "left a dead boar **standing to attention**" — and its "What I could not verify" section names only the flash ("The flash could not be photographed… the capture was not attempted rather than implied"). Nothing about the marker on screen, nothing about the tinted boar. The design required both: §12.6 shot 1 is "the `ZONE_TINT` decision on screen" and §11.1 marks the tints and `FLASH_COLOR` "**Check on screen**" — citing this repo's own precedent, where `BODY_COLOR` (Task 22) and `HANDLE_COLOR` (Task 24) each measured correct and read near-black in Play. Claim 8 proves the marker with specs only; a spec cannot see that it is legible, centred, or the right size. Required: capture and inspect the boar idling (side and front, showing the head and the three tints against the plate) and the marker on screen for both a hit and a kill, and describe in the request what is actually on screen; or, if a 0.15 s / 0.45 s marker cannot be caught by a ~1 s capture round trip, say so plainly as was done for the flash and describe what was inspected instead.

## Notes (non-blocking)
- `tests/server/boar_zones.spec.luau` comment "Nine directions, every one of them a real ray", `src/server/Boar/Body.luau` header "fires nine real rays from outside", and `docs/research/2026-09-25-hit-zones.md` §2B/§5 "fires nine real rays" — the spec fires twelve (4 head, 3 chest, 2 legs, 3 body), as the request correctly says. Fix the three counts.
- `src/server/Weapon/init.luau` header — "the owner of … the **four** RemoteEvents"; there are now five (`HitMarker` added to `Remotes.model.json`).
- `TASKS.md` — no row for Task 28. The Builder owns its current task's status row (`CLAUDE.md`, Roles); rows 26 and `PLAYTEST.md` were updated in this branch but 28 was not added.
- `tests/server/boar_hit.spec.luau`, "reacts inside a tick" — asserts `reaction < 0.2`, which is exactly `SENSE_INTERVAL`; a sense-gated reaction of 0.15 s would still pass. The tight one-tick bound is only in `boar_brain.spec`. Tightening the live bound (or asserting against a measured tick budget) would make the live spec defend the design's number too.
- `tests/server/boar_wound.spec.luau`, "the numbers this spec asserts / are the numbers production actually ships" — compares `LETHAL`, `MORTAL`, `DAMAGE`, `FLIGHT`, `BLEED_OUT_MAX_SECONDS`, `LIMP_SCALE`, `WOUND_SPEED_END`, but not `WOUND_SPEED_START`, `BOLT_SECONDS`, `BOLT_KICK` or `CARCASS_SECONDS`, which the spec table also carries. Those four can drift silently.
- `src/server/Boar/init.luau` `Runtime:step` — `Wound.advance` runs for a `DOWN` boar too, so a carcass allocates and re-freezes a new `WoundState` 60×/s for the whole `CARCASS_SECONDS = 120`. Harmless, but `advance` (or the caller) could return early once the boar is down.
- `docs/design/hit-zones.md` §12.5 asked for a `fire-at-boar` scenario appended to `tests/client/input_scenarios.txt`; it was not added and the request does not record the deviation. It is a near-duplicate of the existing `weapon-fire-reload-ammo` scenario and the design itself says it "asserts the weapon's own state changed (Task 24's assertions), **not** that a boar was hit", so nothing is lost — but every other deviation from the design in this task was declared, and this one was not. Queue or declare it.
- `Wound.killRecord`'s `flightStuds` field carries `state.flightRun` (distance actually run), while `WoundState.flightStuds` means the target distance. The code matches design §8.1's wording ("how far it ran after the mortal wound"), so it is the design's name that is wrong — worth fixing at the next regeneration before 1.7 consumes the record.
- `tests/client/hit_marker.spec.luau`, "reaches this client, from the server, over the real remote" — the 30 s wait for a marker sent by `weapon_hits.spec` in the same Play session is a genuine cross-report ordering dependency, as the request says. If it ever flakes, a server-side marker fired from the client spec's own Play session setup (rather than from another report's spec) would remove the coupling without weakening the assertion.
- `stats().downed` counts deaths and `stats().killed` counts carcasses removed, which satisfies design §11.3's "`downed == killed`" only after the carcass expires. The request flags this as an interpretation; it is a reasonable one and belongs in 1.7's design, not another round here.

---
REVIEWER verdict on commit `d72ce0cf9f17192004914126b9d534f7cec42f98` (round 1) · 2026-09-25 11:30 UTC · session cost $4.22, 38 turns · written by tools/agents.py
