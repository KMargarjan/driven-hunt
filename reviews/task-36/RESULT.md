PASS

## Notes (non-blocking)
- `tools/studio_mcp.py`, `run_test2` — the inline comment above `write_token("")` still reads "can replay input into the shooter -- and every input-driven client spec counts its own 25 s from where TestEZ reaches it". Both clients get the replay and `ARRIVE_TIMEOUT` is 45. This was round 2's note 2, and `REQUEST.md`'s "Round 2's non-blocking notes, all addressed" says it "names the real budget (`ARRIVE_TIMEOUT` 45 s, both clients)" — the two docstring copies (lines carrying "input_driving's ARRIVE_TIMEOUT is 45 s" and "ARRIVE_TIMEOUT (45 s)") were updated, this third copy was not. Queue it; the docstring is the source of truth, so this is a stale duplicate.
- `tests/client/camera_client.spec.luau`, "is bound to a gun: a shooter has the aim action, a driver has none at all" — the driver branch does `task.wait(5)` and then `expect(Camera.stats().aimEnters).to.equal(before.aimEnters)`, under a comment claiming "this really is 'the right mouse button arrived and did nothing'". Nothing makes the replayed MouseButton2 land inside those 5 s, so that sentence claims more than the code. The `DrivenHunt.Weapon.` prefix loop is the assertion carrying the rule and it needs no wait. Gating on an observed replay marker would make the comment true.
- `tests/client/weapon_client.spec.luau` — the same shape twice: "is never given a Tool, anywhere, at any point in the session" and the driver branch of "arrives, and is frozen all the way down" are `task.wait(5)` plus a point-in-time check. The names say "at any point in the session" and "arrives"; the branches assert a single later sample. Renaming, or sampling across the replay, would close the gap.
- `tests/server/boar_body.spec.luau`, "sprints away from a threat, and stays upright" — `upright` is now sampled only inside `stepUntil`, which returns as soon as the half-sprint speed is reached (a handful of frames after `FLEE`). `docs/design/boar-ai.md` §11 says "upright: `part.CFrame.UpVector.Y > 0.9` throughout a 3 s flee" and item 6 says "at every sample through the flee". The 1800-frame getaway loop directly below already walks the whole flee frame by frame and could fold `UpVector.Y` into it for nothing.
- `tests/client/zz_tie_to_a_tree.spec.luau` — `onCue` now returns early on `Role.isDriver()`, while `cued()` branches on `Match.myTeam() ~= "Shooters"`. Two sources answer the same question: if `Player.Team` were nil when `Role` resolved on a driver's client, `onCue` would fire the shot *and* `expect(shooter).to.equal(Match.myTeam() == "Shooters")` would still pass. One source for the role would remove the disagreement.
- `tests/client/hit_marker.spec.luau` — the driver branch asserts only `expect(Shotgun.remotes().HitMarker:IsA("RemoteEvent")).to.equal(true)` plus a conditional shape check, so a `FireClient` that never reaches a second client would not redden the run. That is the right containment for a start-order race, and row 36a is the right home — the row should say plainly that the *arrival* claim for a non-shooter client is currently unasserted, not only that the race is unfixed.
- `tests/client/InputReady.luau` header — "the harness's ... check fails after 20 s"; `replay_input` now waits 60 s, per client.
- `TASKS.md` row 36 — "the 2-player line is 30 checks rather than 26". The before-figure is 28 (claim 7, and round 1's note already corrected this figure). Prose only.
- `Code commit: 0ea2ae0` is a paperwork-only commit: `log.txt` shows it touches `reviews/task-36/RESULT.md` alone, so by CLAUDE.md git-workflow step 4's definition the code commit is `ea9435b`. Harmless here — the two trees are identical under `src/`, `tests/` and `tools/`, the harness lines name `0ea2ae0` with "(clean tree)", and `paperwork-after-code-commit.txt` is OK — but the label and the definition differ, and the Director merging on that rule should know it.
- Round 3 against the Director's dispatched "max 2 rounds". The Builder disclosed it, `CLAUDE.md`'s cap is 3, and the extra round was spent on a test that would have passed with `Validator.checkRequest` deleted — that was the right call, but it is the Director's to ratify.

---
REVIEWER verdict on commit `866999171750156a9b021eb1bbbb32180f277e1c` (round 3) · 2026-09-25 23:46 UTC · session cost $3.32, 33 turns · written by tools/agents.py
