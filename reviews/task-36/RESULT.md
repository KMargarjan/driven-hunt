1. `tests/client/weapon_client.spec.luau` / "the exploit path" → `it("ignores every request a client must not be able to make")` — the rewrite removes the only assertions that detected the server acting on a reducer primitive, and the replacements are satisfied by exactly the exploit the test exists for. Evidence: the spec still sends `remotes.ActionRequest:FireServer(bad)` for `{ "Break", "Load", "Close", "Fire", "", 42 }`, but the assertions `expect(after.open).to.equal(before.open)`, `expect(after.barrels[1]).to.equal(before.barrels[1])`, `expect(after.barrels[2])...`, `expect(after.loaded[1])...` are gone, replaced by `after.reserve.Slug <= before.reserve.Slug`, `after.reserve.Buck <= before.reserve.Buck`, `after.ammo == "Slug" or after.ammo == "Buck"`, `#after.barrels == #before.barrels` and `live(after) - live(before) <= shells(before) - shells(after)`. `src/server/Weapon/init.luau`'s action handler has exactly one special case after `local ok = Validator.checkRequest(kind, ammo)` (`if kind == "SelectAmmo" then ... end`); every other kind falls through to `task.spawn(runReload, player, epoch)`. So with the whitelist gone — the defect `src/server/Weapon/Validator.luau`'s own header names, "checkRequest is the whitelist that makes the reducer's primitives unreachable from a client (design section 2.1 defect d)" — a client-sent `"Break"` runs the full `Break -> Load -> Load -> Close` cycle (`RELOAD_BREAK = 0.5`, `RELOAD_SHELL = 0.45` twice, `RELOAD_CLOSE = 0.6`). By the spec's sample point, `task.wait(1)`, both `Load` steps have run at t≈0.95 s: reserves are back to their starting values, both barrels are `Live` again, `#barrels` and `ammo` are unchanged, and `live(after) - live(before) = 0 <= 0`. Every new assertion holds; the old `expect(after.open).to.equal(before.open)` was red until t≈1.4 s. Whether the sample lands before or after that second `Load` is now a 50 ms timing question, which is the wall-clock race this task set out to remove — and REQUEST claim 9's "It now asserts what an exploit would actually break and a legitimate reload cannot" is wrong in the one case that matters: a forced reload cycle *is* the exploit. Required: an assertion that fails when the server acts on a reducer primitive. The cheap shape: wait for a quiet gun (`busyFor == 0` and `not open`) before opening the window, then keep `open` and the per-barrel comparisons — the flakiness the rewrite was answering came from sampling into an in-flight legitimate reload, not from the assertions themselves.

## Notes (non-blocking)
- `reviews/task-36/REQUEST.md` claim 7 — "it was 26 checks before" is wrong. `TASKS.md` rows 34 and 35 both record `[harness2] PASS: 28/28`, and this diff adds exactly two checks (`[driver] status PASS` and `[driver] > 0 passed, 0 failed...`), so 28 → 30. The 30/30 line itself is consistent.
- `tools/studio_mcp.py` docstring — stale after this change, and CLAUDE.md makes it the single source of truth for the test system. `test2` step 3 still reads "input_driving.spec gives the replay 25 s to arrive" (`ARRIVE_TIMEOUT` is 45 now); the "Two players" overview still says "can replay input into the shooter, and every input-driven client spec counts its own 25 s"; and the comment above `REPORT_WINDOW_2P`'s `pending` dict still says "the driver's weapon specs spend their timeouts failing".
- `tests/client/zz_tie_to_a_tree.spec.luau` — two comments the task falsified: the header's "A DRIVER's client (test2, printed as an observation)", and `cued()`'s "THE REPLAY ONLY EVER REACHES THE SHOOTER (tools/studio_mcp.py: a driver carries no gun)". There is a behavioural consequence worth queueing: the `T` cue now does reach the driver, so `onCue` sets `cuedAt` on his client and the `aiming` `RenderStepped` connection then calls `Camera.lookAt(me.Position + Vector3.new(0, 0, DOWN_THE_DRIVE))` on him every frame for `TIE_TIMEOUT` (25 s), concurrently with `camera_client.spec`'s driver assertions. Nothing asserts against it today; it is an unowned camera write in a spec whose header says it "touches the camera only inside its own scenario".
- `tests/client/shoot_boar.spec.luau`, "staged nobody on this client, because a driver has no gun to fire" — the shooter branch is `expect(Role.isDriver()).to.equal(false)` then `return`, and `Role.isShooter()` is defined as `not Role.isDriver()`, so for a shooter this `it` cannot fail. Same shape in `camera_client.spec`'s "cannot be aimed at all by a driver, who has no gun to aim" (bare `return` for a shooter). The role split is the right call; the empty halves would read better folded into their sibling `it` or named so the report shows which half ran.
- `tests/client/hit_marker.spec.luau` — claim 8 says to verify "the header", but the header's numbered point 1 still states "THE WIRE ... a marker sent by the SERVER arrives here and draws ... this spec waits for it" with no mention that only the shooter now asserts arrival. The caveat exists only inside the first `it`.
- `tests/server/boar_body.spec.luau`, "sprints away from a threat, and stays upright" — the request's "a 30-second backstop that can only fire if the body does not move at all" overstates the code. The loop also exits without `gained` when `travelled >= TRAVEL_BUDGET` (60 studs), i.e. for a boar that moved a lot and did not flee — which is the real assertion, not a backstop — and the `for _ = 1, 1800` bound is frames, not seconds.
- `tools/studio_mcp.py`, `replay_input` — `sent += len(payload)` is inside `for target in targets`, so the `[input] replayed every step of N scenario(s)` detail line reports twice the number of scenario steps in `test2` ("`N` steps sent to 2 client(s)").
- `TASKS.md` row 35 says "271 server ... `it` blocks"; the repo has 263 server `it` blocks today and this task removed none, which is also the number the request reports as passing. Row 35's figure looks wrong rather than this one.

---
REVIEWER verdict on commit `108f0ab356b67dfbfe7555a7af152b32b75b6716` (round 1) · 2026-09-25 23:03 UTC · session cost $3.50, 40 turns · written by tools/agents.py
