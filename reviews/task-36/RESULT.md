1. `tests/client/weapon_client.spec.luau` / `describe("the exploit path")` → `it("ignores every request a client must not be able to make")` — the strict assertions are back, but the new window is opened in a gun state where the exploit is a no-op regardless of the whitelist, so the test still passes with `Validator.checkRequest` deleted. Evidence: the readiness predicate is `waitUntil(90, function() return Weapon.get() ~= nil and not stillBusy() and os.clock() - lastWeaponInput > QUIET end)` — it deliberately does not require `not open` or `equipped`, and the spec's own comment says why: "`not state.open` is never satisfied at all: the replay's last scenario leaves the gun broken open and nothing presses anything afterwards", repeated as a measurement in commit `c8601a3` ("Measured on the next one-player run … a readiness condition that included `not open` was never satisfied and the test failed on its own precondition"). In `src/server/Weapon/init.luau`, `onActionRequest` reaches the reload spawn only past `if not state.equipped or state.open or now < state.busyUntil then publish(player, state) return end` (and the `SelectAmmo` branch carries the identical guard, and `if not state then return end` above both). So on the gun the spec measured — `open == true`, or a revoked/`nil` state behind a stale replica — a forged `"Break"`, `"Load"`, `"Close"`, `"Fire"`, `""` or `42` is refused by the open/absent-state guard, not by the whitelist: `after.open`, both `reserve` fields, `ammo`, both `barrels` and both `loaded` are all unchanged, `stillBusy()` is false, `opened = true`, and the `it` is green with `Validator.checkRequest` removed. That is the same defect as round 1's finding 1 arriving by a different route, and it makes REQUEST claim 1 ("the exploit path detects the server acting on a reducer primitive again") false. Required: open the window only on a gun the exploit could actually move — `equipped`, `not open`, and not busy — and fail loudly (as `expect(opened).to.equal(true)` already does) when no such window occurs. If the replay really does leave the gun broken open for the rest of the session, the spec must first bring it back to a closed, loaded state through a request a client is *allowed* to make (`ActionRequest:FireServer("Reload")` is whitelisted) and wait for that cycle to finish before the quiet window starts.

## Notes (non-blocking)
- `tests/client/hit_marker.spec.luau` — the `it` is still named "reaches this client, from the server, over the real remote", but the driver branch asserts only `expect(Shotgun.remotes().HitMarker:IsA("RemoteEvent")).to.equal(true)` plus a conditional shape check. Round 1's note asked for halves a report can tell apart; the header now carries the caveat but the `it` name is still the shooter's claim. Naming the driver's half (or splitting the `it`) would make the report say which ran.
- `tools/studio_mcp.py`, `run_test2` (the comment above `write_token("")`) — still reads "can replay input into the shooter … every input-driven client spec counts its own 25 s from where TestEZ reaches it". Both clients get the replay now and `ARRIVE_TIMEOUT` is 45. The module docstring was updated; this inline one was missed.
- `tests/server/boar_body.spec.luau`, "sprints away from a threat, and stays upright" — `expect(frames <= 120)` is 2.0 s at 60 Hz against `docs/design/boar-ai.md` §11's "within 1.0 s of first sensing". The request states the doubling, but the effective budget for the design's number is now twice the design's. A tighter frame bound, or the design amended to say what the tolerance is, would keep the two in step.
- `tests/client/camera_client.spec.luau`, "is bound to a gun: a shooter has the aim action, a driver has none at all" — the shooter branch's first line `expect(Role.isDriver()).to.equal(false)` cannot fail, since `Role.isShooter()` is defined as `not Role.isDriver()`. The line after it (`player.Team.Name == "Shooters"`) is the one doing the work; the tautology only pads the branch. Same shape in `shoot_boar.spec`'s "staged this client only if it is the shooter's".
- `tests/client/weapon_client.spec.luau`, the exploit `it` — three attempts at `waitUntil(90, …)` is up to ~273 s inside one `it`, against `REPORT_WINDOW_2P = 420`. A gun that never goes quiet now costs most of the two-player report budget before it reports the failure.

---
REVIEWER verdict on commit `22880199203311fae5dbdeb232c6825916fcecd8` (round 2) · 2026-09-25 23:28 UTC · session cost $2.87, 29 turns · written by tools/agents.py
