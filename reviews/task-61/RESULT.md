1. `reviews/task-61/REQUEST.md` — "What the screenshots show", bullet `sounder-drive-6` — the stated reading of the screenshot is one the code cannot produce. Evidence: the request says "the HUD reads `DRIVE 1 · 6:04 · BOARS 6 · SHOOTER`: the drive is running with the flag on **and has released its whole budget**". The HUD's BOARS field is not the released count: `src/client/Hud/init.luau` (the `Running` branch) formats `string.format("BOARS %d", snapshot.boarsLeft)`, and `Match.snapshot` sets `boarsLeft = math.max(0, Match.CONFIG.BOARS_PER_DRIVE - state.boarsAccounted)` — killed plus escaped, not released. `BOARS 6` is what every drive shows from 0:00 onward until something is killed or escapes, with zero boars released and with the flag OFF; the same request reads `BOARS 6` again in `sounder-field-2` at 2:58 with four boars on the floor. Nothing about the released budget is visible in that HUD line. Required: say what the screenshot actually shows (BOARS is the unaccounted-for count), and either cite the evidence that really shows the budget was released or drop the claim. No code change is implied, so the `Code commit:` and both harness lines still stand.

## Notes (non-blocking)
- `src/server/Match/init.luau`, `dispatch` + `applySpawnSounder` — a stall caused by the RUNTIME refusing is never counted and never warns, so claim 8 holds only for the pure-machine hold-back. `Phase.releaseFailed` does `next_.holdbacks += 1`, but it is called from `applySpawnSounder`, which runs inside `applyEffects` — after the `if state.holdbacks > holdbacksBefore then stats.sounderHoldbacks += …` block. The retry re-emits on the next step (`nextReleaseAt = now`), and `maybeRelease` then does `state.holdbacks = 0`, so a permanently refusing runtime oscillates 0→1→0: `stats.sounderHoldbacks` stays 0 and `state.holdbacks == Match.CONFIG.SOUNDER.HOLDBACK_WARN` is never true. Only `stats.releasesDeferred` shows it. Count the hand-back too, or warn on `releasesDeferred` as well.
- `src/shared/Flags/init.luau`, `Flags.DEFAULTS.BOAR_SOUNDERS` — the row's comment is now false: "today nothing reads this row and turning it on changes nothing a player can see … THAT IS THE NEXT TASK". This task is that task, and `Match.CONFIG.SOUNDER.ENABLED = Flags.isOn("BOAR_SOUNDERS")` reads it. One line.
- `tests/server/match_phase.spec.luau`, `liveConfig` — the comment "The flag-off identity proof must not be able to agree with a changed CONFIG: if somebody moves the 90-second rhythm, this fails" overstates it. The spec's literals are independent of `Match.CONFIG` (its `INTERMISSION_SECONDS = 10` and `SCORE_SECONDS = 5` already differ from the shipped 20 and 25), and nothing binds `RELEASE_INTERVAL_SECONDS`/`FIRST_RELEASE_SECONDS`/`BOARS_PER_DRIVE` to the shipped values the way `EXPECTED_SIZE` is bound in "keeps EXPECTED_SIZE equal to the mean of the weights". Moving `Match.CONFIG.RELEASE_INTERVAL_SECONDS` would not fail this spec. Either reword, or add the equality assertion against `Match.CONFIG`.
- `docs/design/drive.md` §12.1 item 2 (released mix within ±20 % of `SIZE_WEIGHTS`) — the deviation is argued in the request's claim 3 and is arithmetically right, but `TASKS.md` row 61a queues only the §12.1 item 5 deviation. Queue item 2 as well, or the design keeps a promise no correct implementation can keep.
- `src/server/Match/Phase.luau`, `releaseGap` — the §12.1 item 5 deviation (worst last release 521 s, 25/200 drives past the tail) comes from the design's own formula, not from `GAP_MIN_SECONDS` alone: `window / releasesLeft` with `releasesLeft == 1` schedules the next release at exactly `phaseEndsAt - TAIL_SECONDS`, before jitter adds up to 15 s. Worth saying in 61a(a), since "move GAP_MIN" would not fix it.
- `src/server/Match/Phase.luau`, `maybeRelease` — the jitter after release N and the size of release N+1 are the same `Phase.unitHash(seed + driveNumber, soundersReleased + 1)` value, so a long gap deterministically precedes a bigger sounder and a short gap a single. It matches the design's pseudo-code in §6.6 exactly, so it is the design's number to change; worth queuing before the pacing is tuned.
- `src/server/Match/init.luau`, `applySpawnSounder` — `position` is typed `Vector3?` and is passed to `Runtime:spawnSounder(center: Vector3, …)`, which does `center + Vector3.new(…)` for any size ≥ 2. Unreachable today (a drive cannot leave `Waiting` without the `boarSpawn` tag), but a nil there would be a pcall failure and a permanent hand-back loop rather than the `config.spawnPoint` fallback `Runtime:spawn` has.
- `TASKS.md` row 61 — still cites `[harness] PASS: 30/30 @ d10817a`, while the request names `14dfda9` for both lines. Paperwork only.
- `GAME_DESIGN.md`, Boar AI owner row — the added "Since Task 60 it also owns sounders …" paragraph is Task 60's mirror, not this task's (rule 4). Harmless and accurate; just outside the scope the request states ("Nothing else changed: no client file, no wire field, no new owner").

---
REVIEWER verdict on commit `90a13f79afaf347023ad5a745f1842a178b69174` (round 1) · 2026-09-26 16:05 UTC · session cost $2.79, 30 turns · written by tools/agents.py
