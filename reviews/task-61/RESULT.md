PASS

## Notes (non-blocking)
- `reviews/task-61/REQUEST.md`, `sounder-drive-6` bullet — round 1's blocking finding is fixed: the bullet now reads "`BOARS` is the count not yet accounted for (`BOARS_PER_DRIVE` minus killed plus escaped, `Match.snapshot` `boarsLeft`), NOT the released count", which is what `Match.snapshot` (`boarsLeft = math.max(0, Match.CONFIG.BOARS_PER_DRIVE - state.boarsAccounted)`) does. Both harness lines name `Code commit: 14dfda9`, "(clean tree)", and `.agent-evidence/paperwork-after-code-commit.txt` says "Not paperwork: none". Code is byte-identical to round 1.
- `src/server/Match/init.luau`, `dispatch` + `applySpawnSounder` — round 1's note is still open and still true. `Phase.releaseFailed` increments `holdbacks`, but it runs inside `applyEffects`, *after* the `if state.holdbacks > holdbacksBefore then stats.sounderHoldbacks += …` block; the retry sets `nextReleaseAt = now` and the next `maybeRelease` does `state.holdbacks = 0`, so a runtime that keeps refusing oscillates 0→1→0, `stats.sounderHoldbacks` stays 0 and `state.holdbacks == HOLDBACK_WARN` is never true. Claim 8 holds only for the pure-machine hold-back. Count the hand-back, or warn on `releasesDeferred` too.
- `src/server/Match/Phase.luau`, `Phase.unitHash` — consecutive indices differ by exactly `12345 % 10007 = 2338`, i.e. `+0.2336` mod 1, so within one drive the drawn sizes are a fixed rotation determined by the first draw, not an independent sample per release. Verified by hand against the request's own claim 5: seed base `1 + 1 = 2` gives `u₁ = 7530/10007 = 0.7525` → size 3, `u₂ = 9868/10007 = 0.9861` → size 5 clamped to the remaining 3, hence "3, 3". This also makes `"draws the weights it is given, within 20%"` a test of an equidistributed sequence rather than of a random draw — it still checks the weight-to-size mapping, so it is not a dead test, but it is weaker than it reads. Worth queuing before `SIZE_WEIGHTS` is tuned.
- `reviews/task-61/REQUEST.md`, claim 1 "byte-for-byte today's drive" — overstated in two ways the request itself documents elsewhere: `MAX_ALIVE_BOARS` went 4 → 6 outside the flag (claim 9), and `maybeRelease` gained a second guard, `entries + size > capacity`, which also applies with the flag off. The old guard was `alive >= config.MAX_ALIVE_BOARS` only. `match_phase.spec`'s `liveConfig` pins `MAX_ALIVE_BOARS = 6` as a literal, so the identity proof is against the *new* ceiling. Both changes are safe (they only widen, or hold back where `Runtime:spawn` would have asserted), but "byte-for-byte" is not the word.
- `reviews/task-61/REQUEST.md`, third screenshot bullet — "three of them clustered inside 20 studs (a sounder) and two lone boars elsewhere — the designed mix" reads a released mix out of the field. With `SEED = 1`, drive 1's sizes are 3, 3 (the request's own claim 5), so two lone boars in drive 1 are dissolved members — which the request's own "an idle sounder dissolves within a minute" predicts — not released singles. Say "a group and two strays" rather than "the designed mix", or name the drive number.
- `src/server/Boar/init.luau`, `CONFIG.maxBoars` comment — this change made it stale: "The drive's own MAX_ALIVE_BOARS = 4 counts only boars that are neither DOWN nor GONE" while `Match.CONFIG.MAX_ALIVE_BOARS` is now 6. One line, in the other owner's file.
- `src/shared/Flags/init.luau`, `Flags.DEFAULTS.BOAR_SOUNDERS` — round 1's note, still open: "today nothing reads this row and turning it on changes nothing a player can see … THAT IS THE NEXT TASK" is now false, `Match.CONFIG.SOUNDER.ENABLED = Flags.isOn("BOAR_SOUNDERS")` reads it.
- `src/server/Match/init.luau`, `applySpawnSounder` — the fake-world fallback `return { world.boars:spawn(position) }` spawns exactly one boar for any `animals`, while `Phase` has already committed `boarsReleased += size`, and `stats.boarsReleased += #handles` then records 1. Unreachable in production (the real `Runtime` has `spawnSounder`), but a fake that takes this path silently diverges from the budget.
- `TASKS.md` row 61 — still cites `[harness] PASS: 30/30 @ d10817a` while the request names `14dfda9` for both lines. Paperwork only; `docs/design/drive.md` §12.1 item 2 (released mix within ±20 % of `SIZE_WEIGHTS`) is still not queued in row 61a, which carries only the item 5 deviation.

---
REVIEWER verdict on commit `31d6b91ef78db72debca4f5ccb3af290ea77bff4` (round 2) · 2026-09-26 16:11 UTC · session cost $2.54, 32 turns · written by tools/agents.py
