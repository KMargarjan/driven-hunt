1. `src/serverstorage/MapGen/Markers.luau` / `Markers.place` — every marker the generator places is shootable, and 8 of the 14 are solid opaque blocks, against an explicit design paragraph that gives the gameplay reason. Evidence: design §4.3 — "Every marker is an **`Anchored`, `CanCollide = false`, `CanQuery = false`, `Transparency = 1` `Part`** under `Workspace.DrivenHuntMap.Markers` … Invisible and inert: a marker that can be shot, walked into or collided with is a gameplay object pretending to be metadata." The code sets `part.Anchored = true` and then `part.CanCollide = SOLID[marker.kind] == true` with `local SOLID = { shooterPost = true }`, `part.Transparency = if SOLID[marker.kind] then 0 else 0.5`, and **never sets `CanQuery`**, which defaults to `true`. `Weapon.Cast.world` builds `RaycastParams.new()` with `FilterType = Exclude` and the shooter's ignore list only — it sets no `RespectCanCollide`, so `CanCollide = false` does not exempt a part from a pellet. The result is a 340×1×1 `DriveLine` part on the shooter line, a 120×1×12 `DriverStart` and four 6×1×6 `BoarSpawn` pads that stop shot, plus eight solid 4×6×4 white posts, and 13 translucent white boxes visible in every screenshot. The request does not mention the divergence and neither does row 43a. Required: build the markers as §4.3 says — at minimum `CanQuery = false` on all five kinds, since nothing in the game raycasts a marker — or, if the grey box needs the posts solid and the markers visible until real geometry arrives, take that to `ESCALATE.md` so the Architect changes §4.3, and say so in `Markers.luau` and the request.

2. `src/serverstorage/MapGen/Layout.luau` / `Layout.hedgerow` with `Props.hedge` — the hedgerow is an unbroken 7-stud collidable wall straight across the drive corridor, between the boar spawns and the shooter line. Evidence: `Config.SLICE.hedgerow = { z = 40, fromX = -230, toX = 230 }`, `Config.HEDGE = { segmentStuds = 12, size = Vector3.new(12, 7, 3) }`; `Layout.hedgerow` emits `for index = 0, count` at `x = hedge.fromX + index * config.HEDGE.segmentStuds` with `count = math.floor(460 / 12) = 38`, i.e. 39 centres from x = −230 to x = 226, each a 12-wide segment — continuous cover from x ≈ −236 to 232, with no rejection of the corridor anywhere in `Layout.hedgerow` or `Props.hedge`. The corridor is `corridor = { minX = -190, maxX = 190, minZ = -210, maxZ = 210 }`, the drivers start at `driverStartZ = 180`, the boars at `boarSpawnZ = 150`, the line at `lineZ = -180`; `Props.hedge` places each segment through `part(...)`, which sets no `CanCollide`, so every segment collides, and `Boar.CONFIG.AGENT` has `AgentCanJump = false` / `AgentCanClimb = false`. The only ways past are x < −236 or x > 232, outside the corridor at the slice edge. The design contemplates exactly this being solved — §5.3: "Everything discrete (which tree, where, **hedgerow gaps**) comes from `Random.new(seed * 1000 + LAYER_ID)`" — and `Scatter.LAYER = table.freeze({ trees = 1, hedge = 2 })` declares the layer, which nothing in the repo ever uses (`grep LAYER` finds `Scatter.LAYER.trees` only). Design §1.1 item 7 requires the markers to be "**reachable by the boar's own pathfinding agent**"; §13.1 check 5 is the check, and it is not marked *(map branch)*, but in M2.1 it runs against the arena, so nothing looked at the slice. This also makes claim 10's "`map-corridor` an open lane toward a wood edge" hard to hold: the shot is camera (0, 12, 190) → (0, 0, −190), whose sightline crosses z = 40 at y ≈ 7.3, i.e. grazing the top of the wall, with the whole lane below it behind the hedge. Required: leave a gap where the hedgerow crosses the drive corridor (the seeded `Scatter.LAYER.hedge` the design already names, or a plain corridor-width break in `Layout.hedgerow`), or move the line clear of the corridor — and say in the request which the slice has.

## Notes (non-blocking)
- Request, the `[mapgen]` block — "the last three lines of `python tools/mapgen.py verify --seed 7 --backup census`, verbatim". Each line matches its `print` exactly, but they are not consecutive: `command_verify` runs 22 `print_step` lines and `log_run`'s `[mapgen] run log: …` between `build 1:` and `build 2:`, so the real last three are the run-log line, `build 2:` and the `OK:` line. Call it three lines from the run. (Round 1's note 8 is otherwise closed — the `build N:` / `OK:` formats and the research note's `[mapgen] OK  step 2/22 · terrain tile 1/16 (x=-256..-128, z=-256..-128) · voxelsWritten=24576 · 6 ms` now match `print_step` and `command_verify` character for character.)
- `src/serverstorage/MapGen/init.luau` `runStep` — `Props.clearAssets(root)` is called only in the `props` branch, but `Props.hedge` (step 18) also calls `Props.template`, which creates `root.Assets`. Running the hedgerow step alone (`mapgen.py step 18`) would leave the template folder under the map root for good, where `partsUnder` and `digestEntries` both count it. Dormant while `Assets.ROWS` is empty; clear it in every branch that can create it, or at the end of the run as design §8.2 says.
- `src/serverstorage/MapGen/Props.luau` `placed` — the asset branch does `clone:PivotTo(CFrame.new(position))` with `position` at the ground height, while the proxy sits at `y + trunk.Y / 2`. A model whose pivot is its centre lands half-buried. Belongs in row 43a(d) with the rest of the unexercised insertion branch.
- `src/serverstorage/MapGen/init.luau`, the `markers` step — `partsCreated = tagged + Σcounts` = 26, but 12 of those are trunks `Props` placed and `Markers` only tagged. Report the tagged count separately from parts created.
- `src/serverstorage/MapGen/Contract.luau` — `return require(if editTime then module:Clone() else module)` does work on require, while `MapGen/init.luau`'s header still says "NOTHING HAPPENS ON REQUIRE (the rule src/server/Boar/init.luau states in its own header)". True at run time, not in Edit; one clause in the header would close it.
- Request claim 2 — "asserts `total == planned + tieTrees`, so a sixth marker kind without a tag breaks it". What an untagged kind breaks is `expect(Map.TAGS[kind]).to.be.a("string")` in the same `it`; the `total` line constrains `MapGen.expectedCounts` against `Layout.markers` plus `SLICE.tieTrees` (a hardcoded `tree` count or an extra key fails it), which is a different, real thing. The conclusion holds, the mechanism named does not. Finding 2 of round 1 is otherwise closed: `counts.shooterPost` / `counts.boarSpawn` now compare against `c.SLICE.postCount` / `#c.SLICE.boarSpawnX`.
- Measurement B's handover cannot be checked from here: the dispatch routes it to `<runs-dir>/43-save.txt`, outside the repo, and says "write `NEEDS SAVE` … and stop". The request says the note was left and then went to review. Not something I can verify either way, and the measurement needs the Director's click — but the task is not done until it is made, and the request's own warning stands: the slice is sitting in Workspace, so the next `python tools/studio_mcp.py test` fails `map_contract.spec` until `mapgen.py clear` runs.
- Evidence is consistent: `[harness] PASS: 27/27` and `[harness2] PASS: 30/30`, both at `1438665` "(clean tree)", which is the `Code commit:`; `.agent-evidence/paperwork-after-code-commit.txt` says only `reviews/task-43/REQUEST.md` changed after it. selene, stylua and `rojo build` all exit 0. Claims 3, 4, 5, 6 and 9 check out against the files (`Map` frozen via `Shotgun.deepFreeze` with no assignment anywhere; `TestArena`, `Match.Markers` and `MapGen.Markers` all read `Map.TAGS`, values unchanged in the diff; `Ground` the only `Terrain` writer, `Settings` the only `StreamingEnabled` writer; the 22-step plan, 16 tiles, 39 hedge segments, 14 markers and 153 parts all reconcile).

---
REVIEWER verdict on commit `e87a75d116f247dade7ba1547cc5573297302359` (round 2) · 2026-09-26 02:44 UTC · session cost $4.27, 49 turns · written by tools/agents.py
