PASS

## Notes (non-blocking)
- `TASKS.md` row 43 — still reports round 2's build: "**Result: 22/22 steps, 153 parts, the same digest twice** (`87abf267…82b1109f…`)". The gate removed 7 hedge segments, so it is 146 parts and digest `43dc6aeb…82b1109f…`. Update the row, or leave it to the merge report.
- Request claim 2 — "Three specs in `describe("the generator's hedgerow")` assert it **for twenty seeds**". Only `it("leaves a gate wide enough for the drive, inside the corridor")` loops `for seed = 1, 20`; `it("puts no hedge segment inside the gate")` and `it("puts the gate in the same place for one seed…")` use `SEED` and `SEED + 1` only. The three claims are each asserted; the twenty seeds cover one of them.
- Request claim 7 — "both call `Props.template(key, root)` first and proxy only on `nil`, **through one `placed()` helper**". `Props.hedge` does. `Props.trees` calls `placed()` only in its `if template then` arm; its proxy arm builds the trunk-plus-crown `Model` inline with `part(...)`. The verifiable half of the claim (template first, proxy on `nil`, `proxyUsed` lists what was proxied) holds for both.
- `src/serverstorage/MapGen/Markers.luau` `Markers.tagged` and `src/serverstorage/MapGen/Settings.luau` `Settings.current` — no caller anywhere (`grep Markers\.tagged|Settings\.current` over `*.luau`/`*.py` finds only the definitions). Same class as row 43a(g)'s `markerDigest`: either name the reader and the milestone, or queue them together.
- `src/serverstorage/MapGen/Config.luau` `Config.STAND_HEIGHT_STUDS = 3.5` — unused: `Markers.place` derives the centre as `ground + marker.size.Y / 2`. It is also a second copy of `Match.CONFIG.STAND_HEIGHT_STUDS` (`src/server/Match/init.luau`), which is the shape rule 3 exists to stop. Drop it or read it.
- `src/serverstorage/MapGen/Props.luau` `Props.trees` — the scatter's `reject` is `inCorridor or onTrack` only, and nothing rejects the hedgerow line. `SLICE.hedgerow` runs to `x = ±230` while `corridor.maxX = 190`, so a tree at `x ∈ (190, 250)`, `z ≈ 40` can stand inside a hedge segment. Cosmetic, on the flank, seed-dependent; belongs with row 43a(b).
- `Code commit: 87cdf86` names a commit whose only change is one `TASKS.md` line (`git log` stat: `TASKS.md | 2 +-`), where git workflow step 4's definition — "the last commit that changed anything but the loop's own paperwork" — gives `895300e`. The bound is stricter, not weaker, and the harness line names the label, so nothing is unevidenced. Same D note as 36a(3)(f) and 38a(h); it keeps recurring because the status-row commit lands after the code commit.
- `tools/mapgen.py` docstring — "The final line is the one the Builder pastes into `reviews/task-<N>/REQUEST.md` … `[mapgen] OK: 22/22 steps @ <sha> seed=7 digest=<64 hex> (clean tree)`". That is `command_build`'s line; the line this request pastes is `command_verify`'s `[mapgen] OK: same seed twice, same digest @ …`. Name both, since `verify` is the one the design asks for as evidence.
- `src/serverstorage/MapGen/Markers.luau` and `src/server/Match/Body.luau` — with the posts now `CanCollide = false`, `Body`'s stand point (`post.Position + post.Size.Y / 2 + config.STAND_HEIGHT_STUDS`, i.e. ground + 9.5 for a 4×6×4 post) drops a shooter through the marker onto the terrain at the M2.5 switch. §4.3 asks for exactly this inertness, and the pads are flattened to `GROUND_Y`, so the landing is the ground rather than a hole — but it is a behaviour change nobody has stood in. Worth a row on the M2.5 task beside 43a(k).
- Evidence is consistent. `[harness] PASS: 27/27` and `[harness2] PASS: 30/30`, both `@ 87cdf86d63d010d2c5c48b6b03de5d1d766e8ae2 (clean tree)`, which is the `Code commit:`; `.agent-evidence/paperwork-after-code-commit.txt` — "Files changed between them: ['reviews/task-43/REQUEST.md'] · Not paperwork: none · OK". `selene src`, `selene --config tests/selene.toml tests`, `stylua --check src tests` and `rojo build` all exit 0. The `[mapgen]` block's `895300e` differs from `87cdf86` only in `TASKS.md`, as the log's stat confirms. No secret, absolute path or personal datum in the diff. Claims 1, 3, 4, 5, 6, 8 and 9 check out against the files; 10 and measurement B need Studio and Karen, and the request says so.

---
REVIEWER verdict on commit `a4249be1b0311558b75376b7dd80f9234325f353` (round 3) · 2026-09-26 03:03 UTC · session cost $3.14, 48 turns · written by tools/agents.py
