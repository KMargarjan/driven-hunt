1. `src/serverstorage/MapGen/Props.luau` — `Props.trees` and `Props.hedge` never consult the asset manifest, so the design's proxy branch is not built and the step report claims a fact it did not check. Evidence: `Props.trees` goes straight from `Scatter.trees(...)` to `part("Trunk", ...)` / `part("Crown", ...)` and ends `return #points, { Assets.KEY.treeSpruce }`; `Props.hedge` likewise ends `return #points, { Assets.KEY.hedge }`. Neither calls `Assets.byKey` or `Props.template`. `grep -rn "template\|byKey" src` finds `Assets.byKey` called only from inside `Props.template`, and `Props.template` referenced only by its own definition — nothing in the repo calls it. Design §8.3 requires the opposite: "If `Assets.byKey` returns `nil`, `Props` places a **proxy** ... each asset id later replaces one proxy with no code change", and §14 scopes it into M2.1 ("50 trees from one asset key (**a proxy if there is no id yet**, §8.3)"). Consequently `proxyUsed` reports the key whether or not a template exists, and the request's disclosure ("`Props.template` ... is written but **never exercised**, because `Assets.ROWS` is empty") and `Props.luau`'s own header ("UNEXERCISED IN M2.1: `Assets.ROWS` is empty, so this returns nil for every key") both name the wrong cause: an id in `Assets.ROWS` would change nothing, so row 43a(d)'s "the first real asset id is what tests it" is false — the id would be silently ignored while the step still printed `proxyUsed`. Required: call `Props.template(key, root)` from `Props.trees` and `Props.hedge` and place a proxy only when it returns nil, reporting `proxyUsed` only for the keys actually proxied — or, if wiring it is M2.3's, say in `Props.luau` and in the request that the asset route is not called at all, make `proxyUsed` honest, and queue *that* instead of 43a(d) as written.

2. `tests/server/map_contract.spec.luau` — `it("plans exactly the markers the generated map is checked for")` asserts three values against themselves, so the claim in its name is unverified. Evidence: the test builds `counts` by `for _, marker in ipairs(MapGen.Layout.markers(c)) do counts[marker.kind] = (counts[marker.kind] or 0) + 1 end`, then compares it with `local expected = MapGen.expectedCounts()`. `MapGen.expectedCounts` in `src/serverstorage/MapGen/init.luau` is the identical computation over the identical data: `local counts: { [string]: number } = { tree = Config.SLICE.tieTrees }` followed by `for _, marker in ipairs(Layout.markers(Config)) do counts[marker.kind] = (counts[marker.kind] or 0) + 1 end`. So `expect(counts.shooterPost).to.equal(expected.shooterPost)`, `expect(counts.boarSpawn).to.equal(expected.boarSpawn)` and `expect(expected.tree).to.equal(c.SLICE.tieTrees)` cannot fail for any layout, correct or not; only the two hard-coded `1`s and the `Map.TAGS[marker.kind]` loop bite. Required: compare the counted markers with the configuration that is supposed to produce them — `expect(counts.shooterPost).to.equal(c.SLICE.postCount)` and `expect(counts.boarSpawn).to.equal(#c.SLICE.boarSpawnX)` — and either drop the `expected.tree` line or point it at something `expectedCounts` does not assign.

## Notes (non-blocking)
- `src/shared/Map/init.luau` header — "WHICH WORLD IS THE WORLD (EXPECTED_WORLD). One string, in a diff, read by ArenaBoot and by tests/server/map_contract.spec.luau". `ArenaBoot` does not read it: `grep -rn EXPECTED_WORLD src` returns only this file, comments in `MapGen/init.luau`, and the spec. Design §9.2/§14 make `ArenaBoot` a reader at M2.5. Write it in the future tense until then.
- `src/serverstorage/MapGen/Settings.luau` `Settings.apply` — the write is guarded by `elseif typeof(current) == "boolean" and current ~= value then`, so a property that is readable but not a boolean (the four M2.6 values: two numbers, two enums) is silently skipped and lands in neither `changed` nor `unreachable`. `init.luau` then reports `"already matched the contract"`. Harmless today because all four raise "not a valid member of Workspace", but the day the engine exposes one the step will claim it set it. Report a third class, or convert before comparing.
- `reviews/task-43/REQUEST.md`, the `[mapgen]` block — presented as tool output in an indented block like the two harness lines, but assembled from two runs. `[mapgen] OK: 22/22 steps @ ... digest=... (clean tree)` is `command_build`'s line; `build 1:` / `build 2:` are `command_verify`'s, and `command_verify`'s own success line (`[mapgen] OK: same seed twice, same digest @ ...`) is not there. The tool prints full digests, never `87abf26…dd99`. Same shape in the research note's `[mapgen] OK step 2..17/22 · voxelsWritten=24576`, which `print_step` cannot emit (it prints one line per step, with the label and the elapsed ms). The numbers themselves check out: the full digest is in the note, `Ground.writeTile` returns `nx*ny*nz` = 32·24·32 = 24576, `Layout.tiles` gives 16 tiles, and 50·2 + 39 hedge + 14 markers = 153 parts. Paste verbatim, or label the block a summary.
- Request harness preamble — "`7d04836` adds the research note, the owner rows and the task rows and changes no generator code (`git diff --name-only afb983a..7d04836`)". That diff also lists `tools/mapgen.py` (the `map-hedge` camera reframing), which the prose does not name. True as written for `src/serverstorage/MapGen/`; the cited command shows a fourth file.
- The place is left holding the slice: "What I could not verify" says "the slice rebuilt in the place, ready to be saved" and the research note's measurement B says "The slice is built and tagged in the place right now". That is compatible with claim 10 (cleared *before* the harness, rebuilt after), but it means the next `python tools/studio_mcp.py test` will fail `map_contract.spec`'s "names exactly one world" and its tag counts until `mapgen.py clear` runs again. Worth stating in the Director's `NEEDS SAVE` note, since the merge gate may need a re-run.
- `MapGen.Settings` and the `settings` step are outside design §14's M2.1 scope list (`init, Config, Height, Layout, Scatter, Assets, Digest, Ground, Props, Markers`); the design's build-order table puts `MapGen.Settings` in M2.6, alone in its task because it changes client behaviour for every existing system. It is inert today only because `Map.STREAMING.enabled = true` matches the place, so `apply` writes nothing — but if `enabled` were ever set to `false`, step 22 of an M2.1 build would make M2.6's playtest-gated change with no playtest. Rule 4: queue the divergence with 43a(e).
- `MapGen.markerDigest` is dead code: nothing in `tools/mapgen.py` or the spec calls it, and its reader (design §13.1 check 11, the committed-digest comparison) is a map-branch check. Queue it with the M2.5 switch rather than leaving it unreferenced.
- Request claim 4 lists `Assets` among the pure core that "the spec proves" — `map_contract.spec.luau` has no `Assets` describe block and never calls `Assets.byKey`. Four of the five listed modules are driven.
- `Markers.tagTieTrees` counts only trunks it newly tagged (`if trunk and not CollectionService:HasTag(trunk, Map.TAGS.tree)`), while `markersFolder` destroys and rebuilds the marker parts. Re-running step 21 on an unchanged map therefore reports `partsCreated = 14` and "0 tie trees tagged" for a map that has 26 tagged instances. `verifyContract` still passes; only the step report misleads.
- `tools/studio_mcp.py` `run_test` — 41a(a)'s parity ask is met (the polling query now uses `process_call(..., timeout=0, default="")`, as `run_test2` always did), but the new comment says "a transient StudioMCP error while polling is something to retry inside REPORT_WINDOW", and `process_call` re-raises anything `still_loading()` does not match, so only loading-class errors are retried. Also the `QUERY_SET_CLIENTS_DONE` call two lines below is still a bare `studio.query` where `run_test2` wraps its equivalent in `process_call(..., timeout=10)`.
- `tests/server/test_arena.spec.luau` still spells `"DrivenHunt.ShooterPost"`, `"DrivenHunt.DriveLine"` and the rest literally. Defensible as an independent check on the contract, but the request's "three copies of one fact" framing now leaves a fourth, and nothing says which it is.

---
REVIEWER verdict on commit `faa872ff509e88172f2248ce0193dfec1ab153dd` (round 1) · 2026-09-26 02:18 UTC · session cost $5.29, 66 turns · written by tools/agents.py
