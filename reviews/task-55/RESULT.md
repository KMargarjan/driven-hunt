PASS

## Notes (non-blocking)
- `tools/meshy.py` `poll_and_finish` — a *permanent* poll error is not terminal. `if status != 200: print(...); time.sleep(POLL_INTERVAL_S); continue` loops to the 600 s deadline on a 401 (revoked key) or 404 (bad task id), then falls out to `print(f"[meshy] PENDING: ... {state}")` and **`return 0`**. Design §8 makes only "task still running" a non-failure. Fix: give up after N consecutive non-200s, and exit 1 for 401/404. The most substantive of these notes.
- `tools/meshy.py` `finish_preview` — when the thumbnail URL is absent or the download raises, it prints `"[meshy] note: the response carried no URL for {name}"` / `"note: {name} did not download"`, then still sets `record["state"] = "preview-ready"` and still prints `"the preview image is <runs-dir>/{runId}/preview.png -- LOOK AT IT"` for a file it did not write. The ASSET agent is told to Read a path that does not exist. Gate that line (and arguably `preview-ready`) on `preview.png` being in `entry["artefacts"]`.
- `tools/meshy.py` `cmd_preview` / `save_run` — `run_id` is minute-resolution (`utc_now().strftime('%Y%m%dT%H%MZ')`) and `save_run` overwrites `run.json` unconditionally, so two `preview` runs of the same key inside one minute overwrite the first record and orphan a task that has already been paid for. Rule 7 territory. Refuse when the run directory already exists.
- `tools/meshy.py` `_registry_key` — design §16 states *"The tool must handle both `REG_SZ` and `REG_EXPAND_SZ` and expand the latter with `os.path.expandvars`; that branch is covered by the selftest with a fixture"*. The code does `value, _kind = winreg.QueryValueEx(...)` and discards the kind; there is no `expandvars` and no selftest fixture. Practically inert for a `msy_` bearer token (it contains no `%`), which is why this is a note and not a finding — but the design asked for it, so either do it or record the deviation.
- `tools/meshy.py` `poll_and_finish` — `entry["createdAt"] = stamp(parse_stamp(task["created_at"]) or utc_now())` assumes `created_at` is ISO 8601. `docs/research/2026-09-26-meshy.md` item 3 names the field but not its type. If it is a Unix-millisecond integer, `parse_stamp` returns `None` and **every poll resets `createdAt` to now**, pushing the computed expiry later than the truth — the opposite direction to D9's stated "earlier, therefore safe" reasoning. Confirm the format, or keep the earliest value seen.
- `tools/meshy.py` run record vs design §4.2/§8 — the design says *"every task entry carries `expiresAt`"*, and `REQUEST.md` claim 8 says *"`expiresAt` = `created_at` + 72 h"*. No `expiresAt` key exists anywhere in the tool; it is derived by `expiry_of`/`expiry_line`. Derived is defensible (and better than a stale field), but it is an unrecorded deviation and the claim's wording names a field that is not there.
- `tools/meshy.py` `validate_brief` — returns `"texturePx": data.get("texturePx", TEXTURE_PX_DEFAULT)` with no type or range check, while every other field is validated. A string or a 16384 flows straight into Task B's `texture_resolution`.
- `REQUEST.md` claim 4 vs `selftest` — *"`art_style`, `negative_prompt`, `symmetry`, `seed`, `rig` and `rigging` are asserted **absent** from every built body"*. The loop is `for bodies in (body, got_body)`: the text body and the 3-reference multi-image body. The 1-reference `image-to-3d` body is never checked, because `got_body` is only the last iteration's.
- `tools/meshy.py` case numbering — the module docstring says *"selftest case 3 renders every line this tool can emit"* and `redact` says *"Selftest case 3 builds every line"*, but redaction is case **5**; `build_preview_request` says *"Asserted against a frozen fixture in selftest case 2"*, but the fixture is case **3**. `REQUEST.md` claim 2 has it right (case 5), so three comments are stale against one another.
- `REQUEST.md` claim 8 — cites *"`cmd_resume`; selftest case 7"*, but case 7 exercises `expiry_line` only. `cmd_resume`'s expired refusal is untested, and so is `cmd_approve`'s idempotency, which design §13.1 item 5 asks for (*"`approve` twice is idempotent"*). Both need a run directory, so a tmpdir fixture is the cost.
- `tools/meshy.py` `request` vs design §8 — the 429 row says it *"stops the whole run and prints the run id to resume"*; `raise Failed(redact(f"429 after {RETRIES + 1} tries: {last}", key))` has no access to the record and prints no run id.
- `REQUEST.md` claim 1 — *"`key` prints `fingerprint 48a80096, source=registry` and nothing else"*. `cmd_key` also prints `"[meshy] OK: key present (no network call made; --check makes the one free call)"`. Prose only.

---
REVIEWER verdict on commit `e158f87171c9e4d6b225ff55b7fb2bbd61412fa4` (round 1) · 2026-09-26 12:40 UTC · session cost $2.76, 34 turns · written by tools/agents.py
