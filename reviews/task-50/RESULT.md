PASS

## Notes (non-blocking)
- `tests/client/shoot_boar.spec.luau` + `src/client/Camera/init.luau` (`pendingLookAt`, `Camera.lookAt`) — the stated cause is very likely wrong, though the fix works against either cause. The new comments say "a MouseMovement delta is consumed by the owner's Mode.step in the very frame". The repo's own recorded finding says that cannot happen: `TASKS.md` Task 24 Builder's note ("under `MouseBehavior = LockCenter` a replayed absolute `moveTo` delivers no usable delta"), `docs/design/camera.md` §9.3, `docs/design/hit-zones.md` §1049 — and this file's *unchanged* header, three lines above the new comment: "under MouseBehavior = LockCenter StudioMCP's moveTo delivers no usable delta ... so a replayed click fired wherever the camera happened to point". The cause that fits the Director's logs exactly — 82.30°/88.96° on one client and 0.00° on the other in the same run — is a second **lookAt asker** in the shooter's client only: the harness's `stage` query invokes `LookAtRequest` at the boar, and this file's own `tracking` RenderStepped connection calls `Camera.lookAt(boar.Position + …)` every frame once `StagedTarget` latches. The driver is never staged, so nothing disturbs it. Queue correcting the comment, and note that if the `it` ever starts at the latch, `LOOK_AT_SECONDS = 15` races `TRACK_SECONDS = 15` for the same camera.
- `tests/client/camera_client.spec.luau`, "does not count a request until a frame has been rendered" — the second half (`waitUntil(5, function() return Camera.stats().lookAtLanded > before.lookAtLanded end)`) asks once and never re-asks, which is the pattern the same commit removed from `shoot_boar.spec` for the stated reason "it is one-shot by design ... so a disturbed one is simply gone". It is safe today only because `camera_client` runs before the stage. Re-asking inside the wait costs the test nothing and removes the exposure.
- `tests/client/camera_client.spec.luau`, "does not count a request that was refused" — asserts both global counters unchanged across `waitFrames(3)`, which holds only while nothing else in the client asks for a look-at. Corroboration that this is not theoretical: claim 9(c) reports the mutation failing at `:278` with "1" vs "2", and under that mutation only a *second ask* inside those three frames can move `lookAtLanded`. Pin it to a deterministic window or assert the delta against a re-read baseline.
- `tests/client/camera_client.spec.luau`, "never lands more requests than were asked for" — `pendingLookAt` is cleared on land, so construction already guarantees this; it can only fail if the two counters are wired backwards. Harmless, but it is the weakest of the three.
- `src/client/Camera/init.luau` (`update`) — `pendingLookAt` is never cleared when a request is superseded or when the mode is `Dead`, so a stale point can be counted as landed much later by a frame that happens to point at it. Nothing reads the counter that way today; it would break any future 1:1 ask→land assertion.
- `tools/studio_mcp.py` (`Studio.send_input_many` docstring, and the `replay_input` comment above it) — "measured 2026-09-26: ~1 s each against a Play client" and "put a whole minute of pure latency on the critical path" contradict the module docstring ("an input batch against a PLAY client costs about 0.2 s"), the request's table (~0.2 s, 26 batches = 5 s) and the phase tables. One of the two numbers is wrong; the 0.2 s figure is the one the timings support.
- `tools/studio_mcp.py` (`selftest`, case 1) — `ok("the waits overlapped", elapsed < 0.25)` is now a wall-clock assertion in the merge-gating CI step. Overlapped is ~0.15 s and the serial mutation measured 0.252 s, so ~0.1 s of shared-runner jitter separates "green" from a false red *and* from failing to catch a regression. A larger `interval` (0.2 s) widens both margins for the same shape.
- `docs/design/camera.md` (Architect-owned) — `export type Stats` does not list `lookAtRequests`/`lookAtLanded`, the Config table does not list `LOOK_AT_LANDED_DOT`, and §9.3 still states the delta question the new comments answer the other way. Queue for the Architect.
- Scope / `TASKS.md` — rows 49a(b) and 49a(c) were closed here, and `/.assets/` was ignored as well, which 49a(b) did not ask for (it does match `docs/design/asset-pipeline.md` §7.2's "git-ignored run log", so it breaks nothing). Neither row is marked closed in the 49a cell, so the queue still shows both as todo.
- `CLAUDE.md` "Run / test" — still lists only `state`, `console`, `stop` as the other harness commands. `selftest` is now a CI step that gates the PR and is not mentioned there.
- `reviews/task-50/REQUEST.md` — claim 9 cites `camera_client.spec:256`, `:278` and the narrative cites `shoot_boar.spec:142`. CLAUDE.md step 4: cite files and symbols, never line numbers.

---
REVIEWER verdict on commit `593d51e5dc6793e8385a29fda74b5b96402efd53` (round 1) · 2026-09-26 10:45 UTC · session cost $4.40, 42 turns · written by tools/agents.py
