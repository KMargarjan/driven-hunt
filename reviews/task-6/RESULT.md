PASS

## Notes (non-blocking)
- `tests/client/input_driving.spec.luau` / `it("leaves nothing bound behind")` — the test performs the cleanup itself (`ContextActionService:UnbindAction(ACTION_NAME)` plus `connection:Disconnect()`) and then asserts `GetAllBoundActionInfo()[ACTION_NAME] == nil`, so it can only ever pass. It is testing `UnbindAction`, not this code. Move the teardown into `afterAll` and drop the assertion, or keep the teardown and stop counting it as a test.
- `it("saw the key press and release as separate events")` / `it("saw the mouse button through ContextActionService…")` — hardcoded to the committed scenario (`entry.name == "F"`, `expect(casButton).to.equal(2)`) while `expectedFrom` is generic. Editing `input_scenarios.txt` breaks these two for reasons unrelated to the channel. Derive the counts from `expected`, or say in a comment that these two pin the committed scenario deliberately.
- `replay_input` — the `readyAttribute` regex check (`re.fullmatch(r"[A-Za-z0-9_]{1,100}", ready_attr)`) runs during Play and raises, so a bad value becomes `[harness] FAIL: harness error` instead of a named check. Every other scenario validation is in `load_scenarios`, before Play ("at load time, before Play", claim 5). Move it there.
- `scenario_batches` / `check_step` — a `mouseButtonDown` before any `moveTo` leaves `last_xy` unset, so the call goes out with no position and StudioMCP refuses it at run time (the failure claim 7 describes). `load_scenarios` could refuse that shape up front like it refuses the other five.
- `it("carried the scenario's gap")` — `expect(waited.got > (unwaited or 0))` compares the slept 700 ms against unwaited gaps that in this scenario each span a StudioMCP call boundary (keyboard batch → mouse batch, mouse batch → keyboard batch). It holds only while per-call RPC latency stays well under 700 ms. The request discloses the stray-mouse-move flakiness at `matchInOrder` but not this one; worth a sentence beside `GAP_TOLERANCE`.
- `tools/studio_mcp.py` / `main`, `capture` branch — `parse_vector("1,2")` raises an uncaught `ValueError` traceback after `Studio()` has already been spawned, while the adjacent argument errors use `sys.exit` with a usage line. Catch it and `sys.exit` the same usage string.
- `reviews/task-6/REQUEST.md` preamble — "The counter's blind spot … is recorded for the Director in `TASKS.md` under Task 21a" is not what 21a says; item (d) there records the opposite bypass (a fresh `reviews/task-<N'>/` giving a round 1). The re-review-after-`PASS` case appears only in row 21's prose. Paperwork only.
- `tools/studio_mcp.py` — `mouseButtonClick` and `"button": "right"` are documented, validated and mapped on both sides but no committed scenario sends them (the Builder discloses this). Queue one scenario that exercises both, or trim them from the documented format.

---
REVIEWER verdict on commit `e26f52ac30cb22c7bbb52ce328ede8d658f55787` (round 1) · 2026-09-25 08:29 UTC · session cost $1.77, 26 turns · written by tools/agents.py
