PASS

## Notes (non-blocking)
- `tests/client/input_driving.spec.luau` / `expectedFrom` — an unrecognised `device` or `action` is silently skipped, while the harness's `scenario_batches` raises (`raise RuntimeError(f"unknown scenario device {kind!r}")`). A typo'd step would still be *sent* but produce no expectation, so the spec would pass having proved less than the file says. Fix: have `expectedFrom` record an error for an unrecognised step and assert it in `it("has a scenario on disk, synced to the client")`.
- `expectedFrom`, mouse branch — ignores `step.button` and always pushes `"MouseButton1"`, but the docstring advertises `{"device": "mouse", "action": "mouseButtonDown"|..., "button": "left"|"right"}`. A `"right"` step would be replayed but never recorded (`BindAction` binds only `Enum.UserInputType.MouseButton1`), giving a confusing failure. Fix: drop `"right"` from the documented format, or bind MouseButton2 and map it.
- `GAP_TOLERANCE` — the two comments contradict each other and the code: `-- a replayed gap may be shortened by at most this fraction (never lengthened)` vs `-- The harness sleeps this gap, so it can only ever be longer, never shorter, than asked.`, while the assertion is `waited.got >= waited.want * GAP_TOLERANCE`, i.e. it accepts a gap 40% *short*. Since `replay_input` sleeps the gap in Python, the tolerance only covers clock skew; say that.
- `matchInOrder` plus the `uis/Change/MouseMovement` expectation — a real mouse move by whoever is at the machine matches that entry just as well as the replayed `moveTo`, and an early stray move would also shrink `matched[3].at - matched[2].at` and fail `carried the scenario's gap` spuriously. The two CAS `MouseButton1` events still pin the run to the replay, so this is flakiness, not a false pass. Pinning the move harder (recording `input.Position` and comparing it to the scenario's `x`/`y`) would remove both.
- `docs/design/shotgun.md` §13.3 — cited as "Task 6 — exactly what the smallest version must prove" in the spec's header comment, the code commit message, `REQUEST.md` and the research addendum. In this repo §13.3 is "The damage entry point — Director, scope"; the Task 6 unblock is §11.3 ("**Task 6 — drive real input.**"), and it asks for a smaller shape (`tests/input-scenarios.json`, one `R` key, `UserInputService.InputBegan`). The regenerated design the Director's dispatch points at is not on this branch, so the section number cannot be checked here. The delivered behaviour does match the three things the request describes.
- `docs/research/INDEX.md` — the `toolchain` row's Decision column still reads "Rokit + Rojo 7.7 + … Studio built-in MCP harness with sync-token gate (revised rounds 2–4…)" with no mention of the Task 6 addendum that was appended to that note.
- `replay_input` — `time.sleep((step.get("ms") or 0) / 1000.0)` is unbounded, while the docstring documents `{"device": "wait", "ms": <0..10000>}`. Nothing enforces the range, so a mistyped `ms` stalls the run inside Play.
- `Studio.send_input` / `replay_input` — the per-batch `except RuntimeError` catches a tool-level error, but `_rpc`'s `self.lines.get(timeout=timeout)` raises `queue.Empty`, which escapes it and turns a hung input call into a whole-harness error rather than a failed `[input] replayed every step…` check.
- `tools/studio_mcp.py` — `mouseButtonClick` and `"button": "right"` are documented format values that no committed scenario exercises; only `keyDown`/`keyUp`/`keyPress`/`moveTo`/`mouseButtonDown`/`mouseButtonUp`/`wait` have actually been sent.

---
REVIEWER verdict on commit `c545add5499779dcf117942356dba5d2004fba0c` (round 1) · 2026-09-25 08:21 UTC · session cost $1.95, 30 turns · written by tools/agents.py
