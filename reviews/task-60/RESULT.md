PASS

## Notes (non-blocking)
- `Brain:_idleSounderDirection` — executed by *"...and stays IDLE when the leader is calm"* (600 steps) but **nothing asserts its output**: `IDLE_COHESION` and `IDLE_SPREAD_STUDS` have no assertion in any spec, and the grazing shot `drive.md` §12.10 item 1 names ("a sounder of five at the spawn pad, grazing: do they read as a group, or as five unrelated boxes?") is not among the three described. Not held up because nothing in production creates a sounder, so no player can see it in this build — queue the grazing screenshot and one idle-formation case with the release task.
- `tests/server/boar_sounder.spec.luau`, *"drops cohesion and alignment, keeps moving, and spreads harder"* — the fixture passes `leader = if scattering then nil else ...`, and `_sounderDirection` skips cohesion/alignment on `sounder.leader` alone (`if not sounder.scattering and sounder.leader then`). So the `not sounder.scattering` guard and `SCATTER_SEPARATION = 3.0` are not independently pinned: assertions 1–2 hold with the scatter code deleted, and assertion 3 (`toward.X < held.X - 2`) compares two runs that differ in *both* the flag and the leader's presence. One case with `scattering = true` and a leader still in the observation would pin them.
- `tests/server/boar_body.spec.luau`, *"runs as a group when a driver appears"* — `fast = math.max(fast, ...)` is the maximum over **all** members and **all** steps, so `expect(fast >= 0.4 * CONFIG.SPRINT_SPEED)` passes with the leader sprinting and four followers frozen. Design §12.8 item 4 is "**every** member's speed ≥ 0.4 × `SPRINT_SPEED`". The `together` check (all within `BREAK_STUDS` of a leader that sprints for 3 s) covers it in practice; a per-member peak would say what the design asked.
- `Brain:_sounderDirection`, the alignment term — design §6.7's table says "the leader's velocity, **else the neighbour mean**"; the code falls back to `sounder.leader.facing`. Defensible (and better for a stopped leader), but it is a second deviation from the design and the request lists only one (the `TROT_SPEED/2` floor). Record it next to that one.
- `tests/server/boar_body.spec.luau`, *"asked for real paths, and escaped even though this plate has no navmesh"* — the comment still explains the `NoPath` results as "a fact about Roblox's navmesh on a small platform floating at y = 500, **NOT** about this code". Claim 8 says the cause was the start point sitting inside the body. Same file, same task: the comment now carries a cause the change itself contradicts.
- `Runtime._stats.membersLeft` — the comment reads "left **ALIVE**: mortally wounded, or too far for too long. A despawn is not one", but `_stepSounders`'s `LEAVE_ON_MORTAL` branch matches `severity == "mortal" or severity == "lethal"`, so a shot-dead member increments it too (the live block's `memberIds == 4` after the leader is killed depends on exactly that). Design §6.7 wants the lethal case to leave; only the counter's comment is wrong.
- `docs/research/INDEX.md` — the drive row records the "1.7b addendum, Task 35" but not Addendum 2 (sounders), which rule 1 asks for. One cell.
- `src/shared/Flags/init.luau`, `BOAR_SOUNDERS` — `drive.md` §13's blast-radius table assigns this row to **57b** and the section ends "**Nothing else**"; declared here on the Director's instruction and inert, so no impact. Also, its `expires` comment says "20 days": 2026-09-26 → 2026-10-17 is 21.
- `reviews/task-60/REQUEST.md` — claim 2's "every existing path runs unchanged" is contradicted by claims 8 and 9, which both change what a lone boar does; the screenshots' "flag ON" did nothing, since claim 10 says nothing reads the flag (the shots came from calling `spawnSounder` directly); claim 5 says "no heading reversal at all" where the spec allows `reversals < 8`, and quotes seconds 4–9 for a run of 480 steps (8 s). Also `drive.md` §12.7 item 1 asks the leader's request be "on the far side of the threat in X", which the spec does not assert (its boar and threat are both at X = 0).

---
REVIEWER verdict on commit `6dbec46f5aa463cf5bf81eb11e055b7bb547dbe9` (round 1) · 2026-09-26 15:17 UTC · session cost $4.28, 40 turns · written by tools/agents.py
