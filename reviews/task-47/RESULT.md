1. `src/client/Camera/Rig.luau` / `forgetHiddenPart` — the new `DescendantRemoving` path strands a part at `LocalTransparencyModifier = 1` forever, which makes the player's own shotgun INVISIBLE in third person for the rest of that life. Evidence: `forgetHiddenPart` is `local index = table.find(hiddenParts, part) ; if index then table.remove(hiddenParts, index) end` — it removes the part from the list and never writes the value back. Reachable player path: aim (`Camera.update` calls `Rig.setLocalBodyHidden(state.blend > Config.BODY_HIDE_BLEND)`, so `hiddenNow = true` and the `Handle` is at 1), then unequip the Tool — `src/client/Weapon/Input.luau` binds `tool.Unequipped:Connect(unbind)`, and `src/server/Weapon/Hardware.luau` sets `tool.CanBeDropped = false` but leaves the Backpack, so `1`/backtick unequips while RMB is held. The reparent fires `DescendantRemoving` for the Tool and its `Handle` while `hiddenNow` is still true; the `Handle` leaves `hiddenParts` at 1. The blend then decays, `setLocalBodyHidden(false)` writes 0 only `for _, part in ipairs(hiddenParts)` — the `Handle` is not in it. Re-equip fires `DescendantAdded` → `trackHiddenPart`, which writes nothing because `if hiddenNow then` is false. Rig's own header says it is "the only writer of LocalTransparencyModifier on the local character", so nothing restores it. The OLD code could not do this: it never removed anything from `hiddenParts`, so a departed part was still written back to 0 on unhide. This change introduces it. Required: restore the part before dropping it — set `LocalTransparencyModifier = 0` in `forgetHiddenPart` when it was hidden — and cover it with a client spec that hides the body, reparents a tracked part out of the character, clears the flag and asserts the part is back at 0.

2. `tools/mapgen.py` / `check_backup` — the new orphan refusal fires during a normal partial build and blocks `mapgen.py step`, the documented retry, for 256 of the 268 steps. Evidence: the guard is `if command != "clear" and cells > 0 and not any(r["name"] == root_name() for r in rows):`. In `src/serverstorage/MapGen/init.luau`, `MapGen.steps` is `add("clear", ...)` followed by one `terrain` step per tile (`Config.SIZE_STUDS = 2048`, `Config.TILE_STUDS = 128` → 256 tiles, steps 2–257), and `ensureRoot()` is called for the first time only in the `hedgerow` branch — so through the whole terrain phase Workspace legitimately holds terrain and **no** `DrivenHuntMap`. That is exactly the state the guard calls orphaned. `mutating = args.command in ("build", "step", "clear", "verify")` and `check_backup(studio, args.backup, args.command)` runs for all of them, so `python tools/mapgen.py step 3 --backup census` — or resuming after a step failed or timed out, which is why `step` exists ("every world-writing step is IDEMPOTENT ... That is what makes `mapgen.py step` a usable retry", `MapGen` header) — is refused, and the refusal tells the operator "a generated map's ground was left behind when its folder went away ... Run `python tools/mapgen.py clear --backup census`", i.e. destroy the partial build. Required: make the condition distinguish an orphan from a build in progress (e.g. exempt `step`, or only refuse when the last run log did not leave a build mid-flight), and keep a spec or a comment recording which states are legitimate.

## Notes (non-blocking)
- `tools/mapgen.py`, `main`, the `clear` command — `print(json.dumps(call(studio, "MapGen.clear()"), indent=2))` then `print(f"[mapgen] cleared @ {sha} (clean tree)")` and `return 0`, unconditionally. `MapGen.clear` now measures `terrainCleared = after == 0`, and `MapGen.runStep` fails loudly on it, but the CLI command the refusal message points at still prints a success line and exits 0 when cells remain. The JSON above it tells the truth, so it is visible — but this is the same "claims success it did not measure" shape the task set out to close. Queue: branch on `terrainCleared` and exit non-zero.
- `docs/design/map-generator.md` — still declares `MapGen.clear(): { removed: number, terrainCleared: boolean }`; the signature is now `{ removed, terrainCleared, cellsAfter, cellsBefore }`. Architect-owned file, so it is a queue item alongside 45a(g).
- `tools/mapgen.py`, `check_backup` — the terrain-orphan refusal lives inside the `if backup == "census":` branch only. Passing a real `.rbxl` as `--backup` skips it entirely. Claim 1's "refuses a mutating command when terrain is orphaned" is true of the census path only.
- `tests/client/camera_client.spec.luau`, `it("hides every part of the character, the Tool's Handle included")` — the new `describe` is not role-gated, and the spec's own comment at the driver test says a driver "is never given a Tool". On a driver client the title names something that is not there; the assertion is still meaningful. Either gate it on `Role` or drop the Handle from the title.
- `src/client/Camera/Rig.luau` — `Camera.stop()` calls `Rig.setLocalBodyHidden(false)` and `Rig.release()`, neither of which disconnects `hiddenAdded`/`hiddenRemoving`. They stay attached to the character and keep appending to `hiddenParts` after the camera has stopped. Harmless today (nothing calls `stop()` outside a future spec — audit-003 F7), worth closing when F7 is done.
- `src/serverstorage/MapGen/init.luau`, `MapGen.verifyContract` — the `root == nil` early return still omits `streaming` and `markerDigest` against the declared return type (row 45a(c)), and now omits `terrainCells` from `counts` as well. Not made worse by this task, but the new field joins the same gap.
- `TASKS.md` row 47b — audit-003's F1–F12 are listed and lettered individually and all thirteen L items are identifiable in the prose, but L1–L13 carry one collective "(D/L)" rather than a letter each, which is not quite what claim 8's "each classified with Task 38's G / T / D / L letters" says.
- `docs/architecture/audit-004.md` is not in this worktree (the dispatch puts it on `origin/task-46-audit`), so row 47c's F1–F11 could not be checked against their source. Claim 8's audit-003 half verifies exactly: F1–F12 plus L1–L13 = 25.
- Claims 2, 7 and 10 (the Edit-mode census, the `REFUSED`/`FAIL`/`clear` sequence, the live Play `parts=18 visible=0 Handle ltm=1.0`) and the ADS screenshot need Studio and cannot be checked here. The code can produce all of them, and the pasted harness lines are internally consistent.

---
REVIEWER verdict on commit `f7918399fe6cd200d93192793ce62b2015a19ec5` (round 1) · 2026-09-26 04:50 UTC · session cost $2.30, 32 turns · written by tools/agents.py
