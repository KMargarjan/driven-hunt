PASS

## Notes (non-blocking)
- `tests/client/camera_client.spec.luau`, `it("puts the body back when the flag clears")` — this one would pass with the whole task reverted: the old `setLocalBodyHidden` already wrote every listed part back to 0, and the test only hides then unhides with no arrival or departure in between. Fine as regression cover, but claim 6's "Four client specs, and each one failed before its fix" holds for three of the four; the request's own body only claims a measured pre-fix failure for the arrival and departure tests.
- `tests/client/camera_client.spec.luau`, `it("puts a part back when it leaves the character while the body is hidden")` — the discrimination rests on event ordering, not on the assertion. After `probe.Parent = player.Backpack`, if a `Camera.step` resumption reached `setLocalBodyHidden(false)` before the deferred `DescendantRemoving` fired, the unhide loop would write the probe (still in `hiddenParts`) back to 0 and the test would go green with `forgetHiddenPart`'s restore deleted. Deferred signals drain before the next `RenderStepped` in practice, so it does discriminate today. Also `afterUnhide` can never differ from `afterLeaving` — the probe is out of `hiddenParts` by then, so `setLocalBodyHidden(false)` cannot touch it — which makes the second number in that error message unassertable.
- `TASKS.md` row 47a — still the placeholder `(filled from the review's notes)`, so the request's "Round 1's other notes are queued, not fixed" is not yet true of the repo. Fill it with `Camera.stop()`'s two connections, `verifyContract`'s early return, the design's stale `MapGen.clear` signature and row 47b's collective L letter.
- `tests/server/map_contract.spec.luau`, `it("holds the terrain the named world requires, and no other")` — reads `Workspace.Terrain:CountCells()` directly rather than `Ground.cells()`, which this task added as the one place that answers that question. The two cannot disagree today, but the spec no longer asserts against the generator's own reader. Also "and no other" is not asserted on the map branch, which is only `cells > 0` (the request says so).
- `src/serverstorage/MapGen/init.luau`, `MapGen.verifyContract` — the `root == nil` early return returns `counts = {}`, so `terrainCells` joins `streaming` and `markerDigest` in the set of declared fields that branch omits (row 45a(c)). Not made worse, but the new field lands in the same gap.
- `src/client/Camera/Rig.luau` — `Camera.stop()` (`init.luau`, `Rig.setLocalBodyHidden(false)` then `Rig.release()`) still disconnects neither `hiddenAdded` nor `hiddenRemoving`; `Rig.release()` clears only `watcher`. Harmless while nothing calls `stop()` outside a future spec (audit-003 F7), and the request queues it.
- `docs/design/map-generator.md` — still declares `MapGen.clear(): { removed: number, terrainCleared: boolean }` against the new four-field return. Architect-owned, so a queue item alongside 45a(g).
- Claims 2, 9 and the `.screenshots/ads-body-hidden.png` description need Studio and cannot be checked here. The code can produce each of them: `map_contract.spec`'s arena branch produces exactly the quoted failure text, `check_backup` produces the quoted `REFUSED` line, and `MapGen.clear` produces the quoted `cellsBefore`/`cellsAfter`/`terrainCleared` JSON.

---
REVIEWER verdict on commit `18ec69ac9663f1df8e75874b26b7d5294a03d04f` (round 2) · 2026-09-26 05:11 UTC · session cost $2.39, 30 turns · written by tools/agents.py
