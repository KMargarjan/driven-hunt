1. `docs/design/drive.md` §4.1 (`Waiting` row) + §11.1 (`MIN_PLAYERS`=2, `ODD_PLAYER_TEAM`="Drivers", `DRIVERS_MAY_SHOOT`=false) leave the harness's single Play player unarmed, so `tests/client/weapon_client.spec.luau` (`parkTool`, `describe("the harness-driven weapon")`, `describe("the crosshair")`) and `tests/client/camera_client.spec.luau` (`describe("the harness-driven aim")`) go red the moment 1.7a lands; §8.5's "Task 24's specs are untouched" covers only the server specs. Blocks 1.7a and 1.7b. Needs a Director/Karen decision plus an amendment to the Architect-owned design before the build is dispatched.
2. `src/client/Camera/Rig.luau`, `Rig.apply` (local `drifted`) compares only `CFrame.Position` and `FieldOfView`, so a rotation-only foreign write is invisible, while `docs/design/camera.md` §10.1 claims the whole `CFrame` is compared and `tests/client/camera_client.spec.luau` (`it("is the ONLY thing writing the camera and the mouse")`) and `tests/client/weapon_client.spec.luau` (`it("never writes the camera itself")`) assert `foreignCameraWrites == 0` as the proof of rule 3. Blocks 1.7a/1.7b, whose two new client systems have no other runtime check.

---
ARCHITECT verdict on commit `c690f82d221778333ef234cbae6f41250a474d41` (audit -> docs\architecture\audit-003.md) · 2026-09-25 10:58 UTC · session cost $7.87, 75 turns · written by tools/agents.py
