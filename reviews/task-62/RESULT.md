PASS

## Notes (non-blocking)
- `cmd_status` — the one place the "poll the TASK's endpoint" rule (claim 8) was not applied. It still builds `ENDPOINTS[record["endpoint"]] + "/" + task["taskId"]` for every task, so a running **remesh** task is asked of `/openapi/v2/text-to-3d/<remesh id>`; the `if status == 200` guard then silently drops the live line rather than saying anything. `poll_and_finish` and `cmd_fetch` both use `entry.get("endpoint")`. Queue the same one-liner here: `ENDPOINTS[task.get("endpoint") or record["endpoint"]]`.
- `cmd_remesh` re-cut door vs the `brief` route — the door is `record.get("state") == "fetched" and (record.get("validation") or {}).get("ok") is False`, which is true for a `brief`-route failure too (`f"{name} is {width}x{height}, over the brief's texturePx {budget}"`). So `remesh` accepts exactly the run whose own FAILED line says "what it breaks is a number somebody wrote -- no command can fix that", and a re-cut cannot shrink a PNG. The comment above the door claims the opposite intent ("re-cutting it would spend credits for nothing"). Narrow the door to the runs whose problems carried route `remesh` — record the routes on `record["validation"]` alongside `problems`.
- `cmd_runs` / `cmd_status` — neither prints `validation`. A run stopped by `deliver`'s remesh/brief branch is left in `fetched`, so the run table shows it identically to a clean one; only the `[meshy] FAILED:` line at the time of the fetch said otherwise. A suffix on the state in `cmd_runs` would keep the table as honest as the machine-readable line.
- `deliver` / `record["validation"]` — written only when the validate step is reached, so a fetch that stops earlier (a missing deliverable, a failed download) leaves the *previous* fetch's `validation` block, `at` stamp included, on the record. Clear or re-stamp it on the early return.
- claim 9's expiry gate — the `expiry_line(record) == "EXPIRED"` refusal is really present in `cmd_refine`, `cmd_remesh` and `cmd_fetch`, but the only selftest case for it is `resume an expired run`. The three doors that spend money have no case; three fixtures aged 80 h would pin the 3-day trap where it costs credits.
- selftest, the refetch route through `cmd_fetch` — the truncated case calls `deliver(...)` directly on hand-written bytes, and the only validation failure walked end to end through `cmd_fetch` (`oversize`) takes the `remesh` route. Nothing walks cmd_fetch → validation failure → `refetch` stop → `fetch` again. The `noFbx` case covers the missing-deliverable half of that route, so this is a gap in coverage, not in the code.

---
REVIEWER verdict on commit `81389b80c5895c724c22f77bf6f658cc3e5bd510` (round 2) · 2026-09-26 17:22 UTC · session cost $2.93, 24 turns · written by tools/agents.py
