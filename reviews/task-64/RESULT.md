PASS

## Notes (non-blocking)
- `cmd_revive` / `next_phase` / `state_before_failure` — a revived run with **no tasks** prints an unrunnable next command. For `tasks == []`, `state_before_failure` returns `"brief-ok"` and `next_phase` returns `"preview"`, so the line is `next: python tools/meshy.py preview <run-id>`; `cmd_preview` takes a **brief name** (`_load_brief_file(args.name)` → `parse_brief_name`), which refuses a run id ("not a version number"). That is exactly the shape the old build wrote when a *preview* POST was rejected (`record["state"] = "failed"` with `tasks: []`). Fix: refuse a zero-task run (nothing was paid for, so `preview <key>_v<N>` is the route), or print `record["key"]`/`record["version"]` for that case.
- `cmd_revive` — the `existing is not None` guard cannot fire, and the case named for it exercises a different branch. `next_phase` returns the phase *after* `tasks[-1]["phase"]`, so `task_of(record, phase)` is always `None` for an append-ordered record; the case *"revive refuses when a task for that step exists, naming it and `resume`"* is satisfied by the `task.get("status") != STATUS_SUCCESS` loop, because the fixture's `ref-9` is `PENDING`. Either drop the guard and rename the case, or give it a fixture it can actually reach.
- `tools/meshy.py`, the comment above `RESUMABLE_STATES` — still states the rule this task removed: "`failed` is a TERMINAL: a Meshy task came back FAILED or CANCELED, a ceiling stopped the run, **or the POST never created a task**". The third clause is now `stop_unstarted`, and a ceiling raises `Refused` (exit 2) and writes nothing.
- `validate_fetched` — the new `budget` precedence (`record["textureResolution"]["px"]` before the brief's `texturePx`) has no case. The one oversize fixture (`boar.body_v1-20260101T0014Z` via `approved_run`) carries no `textureResolution`, so reverting that line to the brief's number fails nothing; the case the claim rests on is a 1024-brief run whose maps come back 2048.
- `check_request_types` — the unknown-field refusal says "`art_style` is not a documented request field; the documented ones are …", but `REQUEST_FIELDS` is the fields *this tool sends*, not everything Meshy documents. Wording only.
- `docs/design/meshy-tool.md` §4.4 and §5 (Architect-owned, so not this task's to write) do not know `revive`, the second `failed` reason (`failureKind`, a 2xx with no task id), or the `<phase>-unresolved` states; §4.4's "a ceiling stopped the run" was never true of the code. Worth queueing an Architect update alongside 64a.
- `cmd_revive` — `record.pop("stateBefore", None)` pops a key nothing in the tool ever writes.

---
REVIEWER verdict on commit `272085883dca1b7a0eeea8556c38b4ca0800f0d0` (round 1) · 2026-09-26 18:07 UTC · session cost $2.59, 38 turns · written by tools/agents.py
