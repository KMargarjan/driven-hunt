1. `tests/client/camera_client.spec.luau` — "the aim path" / "the harness-driven aim": the fake aim source is never removed, and the final test asserts counters the fake already moved, so it passes with the `aim-hold` scenario deleted. Evidence: the `beforeEach` in `describe("the aim path")` calls `Camera.setAimSource({ isAiming = function() return fake.aiming end })`; nothing restores the production source (no `afterAll`), while `docs/design/camera.md` §9.2 item 4 says "**The aim path, with an injected fake source** (`Camera.setAimSource`, restoring the production source in `afterAll`)". The two tests in that describe each drive `fake.aiming = true` and wait for `Camera.getMode() == "Aiming"`, so by the time `describe("the harness-driven aim")` runs, `Camera.stats().aimEnters` and `aimExits` are already 2 and the assertions `aimEnters >= 1`, `aimExits >= 1`, `string.find(history, "Aiming")` cannot fail — and the aim source in force at that point is the fake, returning `fake.aiming == false`. The request's own pasted evidence confirms nothing came from the button: the history `Loading,Third,Aiming,Third,Aiming,Third` contains exactly two `Aiming` entries, one per fake-driven test, and none for the replayed right mouse. Claim 6 ("The real right mouse button drove the real camera") is therefore unsupported by the spec and contradicted by the history it quotes. Required: restore the production aim source (`afterAll`), and assert the harness-driven aim on a **delta** captured after that restore (counters/history sampled before the replay's button-down and compared after), so deleting the `aim-hold` scenario fails the test.

2. `tests/client/weapon_client.spec.luau` / `it("never touched the camera while aiming")` — with this task merged, that test asserts the opposite of the camera design, and it can only pass while the camera is deaf to the real aim button. Evidence: it samples `fov = camera and camera.FieldOfView` in the `Weapon.Input.AimChanged` handler and asserts `expect(down.fov).to.equal(up.fov)` plus `drift <= Shotgun.CONFIG.CAMERA_DRIFT_TOLERANCE`, while the `weapon-fire-reload-ammo` scenario holds MouseButton2 for 500 ms and `docs/design/camera.md` §1.1 item 2 requires the camera to move to the eye and the FOV to narrow to `FOV_AIM_DEG = 50` within `AIM_BLEND_SECONDS = 0.20`. Its own comment is also now false in this place — "the stock `PlayerModule` is the camera's live writer and moves it every frame" versus the request's claim 9(a) "This place has no `PlayerScripts.PlayerModule` at all". The reported "drift 0.000 studs" and identical FOV are evidence that the real aim never reached the camera, not that the weapon behaved; fixing finding 1 makes this test fail. Claim 8 presents this as confirmation and it is not. Required: rewrite that assertion against what the weapon actually owns (that `src/client/Weapon/` performs no camera assignment, e.g. via `Camera.stats().foreignCameraWrites`), rather than "the camera did not change across an aim hold".

3. `src/client/Camera/init.luau` / `characterParts` — a character whose `HumanoidRootPart` or `Humanoid` has not replicated on the first frame leaves the camera in `Loading` for that whole life. Evidence: the lookups run only inside `if character ~= cachedCharacter then ... end`, and the only refresh afterwards is `if cachedRoot and not cachedRoot.Parent then`, which cannot fire when `cachedRoot` is `nil`; `cachedHumanoid` has no refresh at all. `update` then never enters `if root then`, so `Rig.apply`, `Viewmodel.update` and `setLocalBodyHidden` never run and `Mode.step` keeps returning `hasCharacter = false` → `Loading`. This contradicts `docs/design/camera.md` §4.2 ("`Loading` | `CharacterAdded` and `HumanoidRootPart` present and `Humanoid.Health > 0` | `Third`") and §4.1's `Loading` row, which is a transient state by design. Required: re-resolve `cachedRoot`/`cachedHumanoid` while either is `nil` for the current character (the lookup only costs a frame while they are missing), and keep the existing stale-part guard.

4. `src/client/Camera/Mode.luau` / `Mode.step`, `nextMode` — the `Dead` state is not the state the design specifies, and the deviation is not among the two the request declares. Evidence: `docs/design/camera.md` §4.1 `Dead`: "blend forced to 0 at the normal rate, viewmodel cleared **immediately**, camera holds its position and looks at the last known head point. **No orbit, no input**". The implementation suppresses look input only for `Loading` (`if mode ~= "Loading" then yaw = ... pitch = ...`), `update` keeps recomputing the camera from `root.CFrame` every frame while dead, and the viewmodel is only unparented once `blend < VIEWMODEL_SHOW_BLEND` (0.2 s later), not immediately. `tests/server/camera_mode.spec.luau` "goes Dead when the character dies" asserts only `mode` and `blend`, so nothing catches it. Claim 9 says "Two deviations from the design, both measured, both reported here". Required: implement the `Dead` row (freeze look input, hold the last applied CFrame, clear the viewmodel on entry) and assert it in the server spec; or, if the deviation is deliberate, state it and its reason in the request's deviation list so the Director can accept it.

## Notes (non-blocking)
- `reviews/task-26/REQUEST.md` claim 6 — "(the first to send `"button": "right"`, which closes Task 6a note (g))" is false: `tests/client/input_scenarios.txt` already contained `{ "device": "mouse", "action": "mouseButtonDown", "button": "right" }` in `weapon-fire-reload-ammo` before this task (the diff shows those lines as unchanged context). Note (g) was already closed.
- `tests/client/input_scenarios.txt` / `aim-hold` — `docs/design/camera.md` §9.3 specifies "The scenario therefore starts with a `keyPress` of `One` to equip from the hotbar"; the added scenario has no equip step and depends on the previous scenario leaving the Tool equipped. Either add the equip step or say in the request why it was dropped.
- `tests/client/camera_client.spec.luau` / "shows a viewmodel that is really on screen" — the whole assertion block sits inside `if model then ... else print(...) expect(Camera.stats().viewmodelParented).to.equal(false) end`, so it passes unchanged if the viewmodel is never built. Design §9.2 item 5 asserts it unconditionally; make the missing-Tool case a failure so claim 7 cannot evaporate silently.
- `tests/client/camera_client.spec.luau` — two design assertions are absent: §9.2 item 9 ("`#workspace.CurrentCamera:GetDescendants()` does not grow across those frames", the zero-instances-per-frame target of §8.2) and item 11 (teardown: `Camera.stop()` leaves no `DrivenHunt.Camera` binding, no viewmodel, `CameraType == Custom`). Queue them.
- `tests/client/camera_client.spec.luau` — `expect(stats.frames >= 120)` after `waitFrames(120)` is a cumulative total, like the `aimEnters` problem in finding 1; the occlusion test's `local before = calls ... calls - before >= 10` is the right shape. Use deltas throughout.
- `src/client/Camera/Rig.luau` / `Rig.apply` — the foreign-write detector compares only `cam.CFrame.Position` and `cam.FieldOfView`; a second writer that changes only the camera's *rotation* (or `CameraSubject`/`CameraType`) is not counted, though claim 2 leans on this number as the one-writer proof. Compare the full CFrame.
- `src/client/Camera/Cursor.luau` — it never requires `Config`: the default is hardcoded in `local behavior = if free then ... else Enum.MouseBehavior.LockCenter`, so `Config.CURSOR_LOCKED_DEFAULT` and `FREE_CURSOR_ON_DEATH` are dead values, against §8's "Every number lives in `Camera.Config` … and nowhere else". It also never reads `MouseDeltaSensitivity`, which §5.4 says it owns and for which "a non-default value is a `reassert`".
- `src/client/CameraBoot.client.luau` — `scripts:FindFirstChild("Weapon")` (as the design's §4.4 snippet has it) means a boot-order race permanently yields no aim source and no viewmodel for the session, with no warning. A `WaitForChild` with a short timeout, or a warn when it is absent, would make that visible.
- `src/client/Camera/Rig.luau` / `Rig.acquire` — on every `CharacterAdded` it re-enters two `WaitForChild(..., 10)` calls that, in this PlayerModule-less place, always run the full 20 s before failing. Harmless but it means `stockCamerasDisabled()` is stale for 20 s after each respawn.

---
REVIEWER verdict on commit `5a3945a9cfee9e3b1e5957aff3fc7985ea07da14` (round 1) · 2026-09-25 10:18 UTC · session cost $2.31, 28 turns · written by tools/agents.py
