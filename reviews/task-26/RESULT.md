1. `src/client/Camera/Rig.luau` / `Rig.acquire`, with `src/client/Camera/init.luau` / `Camera.start` — the camera does not exist for the first ~10 seconds of every session, and for ~10 seconds after every respawn. Evidence: `Rig.acquire` resolves the stock module with `local module = scripts and scripts:WaitForChild("PlayerModule", 10)` inside the `pcall`, and the write that takes the camera, `cam.CameraType = Enum.CameraType.Scriptable`, is placed **after** that `pcall`. The same file's header states the measurement that makes the timeout unconditional: "MEASURED, 2026-09-25: this place has NO Players.LocalPlayer.PlayerScripts.PlayerModule at all" (request claim 9(a) repeats it, `stockCamerasDisabled()` = false), so the 10 s elapses in full every time — that is exactly how the absence was measured. `Camera.start` calls `acquired, acquireReason = Rig.acquire()` **before** `Cursor.start()` and before `RunService:BindToRenderStep(Config.RENDER_STEP_NAME, ...)`, on the boot thread, so for those 10 s nothing is bound, the cursor is not locked, `CameraType` is not `Scriptable`, and — in a place with no stock camera module at all — the view does not follow the character. The `CharacterAdded` handler re-enters the same call, so the respawn re-assert the design calls "cheap" (§5.3: "the engine restores `CameraType` on respawn; re-asserting is cheap") costs 10 s, during which `Rig.apply` keeps writing a camera whose `CameraType` has been reset by the engine. The client spec hides it: `waitUntil(20, function() return Camera.getMode() ~= "Loading" end)`. Required: take the camera first — set `CameraType` and bind the render step and the cursor without yielding — and look for `PlayerModule` off the boot path (`FindFirstChild` now plus a `ChildAdded`/`task.spawn` retry that disables it if it ever appears), so neither boot nor respawn waits on a module this place does not have.

2. `src/client/Camera/init.luau` / `update` (the `Dead` branch) with `src/client/Camera/Rig.luau` / `Rig.setLocalBodyHidden` — a player who dies while aiming stays invisible to themselves until they respawn. Evidence: `update` is `if state.mode == "Dead" then Viewmodel.clear() elseif root then ... Rig.setLocalBodyHidden(state.blend > Config.BODY_HIDE_BLEND) ... end`, so while `Dead` nothing calls `setLocalBodyHidden`, and `setLocalBodyHidden` writes only on a change (`if hiddenNow == hidden then return end`) — the `LocalTransparencyModifier = 1` set at `blend > BODY_HIDE_BLEND` (0.60) is never cleared, and is only undone when the next character re-caches (`if hiddenFor ~= character then`). The design makes the body-hidden question a per-frame answer from the blend (§3.1 three-predicate table: "Is the local character's body hidden? | `Camera.Rig` | `blend > BODY_HIDE_BLEND`") and §4.1 `Dead` is "blend forced to 0 at the normal rate" — and the blend does decay: `tests/server/camera_mode.spec.luau`, "goes Dead when the character dies", `expect(state.blend).to.equal(0) -- the blend decays even while dead`. So at blend 0 the body must be drawn again and it is not: the death view is a frozen ADS camera at FOV 50 looking at an invisible corpse. No spec covers `Dead` on the client. Required: keep answering the predicate while `Dead` (call `Rig.setLocalBodyHidden(false)` on entering `Dead`, or call it every frame with `state.blend > Config.BODY_HIDE_BLEND` regardless of mode); if it cannot be asserted from a client spec, say so in the request.

## Notes (non-blocking)
- `tests/client/input_scenarios.txt` / `aim-hold` — the new scenario is not load-bearing. `weapon-fire-reload-ammo` already holds `button: right` for 500 ms and runs first, and the camera test's `waitUntil(60, aimEnters > baselineEnters)` is satisfied by whichever hold comes first; the request's own quoted baseline (`enters=0 ... history=2`, then `Loading,Third,Aiming,Third`) is the weapon scenario's edge, since the camera test returns on the first delta. Claim 6 as written is still true (a replayed right button drove the real camera), but deleting `aim-hold` would not fail the test. Take the baseline after the weapon scenario's aim edge, or assert the second edge pair.
- `src/client/Camera/Viewmodel.luau` header — the cited source is fabricated: `https://devforum.roblox.com/t/how-to-make-a-viewmodel/1234567`. Research source 4 and `docs/design/camera.md` §7 D both name `https://devforum.roblox.com/t/fps-using-viewmodels-the-improved-version-parts-2-out-of-3/1129877`. Rule 9; replace the URL.
- `src/client/Camera/Viewmodel.luau` / `Viewmodel.update` — below `VIEWMODEL_SHOW_BLEND` it sets `model.Parent = nil` but keeps `model`, so `Viewmodel.current()` returns a `Model` while not aiming, against design §9.2 item 6 ("`Viewmodel.current() == nil`"). The spec asserts `stats().viewmodelParented` instead, which is the weaker of the two. Either destroy it or amend the design.
- `tests/client/camera_client.spec.luau` / "holds the camera…" — `Rig.stockCamerasDisabled()` is printed, never asserted, and `Rig.acquire` now returns `true` even when the disable failed, so design §9.2 item 2 ("`Rig.acquire` reported success (the stock `PlayerModule` cameras are off)") is no longer tested by anything. Pin today's measured fact (`stockCamerasDisabled() == false`) so the day a `PlayerModule` appears the spec says so.
- Round-1 notes still open and worth queuing in `TASKS.md`: the foreign-write detector compares only `CFrame.Position` and `FieldOfView`, not rotation/`CameraType`/`CameraSubject`, while claims 1–2 lean on it as the one-writer proof; `Cursor.luau` never requires `Config`, so `CURSOR_LOCKED_DEFAULT` and `FREE_CURSOR_ON_DEATH` are dead values and `MouseDeltaSensitivity` is never read; `CameraBoot`'s `scripts:FindFirstChild("Weapon")` fails silently on a boot-order race; `expect(stats.frames >= 120)` is still a cumulative total; design §9.2 items 9 (camera descendants do not grow) and 11 (teardown) are still absent.
- `reviews/task-26/REQUEST.md` — the round-2 rewrite describes the two ADS screenshots (claim 9(b)) and the cover shot, but design §9.4's shots 1 (third person walking, grip) and 3 (aimed at the boar at ~60 studs) are no longer described anywhere in it. Paperwork only; the descriptions existed in round 1.

---
REVIEWER verdict on commit `e406bc8267e92524ad90709a2636cabd616db3a6` (round 2) · 2026-09-25 10:30 UTC · session cost $3.06, 37 turns · written by tools/agents.py
