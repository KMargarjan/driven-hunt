PASS

## Notes (non-blocking)
- `src/client/Camera/Viewmodel.luau` header — the fabricated citation is still there: `https://devforum.roblox.com/t/how-to-make-a-viewmodel/1234567`. Research source 4 and `docs/design/camera.md` §7 D both name `https://devforum.roblox.com/t/fps-using-viewmodels-the-improved-version-parts-2-out-of-3/1129877`. Second round open; rule 9. Replace the URL.
- `src/client/Camera/Rig.luau` / `Rig.acquire` with `src/client/Camera/init.luau` / `Camera.start` — when a `PlayerModule` *is* present and `disableStockCameras` fails, `acquire` returns `true, err` and `start` warns `"no stock PlayerModule to disable (" .. acquireReason .. ")"`, which describes the opposite of what happened. Design §10.1's row "The stock camera is not disabled | `Rig.acquire` returns `(false, reason)` … the client spec fails at §9.2 item 2" is no longer true of the code, and `camera_client.spec` only `print`s `Rig.stockCamerasDisabled()`. Distinguish "nothing to disable" from "disable failed", and pin today's measured `stockCamerasDisabled() == false`.
- `src/client/Camera/init.luau` / `update` — the predicate passed is `state.mode ~= "Dead" and state.blend > Config.BODY_HIDE_BLEND`, not design §3.1's `blend > BODY_HIDE_BLEND`. The extra term is what makes the corpse visible immediately rather than 0.2 s later, which is the better behaviour, but claim 2(ii) says only "called every frame whatever the mode" and the design table still says otherwise. Amend §3.1 or drop the term.
- `src/client/Camera/Viewmodel.luau` / `Viewmodel.update` — below `VIEWMODEL_SHOW_BLEND` it sets `model.Parent = nil` but keeps `model`, so `Viewmodel.current()` returns a `Model` while not aiming, against design §9.2 item 6 (`Viewmodel.current() == nil`). The spec asserts the weaker `stats().viewmodelParented`. Destroy it or amend the design. Carried from round 2.
- `docs/research/INDEX.md` — there is no camera research note and no addendum. Design §13 asks the Builder to confirm `PlayerModule:GetCameras():Disable()` and the sources' licence/maintenance status "in the research-note addendum, before writing the rest"; no file under `docs/research/` changed in this task. The `GetCameras` question was answered instead by measurement (claim 10(a)), but the confirmation the design asked for is not written down anywhere.
- `src/client/Camera/Rig.luau` / `Rig.acquire` — the `ChildAdded` watcher is never disconnected once a `PlayerModule` is found and disabled; only `Rig.release` clears it. One dangling connection, harmless today, but it will keep firing `disableStockCameras` for any later child named `PlayerModule`.
- Round-1/2 notes still open and worth queuing in `TASKS.md`: the foreign-write detector compares only `CFrame.Position` and `FieldOfView`, not rotation, `CameraType` or `CameraSubject`, while claims 1 and 3 lean on it as the one-writer proof; `Cursor.luau` never requires `Config`, so `CURSOR_LOCKED_DEFAULT` and `FREE_CURSOR_ON_DEATH` are dead values and `MouseDeltaSensitivity` is never read despite the owner row claiming it; `expect(stats.frames >= 120)` is a cumulative session total, not 120 frames of this test; `CameraBoot`'s and `Hud.start`'s `scripts:FindFirstChild(...)` fail silently on a boot-order race; design §9.2 items 9 (camera descendants do not grow) and 11 (teardown after `Camera.stop()`) are still untested by anything.
- `reviews/task-26/REQUEST.md` — design §9.4's shots 1 (third person walking, grip) and 3 (aimed at the boar at ~60 studs) are still not described in the request; only the two ADS shots and the cover shot are. Paperwork; the descriptions existed in round 1.

---
REVIEWER verdict on commit `e78dde2709538960ae23c1449ac97e56958b71c0` (round 3) · 2026-09-25 10:37 UTC · session cost $2.37, 36 turns · written by tools/agents.py
