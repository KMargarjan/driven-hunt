PASS

## Notes (non-blocking)
- `docs/design/shotgun.md` §5.1/§5.2/§5.4 — `CastFn = (origin: Vector3, direction: Vector3) -> Impact?` and the single module-level `Weapon.setCast(cast)` cannot express what step 8 requires ("`RaycastParams.FilterType = Exclude`, filter = the shooter's character and `Workspace.WeaponEffects`"): the cast closure has no shooter. Step 4 casts the *camera* ray with the same function, and under the stock third-person camera that ray starts behind the character, so an unfiltered cast resolves `aimPoint` onto the shooter's own body and `MIN_AIM_DISTANCE` silently swallows it — the same class of defect §2 item 4 corrects. Fix: give `CastFn` an ignore-list parameter (or build the production cast per shot over the shooter) and say so in steps 4 and 8.
- `docs/design/shotgun.md` §3.3, §3.2, §13.2 item 2 — `table.freeze` is shallow. `CONFIG.SPREAD_FULL_DEG` and the replica's `barrels`/`loaded` stay writable, so "frozen … has no writer" and the spec assertion "a write errors" only hold for the top-level table. Freeze the nested tables and assert on a nested write.
- `docs/design/shotgun.md` §5.2 — no lifecycle for `Players.PlayerRemoving`. Per-player `WeaponState` and the rate-limit counters are keyed by `Player`; nothing says they are cleared, which is a leak that also pins the Player instance. Name the cleanup alongside `grant`/`revoke`.
- `docs/design/shotgun.md` §5.5 vs §5.1 — `ActionRequest:FireServer("Reload")` is not an `ActionKind`, and `Break`/`Load`/`Close` are meant to be server-only primitives. State that the server accepts only `"Reload"` and `"SelectAmmo"` on that remote, so the primitive set is never reachable from a client by accident.
- `docs/design/shotgun.md` §13.1 — the preamble says specs "write their own numbers rather than importing `CONFIG`", but `weapon_shot.spec` item 2 is written in terms of `SPREAD_FULL_DEG`. Also "at least one direction beyond `SPREAD_FULL_DEG/4`" is flaky for `Slug`, which returns a single direction (~25% failure on an unseeded RNG). Fix the seed and apply that half of the assertion to `Buck`.
- `docs/design/shotgun.md` §5.4 step 10 — the safety arc is evaluated on the client's camera `direction`, not the `aim` the pellets actually take from the muzzle (step 6). Use the direction the shot uses, so a published `SafetyViolated` matches where the pellets went.
- `docs/design/shotgun.md` §3.1, §4, §5.3 — three cross-references point at "§12" for the specs; the specs are §13 (§12 is Karen's feel defaults). `REQUEST.md` claim 4 cites §13.1 correctly.
- `docs/design/shotgun.md` §14 — "Add these five rows, and **fill** the `UI / anything drawn` row rather than adding a sixth" is self-contradictory as a count: `GAME_DESIGN.md` gains four new rows, one filled row, and the Boar amendment.
- `docs/design/shotgun.md` §2 item 2 — "that file is superseded by `reviews/task-23/ARCH_RESULT.md`" is not true of Task 19's verdict file; the mis-pointed §11.1/§11.3 reference is recorded, not corrected. Harmless, but do not read it as fixed.
- `CLAUDE.md` Layout and "Files the roles talk through" — both still describe `reviews/task-<N>/` as `REQUEST.md` / `RESULT.md` / `ARCH_RESULT.md` only, while `docs/ARCHITECT_PROMPT.md` now depends on a Builder-written `BRIEF.md`. Queue the CLAUDE.md rows so the new document type is written down.
- `reviews/task-23/BRIEF.md` item 5 ("`ARCH_RESULT` should be `PASS` unless something genuinely blocks building") — the Builder never writes verdicts (CLAUDE.md, Roles). It only restates what `docs/ARCHITECT_PROMPT.md`'s design mode already says, so nothing turned on it here; keep verdict instructions out of future briefs.
- `ROADMAP.md` row 1.4 reads "third person, **first-person aim**", which this design deliberately does not ship (brief decision 1, design §9). Record the split into 1.4 plus the camera task so 1.4 is not marked done without ADS.

---
REVIEWER verdict on commit `6fe76192fa4e32b639c85399b7730a8e4f140db0` (round 1) · 2026-09-25 07:55 UTC · session cost $2.52, 34 turns · written by tools/agents.py
