# Research index

One line per note (rule 1). Newest first.

| Date | Note | System | Decision |
|---|---|---|---|
| 2026-09-26 | [shotgun-model](2026-09-26-shotgun-model.md) | The shotgun's LOOK: a side-by-side built from Parts, not a mesh | Two Meshy text-to-3D previews (40 credits) came back over-unders, so the gun is built from **Parts in code**. There is no part-built side-by-side under a usable licence to borrow -- the one open-source low-poly weapon pack is `.rbxm`/`.obj` (both unusable: binaries are banned) under a forum sentence rather than a licence -- so what is borrowed is the **pattern**: Roblox's own Weapons Kit (Limited Use License) builds a weapon as a `Tool` whose gun is "a `Model` made up of one or more `BaseParts` ... with one designated as the `PrimaryPart`" plus a handle and a tip attachment, which is what this repo already half-has. A pure piece list (`Weapon.Shape`) plus one builder (`Weapon.Hardware`), the division `MapGen.Layout`/`MapGen.Props` already uses. **Legacy `Weld` with an explicit `C0`, not `WeldConstraint`**: `Hardware.build()` returns a Tool that is not yet in the DataModel, and a WeldConstraint needs both parts parented (`Boar.Body`'s own comment). The 486 Parallelo's published numbers -- barrel 28 in, overall 45.0 in, length of pull 14.5 in, single trigger, round-bodied action -- give the only three proportions that matter (0.622 / 0.056 / 0.322 of the length); `beretta.com` is JS-rendered and returned nothing to a fetcher, so the overall length rests on one editorial source plus the arithmetic. **Dimensions only: no engraving, lettering or maker mark is modelled.** The gun stays 4.4 studs -- about 8 % oversize at Roblox's 0.28 m/stud -- because `MUZZLE_OFFSET` and `GRIP` are built on it |
| 2026-09-26 | [meshy](2026-09-26-meshy.md) | The Meshy API and the ASSET agent: one brief -> one preview a human looks at | Every one of the design's ten unfetched strings re-fetched and quoted, and **seven changed something**: `CANCELED` is a fifth status the brief never named; `consumed_credits` IS returned, so credits are never estimated; remesh reports **no** polycount, so the triangle count stays declared and no FBX is ever parsed; refine **does** take `texture_resolution`; 429 distinguishes `RateLimitExceeded` from `NoMoreConcurrentTasks`; the Terms delete API output *"three (3) days after it is generated"* -- **generated, not completed**, so expiry runs from `created_at`; and `usage/tasks` is Studio/Enterprise-only, so the free auth check answers 403 on a Pro key and that 403 still proves the key was read. `meshy-dev/meshy-cli` (MIT, TypeScript, official) is the reference for request shapes and is **not vendored**: a Node dependency tree in the one program that holds a secret |
| 2026-09-26 | [feature-flags](2026-09-26-feature-flags.md) | Feature flags: merge dark, switch on for a playtest, flip the default in git | Hodgson's toggle lifecycle made mechanical -- `born`/`expires`/`owner`/`why` per flag, and **expiry is a build failure** (`Flags.problems` -> the server spec -> the harness -> the merge gate). Transport is Roblox's own: the server resolves, publishes attributes on `ReplicatedStorage.Flags.State` with `Digest` written **last**, and clients read that. **Both of the design's unverified assumptions were measured and CONFIRMED** (2026-09-26): server-written attributes on a replicated instance reach clients, initial and on change; and an attribute set on `ServerStorage` in Edit mode reaches the server process of a Play session -- so the named fallback was not needed and is not in the code. The override is guarded three ways (`IsStudio` as an injected parameter, two harness checks that **refuse** rather than reset, and the publish-time strip), and the refusal was proved to bite |
| 2026-09-26 | [asset-pipeline](2026-09-26-asset-pipeline.md) | Asset pipeline (Milestone 2.7): Open Cloud upload, the manifest, the Loader, licences | Every URL in `docs/design/asset-pipeline.md` §11 was unfetched; all 15 were read here, and the design's HTTP shape is right (`POST /assets/v1/assets`, `x-api-key`, multipart `request` + `fileContent`, operation → `response.assetId`). **17 deltas, three of which change what gets built:** Open Cloud can never mint a **Mesh** asset ("Only Asset delivery API content accepted"), so an FBX upload yields a **Model** id and `CreateMeshPartAsync` is a derived second step, not an alternative route; `MeshPart.RenderFidelity` is `PluginSecurity` to write and `CollisionFidelity` cannot be touched at run time, so the manifest's `renderFidelity` is set at import, not by the Loader; and `AssetService.AllowInsertFreeAssets` is `RobloxScriptSecurity` on read **and** write, so a free Creator Store model nobody owns cannot be loaded by any script - which contradicts `docs/design/map-generator.md` §8.2. Corrected numbers: the mesh limit is **20,000** triangles and is **first-party** (not 21,000 community/vendor), `TEXTURE_MAX_PX = 1024` is **ours** (platform: 4096², 8000² for Decal/Image), 20 MB is **Roblox's**, and the `Highlight` limit is **255** client-side counting disabled ones (not "single digits to low tens"). Resolved for the design: `MeshPart.TextureID` **is** run-time assignable; `SurfaceAppearance` is **not**. **Both licences finally read:** Creator Store Terms grant use "in Roblox Studio and in Experiences on the Services" **by purchasing** - a *free* asset is licensed only if its creator ticked share, the same flag that decides whether a script may load it; and Meshy's Terms §3.2 say **free-plan output is owned by Meshy** under **CC BY 4.0**, which is **non-sublicensable**, against Roblox's Creator Terms demanding a **sublicensable** grant - **Karen's decision, recommend a paid Meshy plan.** `en.help.roblox.com` is HTTP 403 to every tool; readable via `.../api/v2/help_center/en-us/articles/<id>.json` |
| 2026-09-25 | [drive](2026-09-25-drive.md) | The drive: teams, the 10-minute round, boar releases, points (1.7a), and **the safety penalty and the score screen (1.7b addendum, Task 35)** | A pure reducer returning **state plus an explicit effect list** (Redux's shape without Rodux's package, so a spec can assert on the effects), inside a folder-module owner with an injected world; Roblox's own `Player.Team` as the one representation of team, enforced by the engine; `CollectionService` tags for the posts, line, spawns and trees, so the Milestone 2 map generator can move them without this system changing; two server->client remotes carrying a server timestamp instead of a pushed countdown, and nothing inbound. **Two of the design's citations are wrong:** Rodux is NOT archived (no notice, 136 commits, CI), and neither `Team.AutoAssignable`'s default nor `GetServerTimeNow`'s synchronisation guarantee is stated on the page cited for it. **1.7b addendum:** the Deutscher Jagdverband page the design cited but never fetched was fetched, and it states the rule as "in the direction of fellow hunters", not as a cone - which is the shape the design chose; it also gives the neighbour rule a number (30 degrees), queued for v1.1 |
| 2026-09-25 | [hit-zones](2026-09-25-hit-zones.md) | Hit zones and wounds (Milestone 1.5): zone geometry, the wound model, the wounded run, the carcass | Protruding, **massless**, welded, queryable zone parts on the existing physics trunk (a nested part can never be hit: the ray returns the trunk), plus a two-quantity wound model - damage decides *whether* it dies, flight decides *how far* it gets first - in a pure module inside the boar's own owner, with no new system owner. `RaycastHitbox` not adopted (it solves swept melee detection; the weapon already casts) - and the design's URL for it is wrong: the module is `Swordphin/raycastHitboxRbxl`, MIT. The Stokke et al. 2018 paper the design cited from memory is **real and validates the two flight numbers**: its `efd = 1.14*M^0.73` and `mfd = 4.92*M^0.73` give 100 and 429 studs at 80 kg against the config's 120 and 420 |
| 2026-09-24 | [boar-ai](2026-09-24-boar-ai.md) | Boar AI, grey box v1: idle wander, flee, route, despawn | PathfindingService route + Reynolds steering follow + `LinearVelocity` mover on an unanchored, server-owned body; sensing and the pathfinder injected so specs need no physics. SimplePath not adopted (no canonical repo, and we need our own mover) |
| 2026-09-24 | [map-generator](2026-09-24-map-generator.md) | Map generator (Milestone 2): terrain, vegetation, markers, assets, backups | **Studio's heightmap import cannot be driven from code or MCP**, so a seeded Luau noise heightfield (domain warping) written with `Terrain:WriteVoxels` at edit time; markers are `CollectionService` tags so the map carries no scripts; Creator Store and Meshy assets referenced **by id with a manifest, never committed**; `File -> Save to File` outside the repo before every rebuild. 2048x2048 studs, <=20k parts, <=8k visible |
| 2026-09-25 | [camera addendum](2026-09-24-shotgun.md#addendum-2026-09-25-task-26-the-camera) | The camera and viewmodel: what the shotgun note's sources 4-5 turned out to mean in practice | Sources 4-5 inherited; measured here: this place has **no** `PlayerScripts.PlayerModule`, so `GetCameras():Disable()` never runs and the foreign-write detector is the only proof of one writer; the viewmodel technique is adopted with `PivotTo`, not the post's deprecated `SetPrimaryPartCFrame` |
| 2026-09-24 | [shotgun](2026-09-24-shotgun.md) | Shotgun: viewmodel, third-to-first-person aim, hit detection, break action | Hitscan rays, client for feedback and server for truth; the server validates and never reproduces, because the Camera is not replicated. FastCast2 not adopted (no travel time at these ranges, split provenance); ACS not adopted (no confirmable licence). Slug 330 studs; buckshot 100 studs / 9 pellets / ~1.6 deg full cone. **The note argues for one camera writer and one viewmodel writer; `docs/design/shotgun.md` defers ADS and the viewmodel to a later camera task, and that cut is an open Director decision (design 13.2)** |
| 2026-09-24 | [toolchain](2026-09-24-toolchain.md) | Dev toolchain: sync, packages, lint/format/CI, tests, Studio automation | Rokit + Rojo 7.7 + Wally + selene + StyLua + TestEZ; Studio built-in MCP harness with sync-token gate (revised rounds 2–4; **addendum 2026-09-25, Task 6:** driving real keyboard and mouse input into the Play client, and saving screenshots; **addendum 2026-09-25, Task 30:** StudioMCP cannot run a 2-player test - no player-count argument and a three-value `datamodel_type` - but every tool takes a `studio_id`, which is the one open route; plus staging a scenario (place the character, ask the camera owner to aim) and two harness bugs; current behaviour: `tools/studio_mcp.py` docstring) |
