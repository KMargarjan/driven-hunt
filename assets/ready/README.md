# assets/ready/

Import-ready exports (`.fbx`, `.png`, `.ogg`, …) sized and named for Roblox. Once uploaded, record
the asset ID next to the file name, for example in `ASSET_IDS.md`, so code references IDs from one place.
